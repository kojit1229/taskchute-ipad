# CHANGES v112

## 概要

K依頼(2026-07-16)。「タスクシュート画面の未完了タスク一覧で、当日のブロックに登録済みの
タスクでも未完了なら一覧に表示したままにする(1日の中で同じタスクに複数ブロックを登録できる
ようにするため)」。

## 調査結果(重要: 除外ロジックは見つからなかった)

依頼文は「現状、未完了タスク一覧(renderOpenTasks)は当日ブロックに登録したタスクを一覧から
除外している(はず)」という前提だったため、まず現物(app.js `renderOpenTasks()`、5132行目〜)
を確認した。結果、**当日Block登録済みタスクを一覧から除外するロジックは存在しなかった**。

- `renderOpenTasks()` 冒頭のコメント(v19)に「今日に既に Block 化されていても表示し続ける
  (1日に複数回追加することもあるため)」と明記されており、そもそもこの要望どおりの挙動が
  既に実装されている。
- 当日Block登録数を数える `blockCountByTaskId` と、「本日 N 件 Block 追加済み」バッジ
  (5147〜5178行目)も既存。除外どころか、登録済みであることを示す軽量表示も既にある。
- 「今日へ追加」ボタン(`task-today` → `createBlockFromTask()`、9193行目〜)側にも重複チェックは
  無く、同じTaskへ複数回クリックすれば複数Blockが作られる(既に可能)。
- 一覧から消えるのは `isTaskDead()`(12756行目、status: completed/suspended/cancelled)による
  もので、これは「タスクが完了・中断・中止したら消える」という別の(意図した)仕様であり、
  「当日Block登録済み」による除外ではない。
- 既存の `tests/v107.test.js` の (a)(b) でも、Block完了チェックのみでは一覧に残り、タスク完了で
  一覧から消えることが既に回帰テストされていた(今回 `npm run test:core` で再確認・ALL PASS)。

WBSタブ(`renderWbsTaskRow`、5075行目)も同様に、当日登録済みなら「＋もう一度」とラベルを変えて
複数回の追加を許容しており、除外はしていない。除外して**disabled**にしているのはホームタブの
「未完了タスク」パネル(`homeBacklog()`、2786行目〜。登録済みなら「追加済み」ボタンをdisabled
にして再追加不可)だが、今回の依頼スコープは明示的にタスクシュート画面(`renderOpenTasks`)で
あり、ホームタブは対象外(必読ファイルで明示された対象関数どおり)。

## 結論・対応

app.js に機能変更は行っていない(既に要望どおりの挙動のため)。依頼文の前提は誤りだったと
判断し、代わりに以下を実施した:

1. K依頼の文言どおりの挙動(「今日へ追加」ボタンをUIから実際にクリックして同一Taskに複数
   Blockを追加できる/一覧に残ったままバッジが更新される/完了で消える/期日なし非表示・
   期日昇順が維持される)を、`tests/v112.test.js` として明示的な回帰テストで新設した
   (既存の `tests/v107.test.js` は事前seedしたBlockに対する操作が中心で、「今日へ追加」ボタン
   をクリックして複数Block化する経路そのものは直接検証していなかったため、価値のある追加)。
2. app.js自体は無変更のため、**SW `CACHE_NAME` は v111 のまま据え置いた**(taskchute-journal
   SKILL.mdの規約どおり、SWキャッシュ対象の内容が変わらないテスト追加のみのコミットでは
   CACHE_NAMEを上げない運用。v110→v111間の「test: v110/v111」コミット群も同様の扱い)。
3. バッジ(「当日ブロック登録済みが分かる軽い表示」)は既存の「本日 N 件 Block 追加済み」を
   そのまま維持した(新設不要)。

## 変更ファイル

- `tests/v112.test.js`(新規): 上記5シナリオを検証する回帰テスト
- `CHANGES_v112.md`(本ファイル)

## 検証

- `node --check app.js` / `node --check tests/v112.test.js`: exit 0
- `node tests/v112.test.js`: ALL PASS(単独実行)
- `npm run test:core`: ALL PASS(直近5本 v108〜v112 + 固定コア5本、v112含む)

## 残るリスク・未対応

- 依頼文にあった「除外をやめる際のバッジ判断」は、除外自体が存在しなかったため判断不要だった。
- ホームタブの「未完了タスク」パネル(`homeBacklog()`)は、登録済みタスクへの複数回追加を
  disabledボタンで防いでいる(タスクシュート画面とは異なる挙動)。今回のスコープ外だが、
  もしKがホームタブ経由でも複数回追加したい場合は別途対応が必要(現状は仕様として残す)。
