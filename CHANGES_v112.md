# CHANGES v112

## 概要

K依頼(2026-07-16)。「タスクシュート画面の未完了タスク一覧で、当日のブロックに登録済みの
タスクでも未完了なら一覧に表示したままにする(1日の中で同じタスクに複数ブロックを登録できる
ようにするため)」。調査の結果、依頼文が名指ししたタスクシュート画面(`renderOpenTasks`)は
既に要望どおりの挙動だったが、**ホームタブの「未完了タスク」パネル(`homeBacklog()`)に
disabledボタンによる実質的な除外が別途存在した**ため、監督者判断でスコープを拡張し、
そちらを修正した。SW `CACHE_NAME` を v111 → v112 に更新。

## 調査結果1: タスクシュート画面(renderOpenTasks)には除外ロジックが無かった

依頼文は「現状、未完了タスク一覧(renderOpenTasks)は当日ブロックに登録したタスクを一覧から
除外している(はず)」という前提だったため、まず現物(app.js `renderOpenTasks()`、5132行目〜)
を確認した。結果、**当日Block登録済みタスクを一覧から除外するロジックは存在しなかった**。

- `renderOpenTasks()` 冒頭のコメント(v19)に「今日に既に Block 化されていても表示し続ける
  (1日に複数回追加することもあるため)」と明記されており、そもそもこの要望どおりの挙動が
  既に実装されている。
- 当日Block登録数を数える `blockCountByTaskId` と、「本日 N 件 Block 追加済み」バッジ
  (5147〜5178行目)も既存。除外どころか、登録済みであることを示す軽量表示も既にある。
- 「今日へ追加」ボタン(`task-today` → `createBlockFromTask()`、9193行目〜)側にも重複チェックは
  無く、同じTaskへ複数回クリックすれば複数Blockが作られる(既に可能)。
- 一覧から消えるのは `isTaskDead()`(12756行目、status: completed/suspended/cancelled)による
  もので、これは「タスクが完了・中断・中止したら消える」という別の(意図した)仕様であり、
  「当日Block登録済み」による除外ではない。
- 既存の `tests/v107.test.js` の (a)(b) でも、Block完了チェックのみでは一覧に残り、タスク完了で
  一覧から消えることが既に回帰テストされていた(今回 `npm run test:core` で再確認・ALL PASS)。
- WBSタブ(`renderWbsTaskRow`、5075行目)も同様に、当日登録済みなら「＋もう一度」とラベルを
  変えて複数回の追加を許容しており、除外はしていない。

## 調査結果2: ホームタブ「未完了タスク」パネル(homeBacklog)が実際の除外元だった

監督者からの追加指摘を受け、Kが日常的に触る画面はホームタブが中心である可能性を踏まえて
`homeBacklog()`(2786行目〜)を確認したところ、こちらには明確な除外(disabled化)が存在した。

- 修正前: 当日Block登録済み(`todayTaskIds.has(t.id)`)なタスクは、「＋今日に追加」ボタンが
  `disabled` になり「追加済み」という文言に固定されていた(再追加が一切できない)。
- タスク自体は一覧から消えてはいなかった(disabled表示のまま残る)ため「一覧から消える」との
  完全一致ではないが、**「同じタスクに複数ブロックを登録できない」というKの体感の実害はこちらが
  原因だった可能性が高い**と判断した(タスクシュート画面は既に複数回追加できていたため)。

## 対応

### 1. homeBacklog() のdisabled化を撤去し、renderOpenTasksと同じ思想に揃えた

- 除外(disabled)を撤去し、常に有効な「＋今日に追加」ボタンを表示するようにした。
- 当日Block登録済みであることは、`renderOpenTasks`の`blockCountByTaskId`と同じ流儀で
  `todayCountByTaskId`(taskId→当日Block件数)を新設し、`home-due-sub`行に
  「本日 N 件追加済み」という軽いバッジで示すようにした(除外・disabled化はしない)。
- 対象: `app.js` `homeBacklog()`(2786行目〜)。

### 2. ホーム側の表示ルール(期日なし・並び順)の統一は今回見送り

`renderOpenTasks`との差分を確認した結果、以下の違いがあることを確認したが、**disabled解除+
バッジ追加という今回のスコープを超える(既存のv88折りたたみテスト等への影響範囲が広がる)ため、
統一は行わなかった**:

- 期日なしの扱い: 両方とも除外で一致(`renderOpenTasks`は`Boolean(task.dueDate)`、
  `homeBacklog`は`t.dueDate &&`)。差分なし。
- 並び順: 両方とも期日昇順だが、同一期日のタイブレークが異なる。`renderOpenTasks`は期日→
  タイトルのja比較。`homeBacklog`は期日のみ(タイブレーク無し、元配列順)。
- 表示範囲(v97/v88の窓): `renderOpenTasks`は全件取得し、選択日+7日以降は「折りたたみ+
  トグルで全展開可能」(データは隠さない)。`homeBacklog`は選択日+7日を超えるタスクを
  **そもそも取得しない**(トグルで見る手段が無い)。+3〜+7日は折りたたみで展開可能。
- 除外対象Projectの判定方法: `renderOpenTasks`は「Wish種別Projectのタスク」+「kind==="other"の
  タスク単体」を除外。`homeBacklog`は「Wish種別またはOther種別のProjectに属するタスク」を
  除外(Project単位)。通常はほぼ同じ集合になるが、理屈上のズレはあり得る。

これらは今回の依頼(複数ブロック登録の可否)には影響しないため据え置いた。統一が必要な場合は
別途Kの判断を仰ぐ。

## 変更ファイル

- `app.js`: `homeBacklog()` のdisabled化撤去+バッジ追加(v112コメント付き)
- `sw.js`: `CACHE_NAME` を v111 → v112
- `tests/v112.test.js`: (a)〜(e) タスクシュート画面の回帰テスト(既存)に加え、
  (f)(g) ホームタブでの再追加ボタン押下・2件目Block作成・バッジ更新を追加
- `CHANGES_v112.md`(本ファイル): 調査結果1・2とホーム側修正の内容に更新

## 検証

- `node --check app.js` / `node --check sw.js` / `node --check tests/v112.test.js`: exit 0
- `node tests/v112.test.js`: ALL PASS(単独実行、(a)〜(g)全項目)
- 回帰確認: `homeBacklog()`修正前のapp.jsに対して同テストを実行し、(f)が実際にFAILすることを
  確認済み(disabled化が残っていれば検知できるテストであることの裏取り)
- `node tests/v88.test.js`(ホームの当日+3日絞り込み・+4日以降折りたたみのコア回帰): ALL PASS
  (今回の変更で影響を受けていないことを確認)
- `npm run test:core`: ALL PASS(直近5本 v108〜v112 + 固定コア5本、v112含む)

## 残るリスク・未対応

- ホーム側の表示ルール(並び順のタイブレーク・+7日超の完全非表示・除外Project判定方法)は
  `renderOpenTasks`と完全には揃っていない(上記「対応2」参照)。今回のスコープでは意図的に
  見送ったが、Kが違和感を覚えた場合は追って統一を検討する。
- 依頼文にあった「除外をやめる際のバッジ判断」は、タスクシュート画面については除外自体が
  存在しなかったため判断不要だった。ホーム側は新規にバッジを追加した(既存の
  `blockCountByTaskId`/「本日N件 Block 追加済み」と表記を極力揃えた)。
