<!-- generated by scripts/release-record.js; edit releases/vNNN.json instead -->
# CHANGES v272: IRON LOG種目メニュー管理

## 概要

IRON LOG画面内で筋トレ種目を追加・削除・上下移動でき、LOAD SETの選択肢へ即時反映される種目メニューを追加した。

## 変更ファイル

- CHANGES_v272.md
- docs/test-impact-map.generated.md
- releases/v272.json
- src/features/iron-log.js
- styles.css
- sw.js
- tests/iron-log-core.test.js
- tests/iron-log-e2e.test.js
- tests/suite-manifest.json

## 変更内容

- LOAD SETとTODAY'S SETSの間へ常設MENUセクションを追加し、種目名入力、▲▼並び替え、削除ボタンを実装した。
- 空白・重複追加、最後の1件削除、先頭/末尾の範囲外移動をno-opにし、disabled属性とロジック側ガードの二重防御を設けた。
- 非配列または空のgymExerciseListは表示時に既定6種目へフォールバックし、最初の有効操作で既定値の複製へ自己修復するようにした。
- 種目名をescapeHTMLし、省略時も全文確認できるtitle属性を付け、追加入力をmaxlength=24・font-size 16px・高さ44px、操作ボタンを44pxのタップ標的にした。
- MENU一覧を既定6行分の264pxに制限し、7件目以降は一覧内を縦スクロールするようにした。
- Node coreへ登録ハンドラ直呼びの単体検証、Playwright E2EへMENU/LOAD SET同期・履歴保持・既存セット操作回帰を追加した。壊れた値からの削除/移動、空配列、24文字追加成功、負例の個別判定、seedの明示削除も固定した。
- Service WorkerのCACHE_NAMEをv272へ更新した。

## 変更意図

- state.settings.gymExerciseListを文字列配列のまま管理し、既存DEFAULT_EXERCISESとの後方互換を維持する。
- 界面凍結済みiron-log.jsのimport・DI・exportを増やさず、既存action registryとsaveAndRenderの結線だけで完結させる。
- メニューから種目を削除しても過去のcondition.logs[date].gym[]を変更せず、筋トレ履歴を保持する。

## 検証

- 修正後のnode --check src/features/iron-log.js / tests/iron-log-core.test.js / tests/iron-log-e2e.test.js: PASS。
- 修正後のnode tests/iron-log-core.test.js: 全件成功。壊れた値からの削除/移動と各saveAndRender 1回、空配列、負例の操作別no-opを含む。
- 修正後のCHROME_PATH=Microsoft Edgeでnode tests/iron-log-e2e.test.js: 全PASS。24文字追加成功、44px実寸、title、7件目スクロールを含む。
- npm.cmd run code:index:write / npm.cmd run test:manifest:write: PASS。iron-log 2スイートはlegacy-crosscuttingではなく既存domain分類を維持。
- 修正後にRELEASE_HANDOFF_PATHを作業ツリー内一時ミラーへ向けたrelease-record --write / --check: PASS。本番handoff反映は既知の役割分担どおり監督者側。
- 修正後にCHROME_PATH=Microsoft Edge・同一handoffミラー指定でnode scripts/release-gate.js releases/v272.json --final: exit 0（選定回帰+coreを含め全スイートPASS）。
- 最終gate後のnode scripts/test-manifest.js --check / git diff --check: PASS。

## 既知の制約・保留

- 種目別の既定重量・回数プリセットはフェーズ2であり本リリースには含めない。
- 種目名の編集・ドラッグ&ドロップ・設定画面への集約は対象外で、並び替えは▲▼ボタン方式に限定する。
