# TaskChute Journal — 技術設計書

最終更新: 2026-08-29 / 対象バージョン: app v289 / SW cache v289

> ⚠️ この設計書は更新が追いつかず、実装から乖離することがある。**正典は常にコード本体**
> (`app.js` / `sw.js`)。現行バージョン・変更履歴・計画の正典ポインタは `README.md` の
> 「正典ポインタ」節を参照。本書は構造理解の補助であり、食い違いに気づいたら本書を疑うこと。
> 2026-08-29に v137時点で止まっていた§5/§6/§7を棚卸しし v289 時点へ現行化した(§8以降は
> 今回のスコープ外・未棚卸しのまま)。棚卸し後も本書は今後また乖離しうる点は変わらない。

---

## 0. この文書の位置づけ

- **本書(設計書)** … 「どう作られているか」= 技術構造・データモデル・サブシステムの設計。
- **`CONCEPT.md`(思想・コンセプト)** … 「なぜそう作るか」= 設計哲学と判断基準。※リポジトリ外で管理。

新しく実装に入る人(または新しいチャットの AI)は、まず本書で構造を掴み、変更の是非は `CONCEPT.md` のチェックリストで判断する、という二本立て。

---

## 1. 概要

**一言で**: 「時間ではなく "どんな日を積み重ねているか" を可視化する、ローカルファーストな自己ダッシュボード」。

タスクシュート + 12 Week Year + 0秒思考 + ジャーナルを 1 つの PWA に統合した、単一ユーザー(K)専用の個人道具。

| 項目 | 内容 |
|---|---|
| 形態 | PWA(Progressive Web App)。iPad / iPhone / PC のブラウザで動作、ホーム画面追加で常用 |
| 実装 | Vanilla JS(フレームワークなし)、単一 `app.js`。ビルド工程なし |
| データ | ブラウザの `localStorage` を主。GitHub 上の 1 ファイルを端末間同期・バックアップに使用 |
| 依存 | 実行時依存は Markdown パーサ `marked` のみ(リポジトリ同梱、CDN 非依存) |
| 公開 | GitHub Pages(public リポジトリ)から配信 |

---

## 2. 技術スタックと制約

- **ランタイム依存ゼロ方針**: npm パッケージをバンドルしない。`marked.min.js` だけ同梱(v38 で CDN から移行)。
- **ビルドなし**: `app.js` を直接 `<script type="module">` で読み込む。トランスパイル・minify もしない(可読性優先)。
- **オフラインファースト**: Service Worker がアプリシェルをキャッシュ。ネットワークなしでも起動・記録できる。
- **データ主権**: サーバー・アカウント・課金なし。すべて端末内 + ユーザー自身の GitHub リポジトリ。
- **単一ユーザー最適化**: 汎用化しない。K の思考・行動の型に合わせ切る(`CONCEPT.md §2`)。

---

## 3. ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | 入口。静的な骨格(`#app` `#sidebar` `#main` `#timelineRail` `#bottomNav` `#modalRoot` `#toast`)のみ。中身は JS が生成 |
| `app.js` | **本体(約6,000行)**。状態管理・全画面描画・イベント処理・同期・繰り返し等すべて |
| `styles.css` | レスポンシブ UI・テーマ変数・全コンポーネントのスタイル |
| `marked.min.js` | Markdown → HTML(v12.0.0 同梱)。ジャーナル/ビジョン/日報の表示に使用 |
| `sw.js` | Service Worker。キャッシュ戦略とオフライン対応 |
| `manifest.webmanifest` | ホーム画面追加用メタ |
| `Vision.md` / `Daily_Affirmation.md` / `*_vision.pdf` | ビジョンタブが読み込む静的コンテンツ |
| `app-state.json` | GitHub 同期先(state のスナップショット。**token は除去して保存**) |
| `日報_YYYY-MM-DD.md` | 日報の push 先 |

---

## 4. アーキテクチャ

### 4.1 三本柱

```
┌─────────────┐   render()    ┌──────────────┐  ユーザー操作  ┌─────────────────┐
│  state       │ ────────────▶ │  DOM (innerHTML) │ ───────────▶ │ document レベルの │
│ (単一オブジェクト) │               │  全画面を再生成    │              │ イベント委譲       │
└─────────────┘ ◀──────────── └──────────────┘              └─────────────────┘
      ▲            saveAndRender()                                     │ state を変更
      └─────────────────────────────────────────────────────────────┘
```

1. **単一状態 (`state`)** — アプリの全データが 1 つのオブジェクト。`localStorage` の 1 キー(`taskchute-journal-pwa-state-v1`)に JSON でシリアライズ。
2. **全再描画 (`render()`)** — 状態が変われば画面全体を作り直す。仮想 DOM も差分更新もない。テンプレートリテラルで HTML 文字列を生成し `innerHTML` に代入。
3. **イベント委譲** — 個々の要素にリスナーを付けず、`document` に対して `click` / `input` / `change` を 1 つずつ登録。`data-action` 属性でディスパッチする。

### 4.2 なぜこの構成か

- 依存ゼロ・ビルドなしを貫くと、フレームワークの状態管理が使えない → 「単一状態 + 全再描画」が最も単純で壊れにくい。
- 全再描画は一見富豪的だが、単一ユーザーのデータ量なら性能上まったく問題ない。**バグの温床(部分更新の取りこぼし)を構造的に排除**できるのが最大の利点。
- イベント委譲により、再描画で要素が作り直されてもリスナーの再登録が不要。

### 4.3 描画の入口

```
render()
 ├─ renderSidebar()      左ナビ(折りたたみ対応)
 ├─ renderBottomNav()    モバイル下部ナビ
 ├─ renderMain()         現在ビューを描画(下表)
 └─ renderTimelineRail() タスクシュート時のみ右レール
```

`renderMain()` は `state.currentView` を見て 1 つの `renderXxx()` を呼ぶだけの分岐。

### 4.4 保存の3系統(重要)

| 関数 | 用途 | `dataModifiedAt` | 自動保存タイマー |
|---|---|---|---|
| `saveState()` | 実データ変更時 | **更新する** | 再セットする |
| `persistLocalNoSchedule()` | 同期処理内部・UI操作 | 更新しない | 再セットしない |
| `saveAndRender(msg)` | 変更 + 再描画 + トースト | `saveState` 経由 | 〃 |

**原則**: 画面切替・日付移動・タブ切替などの *UI 操作* は `dataModifiedAt` を汚さない(`persistLocalNoSchedule`)。これを守らないと「古い端末がタブを触っただけで最新扱いになり、新しいリモートを上書きする」データ消失事故が起きる(v37 で対策済み)。

---

## 5. 状態モデル(データモデル)

`state` のトップレベル:

```
state = {
  currentView, selectedDate,          // UI カーソル
  settings { … },                     // 設定・マスタ(§5.1)
  projects [ … ],                     // §5.2
  tasks    [ … ],                     // §5.3
  blocks   [ … ],                     // §5.4
  recurrences [ … ],                  // 繰り返しルール(§7.2)
  journals { 'YYYY-MM-DD': md },      // ジャーナル本文
  journalMeta { 'YYYY-MM-DD': {…} },  // 日ごとのAI取り込みメタ(aiMitCandidates/ideal/aiRequest等、§7.13。v42)
  dailyDeclarations { 'YYYY-MM-DD': {…} }, // 今日の宣言(v117)
  weeklyWishes { '週開始日': {…} },    // 週次Wish選定(v121)
  feedback { 'YYYY-MM-DD': md },      // AIフィードバック
  feedbackFiles [ … ],                // push済みAIフィードバック.mdの日付集合(v56)
  feedbackIngestedDates [ … ],        // AIフィードバック自動取込の冪等マーカー(v86)
  reports  { 'YYYY-MM-DD': md },      // 生成した日報
  zeroThinking { themes[], entries[], groups[], suggestedThemes[] }, // 0秒思考(§7.7)。theme/entry は questionId を持つ
  zeroSecThemeLog [ … ],              // 0秒思考テーマ採否ログ(v75)
  questions [ … ],                    // 問い(10x)エンティティ(§7.10)
  experiments [ … ],                  // 人生実験カード(v68)
  chainRuns [ … ],                    // 連続ルーティンの当日進行状態(v115)
  condition { logs{} },               // コンディションOS(睡眠/服薬/余力等、v73)
  declarations [ … ],                 // 宣言→終了報告ログ(上限300、v87)
  bodyScans [ … ],                    // ポモドーロ身体スキャン(v129)
  sleep { logs{} },                   // AutoSleep CSV取込ログ(v105)
  storeVisits [ … ],                  // 「今日行ったお店」ログ(v141)
  migrationRitualLog [ … ],           // 繰り越し儀式の選択ログ(v61)
  aiLinkFreshness { feedbackAt, planAt }, // AI連携の鮮度(v67)
  aiWorkProcessedIds [ … ],           // AI作業結果の処理済みID(v67)
  aiStepProcessedIds / aiStepDismissedIds / aiStepPendingRequests, // AI実行ステップの処理済み/取消/保留台帳(v197)
  aiReportReadIds [ … ],              // AIレポート既読ID(未読通知バッジの土台、v283。§7.33)
  aiScheduleHistory [ … ],            // スケジュール実績ログ(v52。§7.21)
  swipeTriageLog [ … ],               // 仕分けモード(繰り越し+Wishバックログ三択)選択ログ(v152)
  coachLog { meals[], settings{} },   // AI Coach食事ログ+設定(v201)
  earlyBird { logs{} },               // EARLY BIRDログ(v229。TOWER系)
  habitStreaks { ruleId: {…} },       // 固定化ルーティンの習慣ストリーク(v252。§7.28)
  habitPinHistory { ruleId: [...] },  // 固定化解除期間の履歴(PIN ARCHIVE、v280。§7.28)
  ironImport { done, … },             // IRON LOG初回移行の冪等フラグ(v234。§7.30)
  tracks [ … ],                       // 12WY二軸(成果トラック)定義(v243。§7.27)
  trackMeasurements [ … ],            // numericトラックの絶対値測定履歴(v243。§7.27)
  weeklyCommitments [ … ],            // 週次確定シート(週メタ+コミットitem、v243。§7.27)
  _trackToastLog { … },               // トラック進捗トーストの重複防止ログ(非永続寄り)
  pomodoro { … },                     // タイマー状態(§7.5)
  dataModifiedAt,                     // 実データ最終更新時刻(同期の勝敗判定)
  modal                               // 開いている編集モーダル(永続化しない)
}
```

**旧 `weeklyReviews` / `cycleReviews`(週次・12週サイクルレビューメモ)は v217 で両ビューごと
削除済み**(§7.11/§7.15参照)。

**共通規約**:
- すべてのエンティティは `id`(`crypto.randomUUID()`)、`createdAt`/`updatedAt`(`nowDateTime()`)、`deleted`(論理削除フラグ)を持つ。
- 物理削除しない。`deleted: true` にして描画時にフィルタする(復元可能性を残す)。
- `normalizeState()` が起動時に全エンティティへ後方互換のデフォルト値を補完する。新フィールド追加は「デフォルトを足すだけ」で既存データを壊さない。

### 5.1 settings

| フィールド | 意味 |
|---|---|
| `birthDate` | 生年月日。寿命カウントダウン(45歳/80歳)の基点 |
| `twelveWeekStartDate` | 12週サイクルの開始日 |
| `morningEnergyLog { date: 0-10 }` | 朝の体調(ジャーナルで記録、エネルギーグラフ始点) |
| `journalTemplate` | ジャーナルの雛形。日付部分は生成時に当日へ置換 |
| `vision` / `affirmation` | ビジョン/アファメーション本文 |
| `github { owner, repo, dataOwner, dataRepo, branch, path, token, autoSave, lastSavedAt }` | 同期設定。**token は同期・エクスポートに含めない**。v72で個人データ用の `dataOwner`/`dataRepo`(既定 `kojit1229`/`personal-data`)を追加(§7.1)。旧 `owner`/`repo` はコードから未参照(後方互換のみ) |
| `categories []` | カテゴリマスタ(名前 + 色) |
| `breakMessages []` | 休憩メッセージ(残り秒レンジ → 文言) |
| `lifeAreas []` | やりたいことリストの人生領域マスタ |
| `avoidList []` | やらないことリスト |
| `visionSection` / `visionBoardIndex` | ビジョン画面の表示状態 |
| `sidebarCollapsed` / `showSuspended` | UI トグル状態 |
| `autoSync` / `lastPushedAt` / `lastPulledAt` | 自動同期(v43、既定OFF)。`lastPushedAt` は最後に push した時の `dataModifiedAt` |
| `ai { }` | v60でAI呼び出し前提の機能(§7.16/§7.19/§7.20、旧v49〜v51)を全廃した際、`apiKey`/`model`/`prompts`/`autoMorningReview` は`normalizeState`で明示的に削除するようにした(既存端末のlocalStorageからも掃除)。当初唯一残っていた自動下書きトグル`autoMorningPlan`も**v214で廃止**(`normalizeState`が明示的に削除)し、`ai`配下は実質空になった。朝の一括プランニング自体(決定論配置、§7.19②)は手動実行と下書き機構(`_scheduleDraft`)のみで現存する |
| `todaySkin` | v221でcockpitスキンを廃止し、"tower"固定へ正規化(旧値も含め後方互換で維持) |
| `theme` | "light"/"dark"/"cockpit"/"auto"(OS追従)の4択(v185)。既定"dark" |
| `battery { start, decayPerHour, decayStartMinutes, max }` | エネルギーバッテリーモデルのパラメタ(v144)。タイムラインの残量線表示に使用 |
| `twelveWeekScoreTarget` | 12WY週次コミット達成率の目安(70–100、既定85、v243。§7.27) |
| `towerMotion` | TOWERのモーション強度。"normal"/"calm"/"off"の3択(v210) |
| `earlyRiseTarget` | 早起きチェックの基準時刻(HH:mm、既定"06:00"、v229) |
| `ironDailyTarget` | IRON LOGの1日あたり目標重量(kg、既定2000、§7.30) |

### 5.2 Project

`id, kind, title, category, status, twelveWeekStartDate, startDate, dueDate, description, collapsed, …`

- `kind`: `normal`(通常) / `wish`(やりたいこと) / `other`(単発ブロックの受け皿)。`wish`・`other` は必ず 1 つ存在を保証。
- `status`: `active` / `paused`(=中断) / `completed` / `archived` / `cancelled`。
- `twelveWeekStartDate` を持つ `normal` Project = 12週サイクルの目標。

### 5.3 Task

`id, projectId, parentTaskId, title, category, status, dueDate, description, targetYear, lifeArea, motivation, realized, realizedDate, nextRoutineId, kind, collapsed, …`

- `parentTaskId` で最大 3 階層のツリー(WBS)。
- `status`: `todo` / `doing` / `completed` / `suspended`(=中断) / `cancelled`。中断・中止は一覧と進捗分母から除外。
- `targetYear` / `lifeArea` / `motivation` / `realized` … やりたいことリスト用。
- `nextRoutineId` … ルーティン連携(完了時に次のルーティンを提示)。

### 5.4 Block(タスクシュートの実行単位)

`id, taskId, date, title, category, plannedStartAt, plannedEndAt, actualStartAt, actualEndAt, completed, charge, discharge, expectedCharge, expectedDischarge, comment, recurrenceGroupId, pomodoroCount, isMIT, source, migratedTo, …`

- 特定の 1 日(`date`)に属する。予定時刻(`planned*`)と実績時刻(`actual*`)を分離。
- `charge`/`discharge`(各 0–5)= **エネルギー会計**(§7.3)。
- `recurrenceGroupId` があれば繰り返しルール由来の実体。
- `isMIT` = 今日の主役(MIT、最大3)。
- `source`: `timeline` など生成元の識別。
- `estimateMin`(v41): 見積時間(分)。null は解決順(手入力→同系列の過去実績中央値→30分)で補完。入力必須にしない。
- `migratedTo`(v46で活用): 繰り越し先の Block id。セット済み = 「先送り済み」で未完了リスト・繰り越し提案から外れる。

### 5.5 Question(問い・v39)

`id, text, origin, status, settledNote, settledAt, lastTouchedAt, …`

- `origin`: `manual` / `zero`(気づきから昇格) / `review`(週次から)。
- `status`: `open`(未着手) → `deepening`(0秒思考で掘り中、entry紐づけで自動遷移) → `settled`(結論)。
- `lastTouchedAt`: 最後に entry が紐づいた日。30日以上前の open/deepening はグレー表示(削除・警告なし)。
- 数週間〜12週スパンの「10xの問い」を保持する器(§7.10)。
- `linkedProjectId` / `linkedTaskId`(v44): 結論を実行に移した先(what→how の橋)。

### 5.6 【v217で削除】WeeklyReview(週次レビュー・v39導入 → v217廃止)

> ⚠️ `weeklyReviews` は **v217で `cycleReviews` と共に削除済み**(§7.11参照)。以下は歴史的記録。

`weeklyReviews['YYYY-MM-DD']`(キー = 週開始土曜)= `{ md, changeThemeCreated, createdAt, updatedAt }`。

- 指標は blocks/entries から**都度計算**(非正規化しない)。永続化するのは**レビューメモと締めテーマ発行フラグのみ**(§7.11)。

---

## 6. 画面(14ビュー、v289時点。renderMainのview switch実数で数え直し)

| view | 関数 | 役割 |
|---|---|---|
| `today` | `renderToday` | TOWER統合画面。v230で`home`(コックピット4ゾーン)を撤去し一本化(§7.29) |
| `tasks` | `renderTasks` | タスクシュート。当日Block一覧 + 右タイムラインレール |
| `timeline` | `renderTimelineView` | 時間軸 + 累積エネルギー。予定/実績切替・カテゴリフィルタ |
| `wbs` | `renderWBS` | Project→Task ツリー。中断トグル・インライン一括編集(§7.25)・検索/折りたたみ(§7.36) |
| `journal` | `renderJournal` | 前日/当日/AIフィードバックの3ペイン。朝の体調・日報生成 |
| `ai-reports` | `renderAiReports` | AIレポートビューア(コンテンツ総括・自己分析・基盤ヘルス・週次提案・バッチ実行サマリ・英語表現集。v92追加)。v283以降は未読通知バッジ機構(§7.33)も統合 |
| `wish` | `renderWish` | やりたいことリスト |
| `vision` | `renderVision` | ビジョン / アファメーション / ビジョンボード |
| `zero` | `renderZeroThinking` | 0秒思考。テーマ / 問い の2タブ(§7.7, §7.10) |
| `instruments` | `renderInstruments` | INSTRUMENTS(統計ダッシュボード)。v217で旧`stats`を削除→v232〜v233で再構築(§7.31) |
| `iron-log` | `renderIronLog` | IRON LOG。筋トレ記録(§7.30、v232〜v234) |
| `fund` | `renderFund` | FABLE FUND閲覧タブ。模擬運用のNAV・保有ポジション・注文・約定を読み取り専用表示(§7.32、v281) |
| `more` | `renderMore` | 「その他」メニュー |
| `settings` | `renderSettings` | プロフィール・GitHub同期・マスタ編集・データ入出力(APIキー設定はv60で廃止) |

左サイドバー(`navItems`)は全14(上表すべて)。**`more`もPCサイドバーに直接ボタンとして表示
される**(v265で追加。旧記載「moreはモバイル専用」は誤り)。モバイル下部(`mobileNav`)は
主要4(今日/ジャーナル/実行/時間) + 「その他」(v82でWBSを「その他」へ降ろしジャーナルを昇格)。

---

## 7. 主要サブシステム

### 7.1 GitHub 同期(ローカルファースト)+ 個人データリポジトリ分離(v72)

**方針**: 端末内 localStorage が主。GitHub 上の `app-state.json` 1ファイルを「新しい方が勝つ」で同期。

- **v72: 個人データは private リポジトリへ全面分離**。`state.settings.github` に `dataOwner`/
  `dataRepo`(既定 `kojit1229`/`personal-data`)を追加し、app-state.json・日報・AIフィードバック・
  AIプラン・週次レビュー・AI作業結果・Vision/Daily_Affirmation は**すべて**このリポジトリの
  `taskchute/` 配下(`personalDataConn`/`personalDataPath`)で読み書きする。`branch`/`token` は
  従来のフィールドを共用。旧 `owner`/`repo`(既定 `kojit1229`/`taskchute-ipad` = このアプリ自身の
  public リポジトリ)は個人データの読み書きコードからはもう参照しない(スキーマ上は後方互換で残す)。
- **トークンゲート(v72)**: token・dataOwner・dataRepo が揃うまで `render()` は中身を一切描画せず、
  全画面セットアップ画面(`renderGate`)だけを出す。判定は localStorage の設定有無のみ(有効性は
  検証しない)。
- **読み込みの一本化(v72)**: Vision/Daily_Affirmation・AIフィードバック・週次レビュー・AIプラン・
  AI作業結果は同一オリジンfetchを廃し、`fetchGitHubRawText(name)`(Contents API の
  `Accept: application/vnd.github.raw+json` で raw 取得)に統一した。401 のみ具体的な案内バナー
  (`renderPersonalDataAuthBanner`)を出す。同一オリジンへのフォールバックは無い。
- **保存 `saveToGitHub`**: `sanitizedStateForGitHub()`(=**token を空にした複製**)を Contents API に PUT。
- **読込 `loadFromGitHub`**: リモートを丸ごと採用(手動)。1MB 超のため Blob API 経由でも取得。
- **起動時同期 `syncFromGitHubOnStartup`**: リモートの `dataModifiedAt` が**起動時点のローカル**より新しい時だけ取り込む。
- **自動保存**: 変更後 30 秒のデバウンス(`scheduleAutoSave`)。同時実行ガードあり。
- **上書き防止(v37)**: 保存前にリモート SHA を「この端末が最後に同期した SHA(`lastSyncedSha`)」と比較。リモートが進んでいてかつ新しければ**中止**。未同期の新規端末からの保存は確認ダイアログ。
- **トークンの扱い**: `token` は同期にもエクスントにも**絶対に含めない**。端末内(+任意で iOS キーチェーン)のみ保持。private リポジトリへの移行後もこの原則は維持している。

エラーは 401/403/404 に日本語の対処ヒントを付けて表示(`gitHubErrorMessage`)。

### 7.2 繰り返し / ルーティンエンジン

- **ルール方式**: `state.recurrences[]` にルール(`kind: daily|weekdays|weekly|monthly`、`startTime`/`endTime`、既定カテゴリ・充電/放電 等)を保持。
- **実体化 `maintainRecurrences`**: 起動時に、過去 `RECURRENCE_KEEP_PAST_DAYS(7)`日 〜 未来 `RECURRENCE_FUTURE_DAYS(31)`日 の範囲でルールから Block を生成(`makeRecurrenceInstance`)。範囲外・未編集の実体は purge。
- **ユーザー編集の保護**: 実体化済みで編集された回は保持し、二重生成しない。
- **ID 規約**: 実体の id は `rec_{ruleId}_{date}` で決定的 → 冪等な生成が可能。

### 7.3 エネルギー会計(二重会計)

各 Block が **充電(charge)/放電(discharge)**(各 0–5)を持つ。完了 Block を集計して「今日の足あと」に充電合計・放電合計・差引を表示。**生産性 = 時間 × 持続可能性**を可視化する(`CONCEPT.md §4.2`)。ルーティンはルール既定値を全実体へ適用。

### 7.4 多スケール時間(`computeMetrics`)

同一画面に4つの時間スケールを並置:
- **12WY** … `twelveWeekStartDate` から 84 日。残り日数と進捗率。
- **今年** … 年初〜年末の経過。
- **45歳まで / 80歳まで** … `birthDate` からの寿命カウントダウン(memento mori、`CONCEPT.md §4.8`)。

### 7.5 ポモドーロ

- **任意タイマー**: Block に紐づく 25 分カウントダウン。秒はミリ秒基準で計算(切り捨てで短くならないよう v37 修正)。
- **常時タイマー(壁時計モデル)**: 30分サイクル(0–25分=集中 / 25–30分=休憩)を時計から直接読む `getPassiveSessionStatus`。状態を持たないので中断・再開に強い。
- **通知は行わない**: 旧・通知サブシステムは v38 で削除(`CONCEPT.md §5.4`「静かな道具」)。

### 7.6 MIT(今日の主役)と着手率

- **MIT**: `isMIT` な Block、1日最大3(`toggleMIT` でガード)。ホーム最上ゾーンに配置。
- **着手率**: `taskchuteStartRate` = (完了 or 実績開始あり) / 全対象。**完了率ではなく着手率**を主指標に(`CONCEPT.md §4.3`)。
- **着手ジュース(v40)**: 着手(`actualStartAt` セット)の瞬間だけ、その行に 0.3秒の軽いスケール+amberグロー(`.just-started`)。完了ではなく**着手**を祝うことで着手第一主義を感覚レベルで補強。報酬・履歴・比較ゼロ(ゲーミフィケーションではなくフィードバック)。非永続フィールド `state._justStartedBlockId` を render で1回消費、localStorage/GitHub には保存しない。

### 7.7 0秒思考

- **テーマ**(`zeroThinking.themes`): ★お気に入りは残り、それ以外は書いたら消える。
- **書く**: 1分カウントダウン(0で停止、入力は継続可)。
- **履歴**(`zeroThinking.entries`): 検索可能。日報にその日の分を出力。

### 7.8 反省 → 行動ループ(アプリの背骨)

```
日報生成 ──▶ AIプロンプト(0秒思考テーマ / 明日のMIT候補を要求)
   ▲                    │
   │                    ▼
今日の記録 ◀── 主役化/テーマ登録 ◀── AIフィードバックを貼付
```

- 日報 `generateReport` の AI プロンプトに「明日の0秒思考テーマ」「明日のMIT候補(最大3)」を含める。
- 翌朝、ホームの「今日の主役」に前日フィードバックの候補を提示 → ワンタップで主役 Block 化(`extractMITCandidatesFromReport` → `addMITCandidate`)。
- 記録が行動に接続され続けることが本アプリの価値(`CONCEPT.md §4.6`)。

### 7.14 GitHub 自動同期(v43)

**最も事故リスクが高いので既定OFF・保守的。既存の手動 push/pull 関数の上に薄く載せ、置換しない。マージはしない(競合は必ず人間判断)。**

- **自動 push**: `saveState` 後 3分デバウンス(`scheduleAutoSync`)。実行条件 `autoSync && token && onLine && dataModifiedAt > lastPushedAt`。push前ガードで remote の `dataModifiedAt` を確認し、進んでいれば**中止してバナー**。push は既存 `saveToGitHub`(SHAガード付き)を共用。成功で `lastPushedAt` 更新。
- **自動 pull**: 起動 + `visibilitychange`、60秒スロットル(`runAutoSyncPull`)。remote が新しく **かつ ローカルに未push変更なし** のときだけ自動適用(→ `normalizeState` → `runDailyOpen`(pull後) → `render`)。両方に変更があれば自動適用せずバナー。
- **legacy autoSave との排他**: `autoSync` ON のとき 30秒 `autoSave` はバイパス(二重push防止)。
- **計器(催促でない)**: サイドバーに 6px 状態ドット(グレー=OFF/同期済み、amber=未push変更あり)。競合時のみ静かなバナー(モーダルで作業を止めない)。
- **安全**: token は従来どおり localStorage のみ・`app-state.json` 書き出し時に除去。自動系が壊れても手動 push/pull は常に生きている。非永続: `_autoSyncTimer` / `_lastPullCheckAt` / `_syncBanner`。

### 7.13 AIループ搬送自動化(v42)

原則: **搬送(コピペ・転記)だけ自動化。思考はAIと人間**。当初は「API統合はしない」(データ主権)としていたが、v49 でユーザー判断により直接統合(§7.16)を追加、**v60でコスト理由により再び全廃**した(§7.16参照)。コピペ搬送・`.md`アップロード・貼り付けは変更前から一貫して手動経路として維持されている。

- **出力(1タップ搬出)**: 日報画面に「AI用にコピー」(`navigator.clipboard`、失敗時 textarea 選択フォールバック)と「共有」(`navigator.share`、未対応環境は非表示)。
- **プロンプト固定**: `generateReport` 末尾に出力見出し構成(`## フィードバック` / `## 明日の0秒思考テーマ` / `## MIT候補` / `## 問い候補`、各候補は `- `)を指定。**パーサの頑健性はプロンプト側で買う**。
- **入力(構造化取り込み、v141で入口撤去→v143で本体も削除)**: ジャーナルタブのAIフィードバック
  手動貼り付け欄はv141で撤去済み。その欄への`paste`・「AI返信から取り込み」ボタン経由で
  `parseAiFeedback`(`^##`で分割、`- `を候補化)→チェックボックス付き取り込みモーダル
  (`openAiImportModal`/`buildAiImportModal`/`submitAiImport`)へ渡す一式は、v141で入口欄が
  無くなった時点で到達不能になっていたため**v143で削除した**(CHANGES_v143.md参照)。
  テーマ→`zeroThinking.themes`・問い候補→`questions`・MIT候補→`journalMeta[date].aiMitCandidates`
  という取り込み経路自体がこの削除で無くなっている。
- **MIT候補チップ(表示UIは残存・書き込み経路は無し)**: 翌日のタスクシュート上部に前日取り込み分を
  表示する`aiMitChips`/タップで採用する`adoptAiMit`のUI自体はv143でも削除していない(スコープ外)。
  ただし`journalMeta[date].aiMitCandidates`への唯一の書き込み元が上記モーダル(`submitAiImport`)
  だったため、**現状ではこのチップに値が入ることは無い**(v141時点で既にその状態だった可能性が
  高いが未検証。次の死コード掃除候補)。
- `state.journalMeta['YYYY-MM-DD'] = { aiMitCandidates:[], aiImported }`(journals は文字列なので別ストア。フィールド自体は後方互換のため`normalizeState`に残る)。
- **現行のAI取り込み経路**: AIフィードバックからの提案取り込みは`autoIngestFeedback()`
  (`hydrateStaticMarkdown`内、確認なしで`aiTaskCandidates`/`zeroThinking.themes`へ自動登録)に
  一本化されている。上記の手動取り込みモーダルとは別系統。
- **v201: AI Coach食事ログ(`coachLog`)追加**。自宅PC側バッチが生成する食事ログ・カロリー設定を
  `state.coachLog { meals[], settings{ dailyKcal } }` として保持する(append-only、削除は
  tombstone)。搬送は自動化・判断は人間という本節の原則の食事版。
- **v283以降: AIレポートタブへの統合**。AIフィードバック等の各種.mdはAIレポートタブ
  (`ai-reports`)へ集約され、既読state(`aiReportReadIds`)ベースの未読通知バッジ機構が
  追加された(FABLE FUND日誌・朝の投資ブリーフ等の投資系レポートも同機構に相乗り)。
  詳細は新設§7.33を参照。

### 7.12 実行系の自動化(v41)

原則: **搬送は自動化、判断・内省は自動化しない**。

- **日次オープン `runDailyOpen`**: 日付が変わって最初の起動/`visibilitychange`復帰で `maintainRecurrences` を実行し今日のルーティンを自動展開。冪等(`recurrenceGroupId×date` 既存は skip)。`lastOpenedDate` で1日1回。完了演出なし。
- **見込み終了時刻 `computeProjectedEnd`**: 今 + Σ(残ブロックの残見積)を「見込み終了 21:40」でタスクシュート/ホームに表示。色分け・警告なし(有限性を時刻で見せるだけ、`CONCEPT.md §4.8`)。毎分ティックで該当 `span` のみ差し替え(全再描画しない=フォーカス保護)。見積は `resolveEstimateMin`(手入力→同系列実績中央値→30分)。
- **充電/放電プリフィル `prefillEnergy`**: 完了入力の初期値を過去実績(直近8週・3件以上)の中央値に。注記なし・フラグなし(通常入力と同格)。ワンタップで変更可能な既存UIが前提。
- **未完了ブロックの繰り越し(v46)**: `carryOverPanel` がタスクシュート上部に**昨日の未完了(ルーティン/繰り返しを除く)**を表示。ワンタップで今日へコピー(予定時刻は同HH:mmで今日へシフト、見積も継承)し、旧ブロックに `migratedTo` をセット。**昨日分のみ提示 = バックログ化しない**(`CONCEPT.md §5.1`)。判断は人間、搬送だけ自動。

### 7.9 Service Worker / キャッシュ

- `CACHE_NAME`(例 `taskchute-journal-pwa-v39`)を**変更ごとにインクリメント** → `activate` で旧キャッシュを一掃。
- **同一オリジンのみ SW を経由**(v38 で CDN 特別扱いを撤去)。
- **アプリ本体(HTML/JS/CSS/manifest)と `.md` は network-first**: オンライン時は常に最新、オフライン時のみキャッシュへフォールバック。デプロイ後の旧キャッシュ居座りを防ぐ。
- **正常応答のみキャッシュ**(初回の 404/500 を配り続けない、v37)。
- 動画・レンジリクエスト・GitHub API はキャッシュしない。

### 7.10 問い(Question)— 10x の器(v39)

「効率化(2x)ではなく価値の中身(10x)」を支える第一級オブジェクト(`CONCEPT.md §6.1`)。

- **配置**: 新ビューを作らず `zero` ビューに第2タブ(テーマ / 問い)として統合。ホーム Zone 3 に「開いている問い」カード(最大3件、`deepening` 優先)。
- **ループ接続**: 問い →「この問いで書く」→ 0秒思考 entry(`questionId` 付き)→ 日報に**問い別**で出力 + AIプロンプトに問い一覧を含め「一段深める明日のテーマ」を要求 → AIフィードバック → 履歴の気づきを問いへ昇格(`entry-to-question`)。
- **自動遷移**: entry が紐づくと問いは `open → deepening`、`lastTouchedAt` を更新。
- **静かな道具**: 件数上限・締切・リマインドなし。30日/14日の停滞は**表示のみ**(催促文言なし)。
- **問い→プロジェクト橋(v44)**: 問いを `settled` にすると「実行へ」モーダル(`openQuestionBridge`)。結論を**新規12WYプロジェクト**または**既存プロジェクトのタスク**に接続(スキップ可)。`linkedProjectId` を記録し、settled カードに「→ 実行中」リンクを表示。**what(問い)→ how(実行)** を閉じる(採用判断は人間)。

### 7.11 【v217で削除】週次レビュー + エネルギー構造(v39導入 → v217廃止)

> ⚠️ 本節が説明していた `weekly` ビュー(`renderWeekly`)は **v217で専用ビューごと削除済み**。
> 以下は歴史的記録として残す(削除の経緯は `CHANGES_v217.md` を参照)。「この週から何を
> 変えるか」の来週タスク提案機能のみ AIレポート(`ai-reports`)の週次種別へ移設され、既存の
> 1件登録処理・見積分数反映は継続している。週(日報)と84日(12週)の間の**週スケール**を
> 埋める役割そのものは、後継の12WYトラック(§7.27)とAIレポートの週次提案へ分散した。

日(日報)と84日(12週)の間の**週スケール**を埋める(`CONCEPT.md §4.4`)。週定義 = **土曜〜金曜**(`weekRange` の起点)。

- **指標は都度計算**(`computeWeeklyMetrics`): 着手率・MIT・ルーティン実行率・エネルギー収支(日別)・12週の弧・問いの動き。既存の `taskchuteStartRate` / `routineRate` を週の blocks に適用。
- **エネルギー構造分析**(`computeEnergyStructure`): 直近4週の completed blocks から、曜日別平均差引・カテゴリ別差引を計算し、**放電超過の上位3件だけ**提示。4週未満のデータなら非表示。各行に必ず行動導線(該当曜日のルーティン / カテゴリのタイムライン)を付け、数字だけ見せて終わらせない。
- **締めがループの要**: 「この週から何を変えるか」を0秒思考テーマ化する主ボタン(`weekly-change-theme`)。統計を見て終わりにしない唯一の必須動線。再発行は妨げない(強制ゲートにしない)。メモ・GitHub push は任意。
- **導線**: サイドバーに `weekly`、ホーム Zone 3 に**土曜のみ**静かなリンク。週カーソル(`weeklySelectedWeek`)は UI 状態。
- **v40 の精緻化**:
  - 既定週 = 直近の完了週(土曜なら先週、平日は今週=進行中で「進行中」注記)。**未来週へは進めない**。
  - 実行スコアに**4週着手率スパークライン**(`startRateHistory`)。目標線・達成色分けなし(付けた瞬間ストリークに変質するため)。
  - エネルギー収支は**双極バー**(ゼロ軸中央、正=teal / 負=red)。
  - エネルギー構造の適格性 = 対象4週の completed が **28件以上**、曜日・カテゴリとも **n ≥ 3**。
  - 曜日 finding → `energy-open-routine`(`routineDayFilter` セット + その曜日の直近日へジャンプ、routine にチップ表示)。カテゴリ finding → `timelineCategoryFilter`。**フィルタは常に解除可能なチップで表示**(気づかない残留を防ぐ)。
  - 空セクションは非表示(記録ゼロ週・12WY未設定・問い未活用)。

### 7.15 【v217で削除】12週サイクルの節目レビュー(v45導入 → v217廃止)

> ⚠️ 本節が説明していた `cycle` ビュー(`renderCycle`)は **v217で専用ビューごと削除済み**。
> 以下は歴史的記録として残す(削除の経緯は `CHANGES_v217.md` を参照)。「最長の実行ループ
> (12週=84日)を閉じる」役割の後継は、12WYトラック(§7.27)の週次確定シート・COUNTDOWN
> スコア信号とINSTRUMENTS(§7.31)の表示に分散している。

日(日報)・週(週次)の上に、**最長の実行ループ(12週=84日)を閉じる**「第13週」の儀式(`CONCEPT.md §4.4`、12WY 方法論の中核)。

- **`cycle` ビュー**(`renderCycle`)。navItems には入れず、週次レビューの「12週サイクルをふりかえる」リンク + ホーム Zone 3(**残り7日以内のみ**)から到達。
- **指標は都度計算**(`computeCycleMetrics`): サイクル84日の着手率・MIT・ルーティン・エネルギー収支合計・**週次着手率の12週スパークライン**(`startRateHistory(…, 12)`)・サイクル目標プロジェクトの到達率・問いの動き(動いた/結論/実行へ橋渡し)。
- **締め = 次サイクルへの接続**: 「**新しい12週を今日から始める**」ボタン(`twelveWeekStartDate` を今日に更新、確認あり)→ WBS へ。総括メモ・md ダウンロード/push は任意。
- `cycleReviews['YYYY-MM-DD']`(キー=サイクル開始日)にメモのみ永続化。サイクルカーソル `cycleSelectedStart` は UI 状態(未来サイクルへは進めない)。

### 7.16 【v60で全廃】AIレビュー直接統合(v49導入 → v60廃止)

> ⚠️ 本節が説明していた機能(`runAiReview`、ボタン1つで日報をAnthropic Messages APIへ直接
> 送信)は **v60でアプリ内Claude API呼び出しの全廃に伴い削除済み**。以下は歴史的記録として
> 残す(削除の経緯・対照表は `CHANGES_v60.md` を参照)。

日報 → ボタン1つ → Anthropic Messages API → フィードバックがアプリ内に返る、という**v42 の
コピペ搬送の完全自動化版**だった(`POST https://api.anthropic.com/v1/messages` をブラウザから
直接fetch、`settings.ai.apiKey`/`settings.ai.model`をUIで設定)。

**v60以降の現行仕様**: アプリ内からAnthropic APIを呼ぶ経路は無い。AIの知恵を取り込む唯一の
経路は「自宅PC側バッチ(`loop/coach-daily.sh`等)が生成した `AIフィードバック_YYYY-MM-DD.md`
をpersonal-dataリポジトリへpush → アプリがGitHub Contents API経由でfetchして表示」
(§7.1「読み込みの一本化」参照)+ `autoIngestFeedback()`(確認なしで`aiTaskCandidates`/
`zeroThinking.themes`へ自動登録)」。手動での`.md`アップロード(`uploadFeedbackFile`)・
貼り付け(`parseAiFeedback`による取り込みモーダル自動オープン)というv42由来の搬送経路は、
v141でジャーナルタブの入口欄自体が撤去された結果到達不能になっていたため、**v143で削除した**
(§7.13参照、CHANGES_v143.md)。

### 7.17 世代バックアップ(v49)

`app-state.json` は単一ファイル上書きのため、誤同期すると過去に戻れない。**GitHub 保存成功後に 1日1回、`backups/app-state-YYYY-MM-DD.json` の日次スナップショットを静かに残す**。

- **書き込み**(`maybeWriteBackupSnapshot`): `saveToGitHub` 成功後に fire-and-forget(手動/30秒 autoSave/3分 autoSync すべて共通の1フックで担保)。1日1回ガードは localStorage 別キー `taskchute-backup-last-date`(state を汚さない)。同日ファイルが既にあれば sha 取得して上書き。失敗は `console.warn` のみ(本体同期は成功済み)。
- **世代整理**(`pruneOldBackups`): スナップショット成功後、`backups/` を一覧して **14日** より古いものを DELETE。
- **復元**: 設定の GitHub カード「📦 バックアップ世代から復元」→ 一覧モーダル → confirm → 取得 → `normalizeState` → token(GitHub同期)はこの端末の値を引き継ぐ(v60でAPIキーは廃止済み) → **`saveState` で dataModifiedAt を今に更新**(復元をこの端末発の最新変更として扱い、直後の自動 pull に黙って上書きされない)。

### 7.19 【①③④はv60で全廃・②のみ決定論化して存続】AI活用の拡張(v50導入 → v60で仕分け)

> ⚠️ 本節導入時はAnthropic API呼び出し前提の4機能だった。v60で①③④を削除、②のみ
> AI呼び出しを取り除いた決定論ロジックへ置き換えて存続させた(対照表は`CHANGES_v60.md`)。

- **① AIタスク分解(WBS)**(`runAiDecompose`) — **v60で削除**。代替なし(手動でタスク追加)。
- **② 空き時間への下書きスケジュール配置(タイムライン D&D)**(`runAiSchedule`) — **v60で決定論化して存続**。AI呼び出しを`computeFreeGaps` + `fallbackMorningPlan`(朝の一括プランニングと共通の決定論配置ロジック)に置換した。確定済み予定(動かせない)+候補(昨日のMIT候補 + WBS未完了タスク上位、Wish/中断/当日Block化済み除外)から空き時間へ**非永続の `_scheduleDraft`** としてタイムラインに点線ブロックで仮配置する。Pointer Events(タッチ/マウス両対応)でドラッグ移動・下端ドラッグで長さ調整・×で除外し、「確定して登録」で初めて実Block化(`estimateMin` 継承、taskId 紐づけ)。**押した時だけ動く。毎朝の自動生成は朝の一括プランニング(§7.20相当、opt-in)が別途担う**。ボタン絵文字はAI呼び出しを連想させる🤖から📋へ変更。
- **③ 週次 / 12週サイクルのAI壁打ち**(`runAiWeekly` / `runAiCycle`) — **v60で削除**。代替なし(手動のメモ欄はそのまま存続)。
- **④ 0秒思考のまとめ所感**(`runAiZeroComment`) — **v60で削除**。代替なし。

### 7.20 【v60で全廃】プロンプト基盤・朝イチ自動レビュー・今日のタスク提案(v51導入 → v60廃止)

> ⚠️ 本節が説明していたプロンプト合成基盤・朝イチ自動レビュー・今日のタスク提案は、いずれも
> Claude API呼び出し前提の機能だったため**v60で全廃**した。以下は歴史的記録として残す。

- **プロンプト合成**(`aiCommonPreamble`/`aiPrompt`/`AI_DEFAULT_PROMPTS`等)・**プロンプト設定**
  (`settings.ai.prompts`、設定画面の「プロンプト設定(上級)」UI)は削除。
- **朝イチ自動レビュー**(`maybeAutoMorningReview`)は削除。代替なし(下記「今日のタスク提案」と
  同じく、AI呼び出し無しでは同等の機能を維持できないため)。
- **今日のタスク提案**(`runAiTodaySuggest`)は削除。**朝の一括プランニング(🌅朝プラン、v59)が
  上位互換のため**(決定論エンジンで前日未完了・MIT候補・WBS候補を空き時間へ仮配置する。
  §7.19②の下書きスケジュールと同じ`_scheduleDraft`機構を使う)。
- `generateReport(dateArg, {quiet})`(任意日・画面遷移なし生成)は他機能(日報フォールバック
  バッチ等)から引き続き使われるため存続。

### 7.21 スケジュール実績ログ(v52導入、v60で「学習ループ」の注入経路を廃止)

導入時は、AIの仮配置に対する**ユーザの採否・修正・実績**を蓄積し、次回のスケジューリング時に
その傾向をAIプロンプトへ注入して精度を上げる「学習ループ」だった(実データの集計をプロンプトに
載せる in-context 方式)。**v60でAI呼び出しごと注入経路を廃止**したため、現在は「記録」の
部分のみが**将来の自宅PCバッチでの分析用途を見込んで**残っている。

- **記録(3層。存続)**:
  1. 下書き item に `aiStart` / `aiMinutes`(決定論配置の元値。フィールド名はAI時代からの互換のため維持)
  2. 確定 Block に `aiPlan {start, minutes}` — 確定値(plannedStartAt)・実績(actualStartAt)と同じ Block 上で突き合わせ可能
  3. `state.aiScheduleHistory[]`(上限300・同期対象): `{date, title, category, aiStart, aiMin, outcome: confirmed|removed|discarded, userStart, userMin, at, source, reason}`(v62でsource/reason追加)。×で外す・破棄も**却下シグナル**として記録
- **傾向ダイジェスト(`buildScheduleLearningDigest`)・体調相関(`morningEnergyCorrelation`)は
  v60で削除**(集計してAIプロンプトへ注入するためだけの関数で、注入先が全て削除された結果
  呼び出し元ゼロのデッドコードになったため)。`SCHED_BANDS`(計器盤のヒートマップで使用)は
  削除していない。

### 7.22 計器盤 → INSTRUMENTS(統計ダッシュボード、v53/v54で拡張 → v217で削除 → v232〜v233で再構築)

> ⚠️ 本節は元は`stats`ビュー(v53/v54拡張)の記述だったが、**v217で一度全削除**され、
> **v232でモジュール(`src/features/instruments.js`)として配置、v233で画面結線**され直した
> (ビュー名は`stats`→`instruments`に変更。詳細は `CHANGES_v217.md` / `CHANGES_v232.md` /
> `CHANGES_v233.md` を参照)。v253で固定化ルーティンの習慣ストリーク表示が追加されている
> (§7.28)。**未検証**: 現行`instruments`実装が以下のv53/v54時点の9種チャート構成と同一
> かどうかは今回の棚卸しでは裏取りできていない(`src/features/instruments.js` の実読みが
> 必要。次回棚卸し推奨)。以下は歴史的記録として残す。

溜まったデータの長期トレンドを見る**静かな計器**(目標線・達成色分け・催促なし)。旧`stats`ビュー(navItems「計器盤」、モバイルは「その他」経由)。集計は都度計算、データ不足のセクションは非表示。`renderStats` は各セクションを文字列で組み `body` に連結する構造(セクション追加は変数を足すだけ)。

- 期間セグメント 4週 / 12週 / 全期間(ローカル保持分。アーカイブ済み期間は含まない)。カーソル `settings.statsRange` は UI 状態。
- v53の4種: ①着手率の週次推移(縦棒、`startRateHistory`)②エネルギー収支の週次推移(週Σ(充電−放電)の双極縦棒)③時間帯×曜日の着手ヒートマップ(`SCHED_BANDS`×土〜金、n<3のマスは非表示)④見積 vs 実績(`estimateMin` と `_actualDurationMin` の中央値比・平均ズレ・カテゴリ別ワースト、n≥5/カテゴリn≥3)。
- **v54で追加した5種(別形式)**: ⑤カテゴリ別 時間配分(**ドーナツ**、inline SVG。`r=15.915`円周≈100 + `stroke-dasharray`/`dashoffset`、セグメント間1単位gap、`getCategoryColor`)⑥カテゴリ別 エネルギー収支(**横向き双極バー**、緑=充電源/赤=放電源、n≥5)⑦主要指標の推移(**複数折れ線**、着手率/ルーティン/MIT を `var(--accent/green/orange)` の polyline。最新値は凡例に直値表示=非等比SVGにテキストを置かず歪ませない)⑧記録の継続(**コントリビューション・カレンダー**、日=ジャーナル+日報+0秒思考+完了Blockの活動量を4段階濃淡、全幅、活動日≥3)⑨時間帯別の活動量(**ヒストグラム**、actualStartAt の時分布、n≥5)。
- テーマ: 新SVGは `var(--…)` でダーク両対応(カテゴリ色はユーザ設定hexで意図通り)。全幅セクション ⑦⑧⑨ は `.stats-wide`(`grid-column:1/-1`)。折れ線の緑/橙は対サーフェス・コントラストが低い(dataviz検証で WARN)ため凡例の直値ラベルで識別を担保。

### 7.23 自動アーカイブ(データ肥大対策、v53)

localStorage は約5MB上限で、日報・フィードバック・ジャーナル・Block は無限に溜まる(maintainRecurrences は繰り返し未編集分しか消さない)。古い分を `archive/archive-<年>.json` へ退避して本体を軽く保つ。

- **保持期間**: reports / feedback / journals = 90日、Block = 180日(生きている集計の最長84日を安全に超える幅)。
- **絶対ルール: GitHub への書き込みが成功して初めてローカルから削除**(`runArchive`)。失敗時は何も消さず console.warn(手動時のみトースト)。
- 年ファイルは GET(`fetchGitHubJSONFile`、1MB超は Blob API)→ 日付キー/Block id でマージ → PUT(sha)。再実行は冪等。
- トリガー: 起動/復帰の8秒後に1日1回(`taskchute-archive-last-date`)、`settings.autoArchive` 既定ON。設定のデータカードに手動ボタン+データサイズ表示(`stateSizeLabel`)。
- 横断検索の「📦 アーカイブも検索」で年ファイルを lazy-fetch(`_archiveCache` 非永続)し「旧〜」バッジで合流(閲覧のみ・ジャンプ不可)。
- 既知の割り切り: WBSの⏱累計・計器盤「全期間」はローカル保持分のみ。

### 7.24 朝の体調相関 + テストCI(v53)

- **体調相関**: `morningEnergyCorrelation()` は**v60で削除**(§7.21参照。AIプロンプトへの注入先が全廃されたためのデッドコード削除)。
- **テスト/CI**: `tests/` にE2Eスイート(v49から導入、v138時点で88スイート前後・fetchモック+ローカルサーバで headless Chromium 実行)。`npm test` = `tests/run-all.js`(直列実行。fail-fastではなく**全スイートを実行しきってから**失敗件数を集計する)。`.github/workflows/test.yml` が push/PR(main)で実行。依存は devDependencies の `playwright-core` のみ(実行時依存ゼロは維持)。`npm run dev` = `tests/serve.js`(port 4173)。開発中は `node tests/run-all.js <絞り込み>` やpush前ローカルの `npm run test:core`(直近5バージョン+固定横断コア5本)で高速に回せる(詳細はリポジトリの`CLAUDE.md`)。

### 7.25 WBS の一括編集(v55導入。AI一括編集はv60で削除)

タスクの日付・状態・カテゴリを、編集モーダルを1件ずつ開かずに直せるようにする機能。導入時は
インライン直接編集+AI一括編集(自然文指示)の2系統だったが、AI一括編集はv60でAnthropic API
呼び出し前提のため削除された。現在は以下1系統のみ。

- **インライン直接編集(無料・オフライン。現行唯一の一括編集手段)**: WBS ツール「✏️ 編集モード」(`settings.wbsEditMode`、UI状態)。ON で各タスク行に 状態select / 期限date / カテゴリselect を表示(`renderTaskRow`)。`change` で `[data-wbs-edit]` → `updateTaskField` + `render`。入力は font-size 16px(iOS ズーム防止)。OFF で通常のバッジ表示に戻る。
- **AI一括編集**(`runAiBulkEdit`、WBS ツール「🤖 まとめて編集」) — **v60で削除**。代替なし(上記インライン直接編集が唯一の手段)。

### 7.26 Codexレビュー反映(v56)

Codex レビュー(`../taskchute-notes/review.md`)の指摘8件を反映。防御的な小修正が中心。

- **日時文字列の Date パース排除(high)**: `weekRange()` は `parseDate()` に統一。`isWishStagnant()` / ポモドーロ(`endsAt`/`startedAt`)/ `remainingText()` は新ヘルパ `localDateTimeToMs()`(`"YYYY-MM-DDTHH:mm[:ss]"` を正規表現分解 → 数値コンストラクタ)に統一。`new Date("...T...")` は iOS Safari が UTC 誤解釈し最大9時間ズレるため、Date(string) を一切通さない。
- **入力の font-size 16px 化(med)**: `.zt-add-text` / `.zt-search-input` / `.home-cd`(充放電 select)を 16px に。AIプロンプト textarea の inline `font-size:12px` も 16px に(iOS フォーカス時ズーム防止)。
- **タイムライン hover のオフセット撤去(med)**: `.timeline-card:hover` の `transform: translateX(-2px)` を廃し、位置を動かさない `box-shadow` に置換(絶対配置 top/left 前提の当たり判定と食い違わせない)。
- **下書き削除ボタンの前面化(high)**: `.draft-remove`(×)に `z-index:2`。短い下書きBlockで下端リサイズ帯 `.draft-resize`(DOM 上あと)にクリックを奪われ削除できない問題を解消。
- **アプリアイコン追加(med)**: `assets/icon.svg` を新規作成(manifest / favicon / apple-touch-icon の 404 解消。maskable 対応で全面塗り+中央安全域)。SW の APP_SHELL にも追加。
- **placeholder の引用符修正(low)**: 問いモーダルの placeholder 内の未エスケープ二重引用符を「」に。属性破壊を解消。
- **AIフィードバック fetch の 404 ノイズ解消(low)**: push 済み `.md` の日付を `state.feedbackFiles`(normalizeState で `[]` 補完)に記録し、起動時 hydrate は「記録あり かつ 手元に本文なし」の日付のみ fetch。存在しない `.md` を毎回叩かない。

### 7.18 横断検索(v49)

0秒思考・ジャーナル・問い・AIフィードバック・日報に散らばるストックを一発で引く。**モーダル完結、ナビは増やさない**。

- **入口**: 日付バー右端の 🔍(全ビュー共通)。
- **検索**(`crossSearchHits`): 2文字以上・大文字小文字無視の部分一致。対象 = `zeroThinking.entries`(テーマ+本文)/ `journals` / `questions`(text+settledNote)/ `feedback` / `reports`。新しい順・最大50件。スニペットはヒット前後 ±30/45 字 + `<mark>` 強調(`escapeHTML` 済み)。
- **フォーカス保護**: input では結果コンテナのみ innerHTML 差し替え(150ms デバウンス。0秒思考の履歴検索と同じ手法)。
- **ジャンプ**(`search-jump`): 該当ビュー + 日付へ遷移(`persistLocalNoSchedule` = UI 操作)。0秒思考ヒットは `ztSearch` に検索語を引き継いで履歴を絞り込んだ状態で開く。

> 以下 §7.27〜§7.38 は 2026-08-29 の棚卸しで追加した節。既存 §7.9〜§7.26 の番号が導入順で
> 飛び番になっている件は現状維持とし(過去の欠番はここでは修復しない)、新規サブシステムのみ
> 新しい節番号を振っている。詳細仕様の書き起こしはせず、参照ポインタ(概要+CHANGES参照)に
> 留める。

### 7.27 12WY二軸(成果トラック)MVP(v243〜)

Project直下に numeric/milestone 型の「成果トラック」を追加。登録フォーム→WBS表示→インライン編集→進捗トースト→週次確定シート(pre/post-commit)→COUNTDOWNスコア信号までを段階実装した。データ層は `tracks` / `trackMeasurements` / `weeklyCommitments`(§5参照)。旧`weekly`/`cycle`ビュー(§7.11/§7.15、v217削除)が担っていた週・12週スケールの後継の一部。
参照: `CHANGES_v257.md`〜`CHANGES_v268.md`、`CHANGES_v282.md`

### 7.28 固定化ルーティン + 習慣ストリーク(v252〜)

daily/weekdaysルーティン最大3件を「固定化」でき、固定化日以降の完了ログのみを静かにカウントする。保護ルーティン(`protection`、v114)は実行率の分母から除外。解除履歴は PIN ARCHIVE(`habitPinHistory`)として一覧表示し、INSTRUMENTS(§7.31)に表示する。
参照: `CHANGES_v252.md` / `v253.md` / `v278.md` / `v280.md`

### 7.29 TOWER統合画面(today、v230〜)

`home`(コックピット4ゾーン、§8)撤去後の後継ビュー(`renderToday`)。S2 GLASS意匠(オーロラ背景・ガラスパネル)、LIFE BAND+時計、全幅STANDING ORDERS等を段階導入した。**§8(4ゾーン組織原理)の記述が現行`today`にどう対応するかは今回未検証**(次回棚卸し推奨)。
参照: `CHANGES_v230.md` / `v274.md` / `v275.md` / `v277.md`

### 7.30 IRON LOG(v232〜)

筋トレ記録タブ(`renderIronLog`)。種目メニュー管理(追加/削除/並べ替え)、旧ジムBlockコメントからの初回一括移行(`ironImport`)、永続ID+削除tombstoneによる同期消失ホットフィックスを含む。
参照: `CHANGES_v232.md`〜`v234.md`、`v272.md`、`v284.md`

### 7.31 INSTRUMENTS(計器盤 再構築、v232〜)

§7.22で述べた旧`stats`削除(v217)の後継ビュー(`renderInstruments`)。固定化ルーティンの習慣ストリーク表示(§7.28)を軸に再構築されている。PCサイドバーから直接到達可能(v265で導線欠落を修正)。
参照: `CHANGES_v217.md` / `v232.md` / `v233.md` / `v253.md` / `v265.md`

### 7.32 FABLE FUND 閲覧タブ(v281〜)

仮想2000万円の模擬運用(K学習用)をアプリ内で閲覧する読み取り専用タブ(`renderFund`)。personal-dataリポジトリのNAV・保有ポジション・当日注文・約定を表示する。
参照: `CHANGES_v281.md`

### 7.33 AIレポート未読通知(v283〜)

AIフィードバック等をAIレポートタブ(`ai-reports`)へ統合し、同期対応の既読state(`aiReportReadIds`)+14日窓の未読バッジを追加した(§5参照)。投資系レポート(FABLE FUND日誌・朝の投資ブリーフ)も同機構に相乗りしている。
参照: `CHANGES_v283.md` / `v285.md` / `v286.md` / `v287.md`

### 7.34 ATIS(AI集約パネル)廃止(v269, v285, v289)

Today画面のATISパネルとFOCUSのAIチップを削除した。旧state互換は維持しつつ、現行はAIレポート(§7.33)への導線に一本化している(§7.33と対の関係)。
参照: `CHANGES_v269.md` / `v285.md` / `v289.md`

### 7.35 GATEルーティン運用改善(v276, v278)

時刻入力の5分整列、ワンタップ完了時の実時刻記録、固定化編集パネル(§7.28)の追加。
参照: `CHANGES_v276.md` / `v278.md`

### 7.36 WBS内検索・折りたたみ(v288)

インクリメンタル検索で対象行へ展開・スクロールする。新規Projectは既定で折りたたみ状態にする。
参照: `CHANGES_v288.md`

### 7.37 レイアウト / PC表示幅(v265, v277)

PCメインコンテンツ(TOWER系ビュー・Project詳細)の拡幅、サイドバーへの「その他」導線追加(§6参照)。
参照: `CHANGES_v265.md` / `v277.md`

### 7.38 SWキャッシュ自己修復(v273)

版ズレでモジュール読込失敗時、1セッション1回だけキャッシュ+SW登録を自己修復する。再失敗・オフライン時は安全な案内へ切り替える。
参照: `CHANGES_v273.md`

---

### 7.33 生活データ契約(v314〜、2026-09-02。CONCEPT §3-11〜14 の実装側規約)

アプリを「生活基盤(身体・お金・心・暮らし・学習の記録を1つの日付軸に束ねる土台)」として
育てるにあたり、**生活記録の種別を追加するときは実装前に次の表を埋める**(埋まらない項目が
あれば追加しない)。既存3系統(`condition.logs[date]` 日付キー / `sleep.logs[起床日]` /
`bodyScans[]`等の id配列)は移行しない。新規の単純な数値記録だけに共通封筒を使う(adapter-first)。

| 質問 | 答え方 |
|---|---|
| 正本はどこか | アプリ(手入力=一次記録。state に持ち personal-data へ同期)/ 外部バッチ生成物(閲覧専用。state に取り込まず fetch 表示のみ。例: `dashboard/fund.json`、`karada/health-daily.json`) |
| 帰属日と観測時刻 | `localDate`(YYYY-MM-DD、帰属規則を明記。睡眠は起床日=18時カットオフ)と `observedAt`(YYYY-MM-DDTHH:MM)を別に持つ。**文字列を `new Date()` に渡さない** |
| 単位・出所 | `unit`(kg/min/bpm…)、`source`(manual/csv/batch:<名前>) |
| 同期の競合規則 | 一次記録は id + updatedAt の新しい方(`mergeByIdPreferNewer`)。日付キー上書き型を新設しない |
| 削除 | `deleted: true` の墓標のみ(物理削除しない) |
| 保持期間・アーカイブ | 種別ごとに決める(1日1行の数値は無期限。高頻度配列は年次アーカイブ対象に加える) |
| 書き出し | 種別単位エクスポート(設定画面)の対象に入れる |
| 使われ先 | 日報・AIフィードバック入力・翌日の計画・計器のいずれか1つ以上を実装前に決める(「記録のための記録」を作らない) |
| 表示の置き場 | 新規タブを作らず、ジャーナル / TOWER BODY-MIND / INSTRUMENTS / AIレポートへ相乗り。入力は朝・Block完了後・夜の「手が止まる地点」だけ |
| 裁かない | 閾値による警告灯・達成色・ストリーク罰・生活値のTask評価点への合算をしない。未入力は「不明」 |

共通封筒(新規の単純計測値用): `{ id, type, localDate, observedAt, value, unit, source, updatedAt, deleted }`。
筋トレ(gym[])・睡眠・身体スキャンをこの形へ潰さない。

**2026-09-02 K裁定**: 体重は手入力欄を作らず Apple Health 経由(健康日次 `karada/health-daily.json`)の
閲覧のみとする(v314 で BODY/MIND に表示。体組成計の連携再開は K 側の運用)。
閲覧専用データの実装型は `src/features/fund.js` / `health.js`(モジュール内キャッシュ・schema検証・
失敗時は前回値維持・鮮度は末尾 `date` で判定・出所と時点を小さく表示)。

## 8. ホーム(コックピット)の組織原理

ホームは "タスク一覧" ではなく **操縦席**。注意の質で4ゾーンに分ける(`CONCEPT.md §4.5`):

| ゾーン | 色 | 内容 |
|---|---|---|
| Zone 1 | amber | 今日すすめる(MIT・タスクシュート着手) |
| Zone 2 | teal | 今日のリズム(ながれ・ルーティン) |
| Zone 3 | blue | 長い弧(12週・未完了バックログ) |
| Zone 4 | green | 今日の足あと(ジャーナル・エネルギー・信条) |

上部に**三つの信条**(毎日読む3行)と**寿命カウントダウン**を常置。最上部に「いま、これ」(進行中/次の Block)と「ひと目スコアボード」(着手率・MIT・ルーティン・12週)。

---

## 9. 命名・保守の慣習

- **バージョンコメント**: 変更には `// v37:` `// v38:` のように導入バージョンと*理由*を残す。コードが自身の変更履歴を語る。
- **SW キャッシュ版**: アプリに変更を入れたら `sw.js` の `CACHE_NAME` を上げる。
- **後方互換**: state のスキーマ変更は `normalizeState` にデフォルト補完を足す形で。破壊的変更をしない。
- **エスケープ**: ユーザー入力を `innerHTML` に入れる箇所は必ず `escapeHTML()`。Markdown 表示は `marked` の後にサニタイズ(v37)。
- **起動処理はモジュール末尾**: 全 `const`/関数の初期化後に `render()` 等を実行(先頭で呼ぶと TDZ で起動不能になる、v38 修正)。
- **非永続フィールドは `_` 接頭辞**: `modal` / `_justStartedBlockId` はシリアライズ時に落とす(`persistLocalNoSchedule` / `sanitizedStateForGitHub`)。localStorage・GitHub に UI 一時状態を持ち込まない。

---

## 10. 既知の制約・今後の論点

- **タイムゾーン**: 端末ローカル時刻前提。日付は `YYYY-MM-DD` 文字列で辞書順比較。多タイムゾーン運用は未対応(単一ユーザーのため許容)。
- **iOS のストレージ削除**: PWA の localStorage は OS に削除され得る。GitHub 同期がバックアップを兼ねる。
- **同期の粒度**: ファイル丸ごと「新しい方が勝つ」。フィールド単位のマージはしない(同一ユーザーの複数端末同時編集は稀という前提)。
- **10x 方向(`CONCEPT.md §6.1`)**: 現状は 2x(効率化)を強く支援。「価値の中身を深める」機能(問いを第一級オブジェクト化する等)は今後の検討課題。

---

## 11. 新機能を入れる前のチェック(要約)

`CONCEPT.md §7` のチェックリストを通す:

- どのコックピットゾーンに属するか / 属さないなら別画面か
- 反省→行動ループに接続するか(孤立機能でないか)
- 着手率・エネルギー可視化を薄めないか
- バッチ処理思考・完璧主義・ゲーミフィケーション・通知に寄っていないか
- 有限性(寿命・残り日数)と信条を埋没させないか

1つでも引っかかったら実装前に議論する。**このアプリは、K が自分の生き方に誠実であるための鏡である。**
