<!-- generated by scripts/release-record.js; edit releases/vNNN.json instead -->
# CHANGES v233: IRON LOG・INSTRUMENTS画面結線

## 概要

v232で配置済みのIRON LOGとINSTRUMENTSをapp.jsへ結線し、その他ナビ・画面間遷移・後方互換state補完を追加して専用E2Eで検証した

## 変更ファイル

- app.js
- docs/code-index.generated.json
- docs/code-index.generated.md
- docs/test-impact-map.generated.md
- releases/v233.json
- sw.js
- tests/action-registry-core.test.js
- tests/instruments-e2e.test.js
- tests/iron-log-e2e.test.js
- tests/suite-manifest.json

## 変更内容

- app.jsへconfigureIronLog/renderIronLogとconfigureInstruments/renderInstrumentsのimport・DI・renderMainディスパッチを追加した。
- instruments-open-iron-logはconfigureInstrumentsへ渡すregisterActionsを統合層で薄くラップし、凍結action名をsetView(iron-log)相当へ結線した。
- allowedViewsへiron-log/instrumentsを追加し、その他のフラットナビへINSTRUMENTS/IRON LOGをツール項目として追加した。
- normalizeStateでironImportとearlyRiseTarget/ironDailyTarget/ironManualBaseKg/gymBlockKeywordsを既存値優先で補完した。earlyBirdの既存v229正規化は重複追加していない。
- 先行E2E 2本をtestsへ適用してhelpers参照を./helpersへ変更し、INSTRUMENTS E2Eのナビ往復は実在する720px以下のbottomNavを使う700px viewportへ合わせた。
- 新規actionをaction registryの保存則ゴールデン一覧へ追加し、既存の重複・網羅性assertionを維持した。
- 目的でv234以降と明記されたNOW LANDING導線・Block完了時自動転記・未記録確認はv233へ持ち込まず、iron-log E2Eもv233対象の表示・追加・集計・実行中Block連動に限定した。
- Service WorkerのAPP_SHELLは変更せず、CACHE_NAMEだけv233へ更新した。
- code indexとtest manifestを再生成した。

## 変更意図

- p4-interface.mdで凍結された依存だけをDIし、IRON LOGとINSTRUMENTSを既存featureと同じ画面描画・action registry経路へ結線する。
- 旧stateを壊さず、ironImportと4つのsettings既定値を既存値優先の展開順で補完する。
- その他ナビから両画面へ到達し、INSTRUMENTSからIRON LOGへ移動してその他へ戻れる導線をブラウザE2Eで固定する。

## 検証

- node --check app.js、sw.js、tests/iron-log-e2e.test.js、tests/instruments-e2e.test.js: exit 0。
- system Edge指定のnode tests/iron-log-e2e.test.js: [1]〜[4]全PASS。
- system Edge指定のnode tests/instruments-e2e.test.js: [1]〜[8]全PASS。その他→INSTRUMENTS→その他→IRON LOG→その他の往復を確認。
- node tests/iron-log-core.test.jsとnode tests/instruments-core.test.js: 全件PASS。
- node tests/action-registry-core.test.js: 新規actionを含む保存則・重複ガードが全件PASS。
- npm.cmd run code:index:write/check: PASS(code index 576 functions)。
- npm.cmd run test:manifest:write/check: PASS(test manifest 130 suites)。
- 実行コード差分はapp.js 36追加/2削除、sw.js 1行置換で原則200行以内。
- CHROME_PATHをsystem Edgeへ指定したnode scripts/release-gate.js releases/v233.json: exit 0。選定31スイート、APP_SHELL 19件、manifest 130、code index 576が全PASS。

## 既知の制約・保留

- NOW LANDINGのopen-iron-log導線、ジムBlock完了時の自動転記と未記録確認、過去コメント移行の実行、睡眠実測一本化はv234以降。
- この環境では標準Playwright Chromiumがspawn EPERMになるため、ブラウザE2Eとrelease gateはsystem EdgeをCHROME_PATHへ指定して実行する。
- taskchute-notes/handoff.mdがワークスペース外書き込み制限でEPERMになる場合、release-recordはCHANGES生成までとして監督者側で再実行が必要。
- iOS Safari実機PWAでの最終確認は監督者側で必要。
