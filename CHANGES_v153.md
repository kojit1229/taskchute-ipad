# v153 ADHD支援「②今日の庭 S1」(gardenLog + 今日の芽)

罰なしゲーミフィケーション機能(`workbench/out/2026-07-27-appidea-designs/11-habit-garden.md`
§①〜⑤、K承認済み)のS1。ルーティン完了率に応じて「今日の芽」が育つ表示を追加した。
月間ピクセル(S2)・仕上げ(S3)は後続ステップ。

**本ファイルは初回実装+2系統レビュー(FAIL判定、必須4件+推奨5件)対応後の最終版**。
初回実装で見つかったデータ改竄バグ・同期マージ漏れ・配色のテーマ依存問題を修正した。

## 1. データモデル: `state.gardenLog`(新フィールド1つ)

日付ISO → `{done, total}` の日別ルーティン完了スナップショット。繰り返し実体は
`RECURRENCE_KEEP_PAST_DAYS`(7日)超過で物理削除され、過去日の分母(total)が失われるため、
`saveState()` の唯一のフックから当日 + `state.selectedDate`(異なる場合)の2キーだけを
`routineRate(blocksForDate(date))`(既存関数の再利用、新しい完了率計算は書いていない)で
upsertする(`updateGardenLog()`, app.js)。

- **フィールド別maxマージ**(2系統レビュー必須1、罰なしルール①のデータ層版): 初版は
  「`done`/`total`両方が既存値未満なら据え置き」という一括ガードだったが、**「doneは同値・
  totalだけ縮小」という組み合わせがガードを素通りする穴**があった(例: 既存
  `{done:4,total:5}`に対し、繰り返し実体のpurgeで再計算した値が`{done:4,total:4}`だと
  `done<existing.done`が偽になりそのまま上書き→4/5(80%)が4/4(100%=全完了)へ改竄される。
  完了取り消しでdoneだけ下がるケースも同様に素通りしていた)。`done`/`total`それぞれ独立に
  `Math.max(新値, 既存値)`を取るフィールド別maxマージへ修正し、この穴を塞いだ
  (`tests/v153.test.js` Part C3で改竄パターンの回帰を確認)。
- **`{0,0}`の無駄書き込み抑制**(2系統レビュー必須4): ルーティン0件の日にまで空エントリを
  書き込むと、日付ナビゲーションのたびに`GARDEN_LOG_KEEP_DAYS`分の無駄なキーが積み上がる。
  `total===0`かつ既存エントリも無い場合は何もせず抜ける。
- **保持上限**: `GARDEN_LOG_KEEP_DAYS`(400日=13ヶ月)超過分は同じフックで `pruneGardenLog()`
  が削除する。
- **マイグレーション**: `normalizeState()` に1行(オブジェクト型・非配列を保証)。既存stateには
  存在しないだけなので後方互換は自明。過去分の遡及生成はしない。
- **端末間同期マージ**(2系統レビュー必須2、両レビュー一致・データ消失クラス): 初版は
  `computeSyncMerge`にgardenLogの配線が無く、**端末Aだけが記録したgardenLogエントリが、
  端末Bのリモート採用時に丸ごと消える**不具合があった(swipeTriageLog等の他コレクションと
  違いマージ対象リストに含まれていなかったため)。`mergeGardenLogMaps()`(日付キーごとに
  `updateGardenLog()`と同じフィールド別maxマージを適用)を新設し、`computeSyncMerge`の
  `values`/`changedVsLocal`/`changedVsRemote`、`applySyncMergeToLocal`/
  `applySyncMergeToRemote`の3箇所へ配線した(設計書§③「doneの大きい方を採用」の実装。
  `tests/v153.test.js` Part Dでローカル限定エントリの生存+共有日付のフィールド別max統合を確認)。
- **AI連携なし**: 完全決定論(既存 `routineRate()` の再利用のみ)。

## 2. 今日の芽(zone2「今日のリズム」ルーティンカード内、`homeRoutine()`)

4段階を静的SVGの出し分けだけで表現する(成長のトゥイーンは作らない。`homeSteps()` と同じ
テンプレートリテラルSVG方式、`createElementNS` 不使用)。

| 段階 | 条件(`routineRate()`の`done/total/pct`から`gardenStageRank()`で導出) | 見た目 |
|---|---|---|
| (非表示) | `total===0`(その日ルーティンが1件もない) | `.home-garden`自体を出さない |
| 土 | `done===0` | 鉢+土のみ、`var(--line)`系の中立色。**文言なし**(沈黙) |
| 芽 | `done>=1` かつ `pct<50` | 双葉、`.g-stage1`(薄緑) |
| 若木 | `pct>=50` かつ未全完了 | 茎+葉、`.g-stage2`(緑) |
| 開花 | 全完了(`done===total`) | 茎+葉+花、`.g-stage3`(濃緑) |

段階が上がった直後だけ`.garden-grew`クラスでフェードイン(CSS `@keyframes garden-grow-in`、
`prefers-reduced-motion: reduce`時は無効化)。フラグは`state._gardenJustGrewDate`
(`state._justStartedBlockId`と同じ「1回の描画で消費」パターンの非永続モジュール変数。
`toggleBlock()`が完了操作直後にトグル前後の段階ランクを比較して立て、`render()`直後に
クリア。`persistLocalNoSchedule()`/`sanitizedStateForGitHub()`両方でシリアライズから除外)。
**2系統レビュー推奨5・6対応**: フェードは設計書どおり不透明度のみに絞り(拡大縮小`scale`を
削除、静かな計器の趣旨)、DOM差し替え型の再描画では実際には発火しない死コードだった
`.home-garden-svg`自体への`transition: opacity`を削除した。

添える文言は加点表現のみ(「今日は4件できた 🌸」形式)。`done===0`のときは何も言わない
(「まだ」「未達」等は出さない、罰なしルール⑥)。

## 3. 配色: 専用トークン`--garden-pale/mid/deep`(2系統レビュー必須3対応)

初版は3段階の濃淡を`opacity: .5/.8/1`(半透明合成)で表現していたが、**ダークテーマでは
背景色(ほぼ黒)と合成されるため「薄緑」が暗く沈んだ緑に化けてしまい、K確定の文言(薄緑/緑/
濃緑)と実際の見え方が食い違う**問題があった。opacityをやめ、ライト/ダーク別に実色を持つ
専用CSSトークンへ差し替えた(`styles.css` `:root`/`:root[data-theme="dark"]`)。

| トークン | ライト | ダーク | 選定理由 |
|---|---|---|---|
| `--garden-pale` | `#6bbd8d` | `#4a8f66` | 薄緑。ダークは黒背景に沈まない明度を確保 |
| `--garden-mid` | `#2fb96d`(既存`--green`と同値) | `#2fb96d` | 緑。既存アイコン装飾トークンとの一貫性を優先 |
| `--garden-deep` | `#1b6b3f`(既存`--green-text`ライト値と同値) | `#33e08c` | 濃緑。ダークは「濃い=明るく鮮やか」で強さを表現
  (GitHub草表示のダークモード配色と同じ考え方。ライトのまま暗緑を使うと黒背景にほぼ溶ける) |

node script実測(非テキストグラフィック3:1目安、WCAG 1.4.11): ライト側`pale`は白パネル上
2.26:1・`mid`は2.54:1(既存`--green`の実測値そのまま、v147コメントの精度と同水準の許容範囲)・
`deep`は6.52:1。ダーク側は`pale` 4.13:1・`mid` 6.32:1・`deep` 9.29:1。ライトの`pale`は
「薄い」色である以上、白背景に対する数値目標(3:1)を完全には満たせていない
(同一色相での輝度差には構造的な上限がある)。段階の判別は色だけに依存せず、形(双葉/茎葉/
花)とキャプション文言(件数)の3チャンネルで冗長化しているため、実運用上の識別性は担保できる
という判断で許容した(要確認事項として下記に明記)。

`decisions.md`「2026-07-27 ADHD支援2機能」でKが確定した「完了1件=薄緑/50%以上=緑/
全完了=濃緑」(元々はS2月間ピクセルの配色仕様)を、S1「今日の芽」の段階配色にも監督者指示で
適用した。このため設計書§④が例示していた「開花=オレンジの花」は採用せず、開花も緑系
(濃緑)で表現している(形=双葉→茎葉→花は設計書どおり維持)。この裁定は`decisions.md`にも
追記済み(K事後確認予定)。

## 4. 罰なし6ルールの遵守確認

1. **減らない**: gardenLog(データ層)はフィールド別maxマージで悪化上書き禁止(§1)。
   当日の「今日の芽」表示は設計書§③が明記するとおりライブ計算(`routineRate`)で、完了を
   取り消せばその場で表示も下がる(既存の「実行率%」バーと同じ挙動)。「当日中は一度上がったら
   下がらないよう凍結する」機構は追加していない(解釈メモ、下記「つまずいた点」参照)。
2. **未達日の表現**: 土=中立色(`var(--line)`系)のみ、赤・警告色・しおれ表現なし。
3. **連続を扱わない**: streak日数・「連続N日」文言は一切出していない。
4. **比較しない**: 先月比較・平均比較・他者比較は出していない。
5. **静かな計器**: 点滅・通知・数値バッジなし。アニメーションは完了トグル直後の1回の
   フェードのみ(不透明度のみ、拡大縮小なし)、reduced-motion時はゼロ。
6. **文言は加点表現のみ**: 「今日はN件できた」のみ。残数・欠落・「もったいない」は出さない。
   `done===0`は沈黙(文言自体を出さない)。

## 5. 検証

`tests/v153.test.js`を新規追加(全42チェック)。

- **Part A**(段階表示、22チェック): ルーティン0件で非表示 / ルーティン0件かつ既存エントリも
  無い日は`{0,0}`を書き込まない / 達成0(土)は段階クラス・文言とも無し / 1・2・3・4件目
  (25%/50%/75%/100%)の境界値で芽→若木→開花が正しく切り替わる / 境界値ちょうど50%が
  若木になる / 3段階の算出color(getComputedStyle)が3値とも異なる(opacity方式からの
  切替確認)/ 段階が上がった時だけ`garden-grew`が付き、同じ段階に留まる・下がる操作には
  付かない / 完了取り消しでライブ表示は下がるが、gardenLog(データ層)のdoneは下がらない /
  再読込後も正しい段階を維持しフェードフラグは残らない
- **Part B**(`normalizeState`後方互換、4チェック): gardenLogフィールド自体が無い旧state・
  不正な型(配列)のどちらも空オブジェクトへ補完される。既存の正常なgardenLogは補完で
  壊されない
- **Part C**(フィールド別maxマージ、6チェック): 前日のルーティンBlockが無い(purge後を
  模擬)状態で前日を閲覧しながらsaveStateを伴う操作(12週サイクルのタスク完了、日付非依存)を
  行っても前日のgardenLogは上書きされない / ルーティン0件の当日は`{0,0}`を書かない /
  **「done同値・total縮小」の具体的な改竄パターン**(既存`{done:4,total:5}`→実体purge後
  `{done:4,total:4}`)を再現し、4/5(80%)のまま維持される(4/4=全完了へ改竄されない)ことを
  直接検証
- **Part D**(端末間同期マージ、3チェック): ローカル限定gardenLogエントリはリモート採用後も
  残る / リモート限定エントリも合流する / 共有日付はフィールド別maxで統合される
  (GitHub Contents APIをモックし、実際のpull経路=`syncFromGitHubOnStartup`を通して検証)
- **Part E**(保持上限、3チェック): `GARDEN_LOG_KEEP_DAYS`(400日)を超えた古いエントリは
  `saveState()`経由で剪定され、上限内のエントリは残る

`node tests/v153.test.js`単体実行で全PASS(42/42)。既存のzone2/homeRoutine/routineRateを
参照するスイート(v73/v82/v114/v117/v146)を`node tests/run-all.js`でまとめて実行し全PASS
(回帰なし)。`npm run test:core`(フォアグラウンド、Bashツールのtimeoutパラメタを600000に
明示指定。対象=直近5バージョンv153/v152/v151/v150/v149+固定横断コア5本v50/v59/v67/v70/v72、
計10ファイル)を実行し全PASS(203.5s)。

push前・CIでの全量実行(`npm test`)は別途必要。

## つまずいた点・K確認事項

- 設計書§④「1. 減らない」の対象範囲が「gardenLog(データ層)のみ」なのか「当日ライブ表示も
  含む」のか、文言だけでは一意に決まらなかった。設計書§③の「当日はライブ計算で足りる」という
  記述(gardenLogが過去日にのみ必要な理由づけ)と、既存の「実行率%」バーが完了取り消しで
  素直に下がる既存挙動との整合を優先し、「当日ライブ表示はライブのまま(取り消せば下がる)、
  gardenLogだけ悪化上書き禁止(フィールド別max)」と解釈して実装した。「当日中は一度上がったら
  段階を凍結する」仕様が意図であれば追加の状態管理(例: 当日の最高到達段階を別途保持)が
  必要になるため、相違があれば指示願いたい。
- decisions.mdのK確定「薄緑/緑/濃緑」はS2月間ピクセルの配色として書かれていたため、S1の
  芽の配色にそのまま流用してよいかは監督者指示に従った(§3参照、decisions.mdにも追記済み)。
  設計書のオレンジの花との差分として明記した。
- §3のとおり、ライトテーマの`--garden-pale`は非テキストグラフィック3:1の目安を完全には
  満たせていない(2.26:1)。色相を保ったまま「薄い」を表現する以上の輝度確保が構造的に難しく、
  形+文言との併用で識別性を担保する判断とした。より高い数値が必須であれば、パレットの
  再設計(例: 薄緑を彩度でなく明度だけで作る等)が必要になる。
