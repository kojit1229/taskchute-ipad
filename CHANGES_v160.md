# v160 AI機能第4弾「言い訳ハンター」

K発注仕様(workbench/out/2026-07-27-taskchute-ai5/spec.md 機能4)。夜間バッチが日報の
未完了理由・言い訳に相当する箇所を決定論で台帳に蓄積し、週次でAIがパターンのランキング
レポートを淡々と生成する(説教・改善命令なし。事実の提示のみ)。バッチ側実装は ClaudeCode
ワークスペース側(`loop/scripts/excuse-ledger.sh` / `excuse-ledger-extract.py` /
`loop/scripts/excuse-report.sh` / `excuse-report-extract.py` / `excuse-report-validate.py` /
`loop/excuse-report/prompt.md`)。本ファイルはアプリ側(taskchute-ipad)の変更点のみを記録する。

## アーキテクチャ

既存のAI連携パターン(バッチ→personal-data→アプリfetch、ai-linked-app-dev Skill)をそのまま
踏襲。アプリ内からAI APIは一切呼ばない。v157(今日の敵)/v158(勝手に格言)/v159(未来からの
手紙)と異なり、K発注仕様が「表示は既存フィードバック導線に相乗りのみ(ホーム導線は作らない
=淡々と)」と明記しているため、**アプリ側の変更は既存AIレポート画面(AI_REPORT_TYPES/
report-index機構)への1エントリ追加のみ**で完結する(v159で追加した`cachedFutureLetterMd`
のようなホームカード専用のfetch・キャッシュ・union防御の類は本機能では意図的に作っていない)。

## 変更内容(app.js)

1. `AI_REPORT_TYPES` に
   `{ id: "excuse", label: "言い訳レポート", prefix: "言い訳レポート_" }` を追加。
   既存の`aiReportFilesForType()`/`renderAiReportBody()`/report-index機構が無変更でそのまま
   使える(v159の未来からの手紙と同型の追加。日次ファイル名の日付部分`YYYY-MM-DD`は既存の
   `slice(prefix.length, -3)`ロジックがそのまま機能する)。
2. ホーム画面への新規導線・新規カードは**追加しない**(K発注仕様どおり)。

## SWキャッシュ

`sw.js` の `CACHE_NAME` を `v159` → `v160` へ更新。

## テスト

`tests/v160.test.js` を新設。観点:
1. AIレポート画面の種類タブに「言い訳レポート」が追加される(タブ表示)
2. 選択するとreport-index.json経由で`言い訳レポート_*.md`の履歴一覧が日付降順(新しい順)で
   表示され、他kind(letter/content/weekly)・他prefixのファイルが混ざらない(一覧・kind判定)
3. 既定選択(最新)の本文が表示される(本文)
4. 一覧から別の日付を選ぶと、その日付の本文に切り替わる(本文の再フェッチ)
5. ホーム画面には本機能専用の新規導線・新規カードが無い(K発注仕様「ホーム導線は作らない」の
   直接検証。v159の`open-future-letter`のような専用data-actionが存在しないことを確認)
6. 公開Pages側(同一オリジン)への`言い訳レポート_*.md`へのfetchは一切発生しない
   (同一オリジンfetch回帰の防止、v157/v158/v159と同じ観点)

テスト内のfixture本文(`LATEST_BODY`/`OLDER_BODY`)は実際のバッチ生成物(private/personal-data
リポジトリ)の逐語ではなく、完全に架空のテスト専用文字列にしている(v157〜v159.test.jsと同じ
「公開repoにprivate生成物の文面を置かない」原則)。

既存テストへの影響は無い想定(新規タブの追加のみで、既存DOM構造・既存アクション・既存
AI_REPORT_TYPES要素は無変更)。

## バッチ側の実証(参考、ClaudeCodeワークスペース側)

### 日次: `loop/scripts/excuse-ledger.sh`(決定論、claude不使用)

「未完了理由・言い訳欄」に相当する専用フィールドがapp.jsに存在しないため(FORMAT_CONTRACT.md
突合表に該当なし)、日報Markdownの既存2見出し `## 6. やり残し`(未完了Block一覧)×
`## 7. Block 内のコメント`(Blockのコメント)の**交差**を代理指標として採用した(実装判断。
詳細は`excuse-ledger-extract.py`冒頭コメント参照)。

- --dry-run: personal-dataへの書き込みをせず、抽出→検証の配線のみ確認(検証OK・pushなしを確認)
- 1回目(実行、`--date 2026-07-23`): 実データで交差が1件検出できた唯一の直近日を指定して
  実行(直近日は交差0件のためskip扱いになるだけで検証にならない)。`personal-data/taskchute/
  excuse-ledger.json`を新規生成しcommit・push成功(追記1件、対象日=2026-07-23)
- 2回目(実行、同じ`--date 2026-07-23`): 台帳に対象日のエントリが既に1件あるため
  「スキップ(冪等)」で即exit 0(抽出・追記を一切行わない)
- 既定(`--date`省略、対象日=当日2026-07-28): 当日の日報がまだ存在しないため
  `source=none`で正常スキップ(exit 0)を確認

### 週次: `loop/scripts/excuse-report.sh`(AI)

- --dry-run: 台帳読み込み・claude呼び出しをせず、ダミーデータで検証(禁止語チェック含む)の
  配線のみ確認(検証OK・pushなしを確認)
- 1回目(実行): 台帳(上記で1件追記済み)の直近4週分(1件)を抽出→claude生成→
  禁止語チェックを含む検証OK→`personal-data/taskchute/言い訳レポート_2026-07-28.md`を
  生成しcommit・push成功→`report-index.json`も再生成・push成功
- 2回目(実行): 直近7日以内に既存レポートがあるため「スキップ(冪等)」で即exit 0
  (抽出・生成を一切行わない)
- 生成された実レポートは「この期間に記録された1件のうち…」と件数の分母を明示した事実提示のみで、
  説教・改善命令・「〜べき」の類は含まれていないことを目視確認済み(禁止語チェックも通過)

## 2026-07-28レビュー対応(2系統、Codex=アプリ側指摘ゼロ、Claude=条件付き)

代理指標の是非(app.jsに専用フィールドが無いことへの対応方針)はK裁定事項として対象外とし、
以下の機械的な修正のみを実施した(ClaudeCodeワークスペース側`loop/scripts/`配下、および
テスト側の修正。personal-dataへの検証push以外はcommit/pushしていない)。

### 必須修正1(R2): 台帳破損時の無音全消去の防止
`excuse-ledger.sh`のマージ・冪等チェックの両方で、「台帳ファイルが存在するのにJSON解析失敗/
配列でない」場合に`[]`へフォールバックしていた(=壊れた台帳を「空の台帳」として扱い、
新規1件だけの内容で上書きpushすると過去の全エントリを静かに消してしまう)。フォールバックを
やめ、ファイルが存在するのに壊れている場合はdie(フェイルラウド、上書きしない)に変更した。
`[]`が正当なのはファイル自体が無い(初回実行)場合のみ。`loop/FORMAT_CONTRACT.md`の該当節に
この挙動(=停止・上書きしない)を明記した。スクラッチ用の一時gitリポジトリで実際に壊れた
JSONを台帳へ置いた状態で実行し、`ERROR: 台帳ファイルが壊れているため停止`でexit 1・
ファイル内容が変化しないことを確認した(検証ログは本節末尾参照)。

### 必須修正2(R3): 禁止語チェックの偽陽性・偽陰性の両方向修正
`excuse-report-validate.py`の`BANNED_PATTERNS`を修正:
- 偽陽性: `べき`を`べき(?![られ])`へ(「食べきれない」「調べきれない」等の可能形否定を除外)。
  `頑張`は`頑張ろう`/`頑張りましょう`のみ常時検出とし、`頑張って`は直後がみ/い(回顧的な
  「頑張ってみた/いた」)の場合を除外。`反省`は単独名詞的言及(「反省点」等)を除外し、
  勧奨語形(`反省しましょう`等)のみ検出するよう限定。
- 偽陰性: `して下さい`(漢字表記)に対応する正規表現バグを修正(旧`(して|下さい)ください`は
  「してください」しか拾えていなかった)。`改善`単体、`〜ほうがいい`/`〜方が良い`、
  `〜と良いでしょう`/`〜とよいでしょう`、`〜が課題`を新規追加。
- `loop/excuse-report/prompt.md`の「コメント本文をほぼそのまま引用してよい」を「短い要約のみ
  (引用符での逐語引用は避ける)」へ変更し、Kの原文にたまたま含まれる禁止語(べき/ください等)
  を逐語引用した結果レポート全体が機械チェックで弾かれる、という機械チェックとの衝突を解消した。
- 検証: レビューが挙げた偽陽性4例(食べきれない/調べきれない/頑張ってみたが/反省点の名詞的
  言及)・偽陰性5例(して下さい〈漢字〉/改善単体/〜ほうがいい/〜と良いでしょう/〜が課題)を
  実際に`excuse-report-validate.py`へ通し、全て意図どおり(偽陽性側はexit 0、偽陰性側は該当
  パターンでexit 1)判定されることを確認した。既存の真陽性(べきだ/しましょう/頑張ろう/
  反省しなさい)が引き続き検出されること、および初回実装時に実際に生成・pushした
  `言い訳レポート_2026-07-28.md`が新ルールでも引き続きexit 0で通ることも回帰確認した
  (検証ログは本節末尾参照)。

### 軽微1: 初回実行時のpush失敗ロールバック
`excuse-ledger.sh`の push失敗時ロールバックは、既存台帳への追記なら`git checkout --`で
追記前の状態へ戻すが、**今回が台帳の初回生成コミットだった場合**(pull直後の時点で
`excuse-ledger.json`自体が存在しなかった場合)は`git reset HEAD~1`後にHEADへその
パス自体が存在しないため`git checkout --`が失敗し、新規作成された台帳が未追跡ファイルとして
作業ツリーに残り続けてしまっていた。pull直後に台帳の存在有無を`LEDGER_EXISTED_BEFORE`として
記録し、初回生成時は`rm -f`(他3機能=today-enemy.sh等と同じ「新規ファイルはrm -fで消す」
パターン)、既存追記時は従来どおり`git checkout --`、という分岐に変更した。スクラッチ用の
一時gitリポジトリ+到達不能なbogus remoteでpushを確実に失敗させ、初回生成/既存追記の両方の
シナリオで実際にロールバックが正しく動作すること(前者はファイルが消え、後者は追記前の
ベースライン内容へ正しく復元される)を確認した(検証ログは本節末尾参照)。

### 軽微2(必須修正4): エントリ組み立ての異常検知漏れ
`excuse-ledger.sh`の台帳エントリ組み立て(`python -c`)が失敗しても`|| die`が無く、
出力ファイルが空/未生成のまま次の判定へ進み、`NEW_COUNT`が0扱いになって「有効な本文が0件の
ため正常スキップ」という非対称な誤判定(実際は異常なのに正常系のexit 0に化ける)が起きうる
状態だった。`if ! python -c ...; then die ...; fi`で確実に検知するよう修正した。

### 推奨修正5: 同名Block重複時の取り違え防止
`excuse-ledger-extract.py`の`parse_comments()`が dict(`{title: body}`)で実装されていたため、
同名Blockが複数存在する場合、後に出てきたコメントが前のコメントを無条件に上書きし、
「やり残しBlockが複数あるのに台帳へは高々1件しか記録されない」という取りこぼしが起きていた。
リスト(`[(title, body), ...]`、日報内の出現順を保持)へ変更し、やり残し行1つにつき
「まだ使っていない同名コメントを先着順に1件だけ」貪欲マッチングで割り当てる方式にした。
これにより、同名Blockが複数あってもコメントの使い回しは起きず(1つのコメントは高々1つの
やり残し行にしか紐付かない)、記録件数が実際の未完了Block数に応じて正しくスケールする
ようになった。**残る限界**: 同名Blockが複数ある場合の「どのコメントがどの具体的なBlock
インスタンスに属するか」の完全な一意特定は、日報Markdownに Block ID が含まれていないため
原理的に不可能であり(どちらの割当も等しく尤もらしい)、本修正は「dictのキー衝突による
静かなデータ消失・取りこぼし」を解消するに留まる。実データ(2026-07-23分、重複無し)での
再検証では修正前と同一の抽出結果になることを確認した(検証ログは本節末尾参照)。

### 推奨修正6: テストの同語反復アサート解消
`tests/v160.test.js`の[5](ホーム画面に専用導線が無いことの確認)で、「data-action名に
'excuse'を含む要素が0件」「HTML内に専用文言が無い」という陰性側のみを見ており、そもそも
ホーム(内省側)タブが正しく描画されていない場合でも同じ結果(0件)になってしまう同語反復
だった。既存の「AIから」カードのラベルが実際に描画されていることを先に確認する陽性側の
アサートを追加し、描画が生きている前提のもとで陰性側を判定する構成に差し替えた。

### 推奨修正7: CHANGES_v160.mdへの精度・再現率の明記
上記「懸念点」節に、レビューで指摘された実データでの精度0/2・再現率実質0(代理指標の限界)を
明記した(指標の扱いはK裁定待ち)。

### 対応不要とされた項目
R1(代理指標の是非そのもの)は監督者がK裁定事項として扱うため、本レビュー対応では変更していない。

### 追加で対応(要求外だが同種の非対称性のため)
`excuse-report.sh`の冪等チェック(直近7日以内の既存レポート有無を確認する`python -c`)にも
必須修正4と同種の「失敗が正常スキップに化る」非対称性があったため、同じ`|| die`ガードを
追加した(要求リストには明記されていなかったが、同一カテゴリのバグとして横展開した)。

## 未対応(K承認待ち)

- `loop/scripts/excuse-ledger.sh` / `loop/scripts/excuse-report.sh` のタスクスケジューラ登録・
  coach-dailyチェーンへの組み込みは実施していない(spec通り単体実行検証まで)。

## 懸念点(監督者へ共有)

- 「未完了理由・言い訳欄」はapp.jsに専用フィールドが存在しないため、`## 6. やり残し`×
  `## 7. Block 内のコメント`の交差を代理指標として実装した(上記参照)。この解釈が正典でない
  場合は`excuse-ledger-extract.py`の抽出ロジックを差し替える必要がある。
- 上記の理由により、実データでの交差が非常に少ない(直近1週間強で1件のみ)。台帳・レポートの
  実効性はデータが蓄積されるまで判断できない。
- **2026-07-28レビュー(Claude reviewer)指摘**: 実データでの精度0/2・再現率実質0(代理指標の
  限界)。指標の扱いはK裁定待ち(代理指標の是非そのものは本レビュー対応の対象外とし、機械的な
  修正〈台帳破損時の挙動・禁止語チェックの精度・ロールバック分岐等〉のみ本バージョンで対応した)。
