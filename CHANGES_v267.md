<!-- generated by scripts/release-record.js; edit releases/vNNN.json instead -->
# CHANGES v267: 12WY MVP統合E2E仕上げ

## 概要

12WY MVPの登録から同期までの全通し、測定後8日未更新、土曜0:00週境界を重複なく固定し、既知の限界を集約した。

## 変更ファイル

- CHANGES_v267.md
- docs/e2e-coverage-map.md
- docs/test-impact-map.generated.md
- releases/v267.json
- scripts/test-manifest.js
- sw.js
- tests/suite-manifest.json
- tests/v266.test.js
- tests/v267.test.js

## 変更内容

- v267 E2Eへnumeric/milestoneフォーム登録からWBS、manual週次確定、候補Block完了、COUNTDOWN 50%、進捗トースト、measurement、同期push/pull生存までの全通しシナリオを追加した。
- 同じ全通しシナリオで週次確定2回・完了4回・成果入力3回のsaveState回数、completedAt系の同秒tie、決定論的commitment idの非重複を確認した。完了は確定items由来の実Block IDと実DOMのdata-actionボタンを使う。
- 確定済み週の実DOM免除操作からCOUNTDOWNの分母減少と全免除N/Aまでを直接確認した。
- 同じ測定の7日後は未更新でなく、8日後に未更新へ変わる両側境界を追加した。
- Asia/Tokyo固定の金曜23:59:59から土曜00:00:00でtodayISO由来のweekStartが切り替わるJST時刻境界を追加した。
- 同期往復後のtracks、trackMeasurements、weeklyCommitmentsを、件数・ID集合・値・trackId/blockId参照を含む全量同値で確認した。
- Service WorkerのCACHE_NAMEをv267へ更新した。
- v266のCACHE_NAMEガードを固定値からreleases最大版との一致へ改め、次リリースの正当なcache bumpでcoreが偽失敗しないようにした。
- §8、仕様追補、v257〜v266で実体化済みの継続制約をknownLimitationsへ集約し、§6.2対応表の確定版をdocs/e2e-coverage-map.mdへ永続化した。

## 変更意図

- 分散していたMVP判定条件1〜4を、登録・確定・完了・スコア・成果入力・同期の1本の利用者シナリオとして機械固定する。
- 既存fast-nodeで固定済みの8日未更新計算と週境界を、実ブラウザの時刻・表示連鎖まで補完する。
- 既存スイートの検証を重複させず、MVP完了判断に必要な残り3件だけを追加する。

## 検証

- node --check tests/v266.test.js / tests/v267.test.js: PASS。
- システムEdge指定 tests/v267.test.js: 実DOM全通し、COUNTDOWN免除、同期全量一致、7日/8日未更新、JST土曜0:00境界を含め全件PASS。
- release-gate --final内の tests/v266.test.js: releases最大版v267のCACHE_NAMEガードを含め全件PASS。
- npm run test:manifest:write: WROTE 151 suites。release-record --write / --check: handoff mirrorでPASS(既定notes側はsandboxのEPERMにより監督者反映待ち)。
- node scripts/release-gate.js releases/v267.json --final: exit 0。impact-regression 6本とtest:core 10本を含めPASS。

## 既知の制約・保留

- 週番号(7日刻み)とスコア週境界(土〜金)は、12週開始日が土曜でない場合にずれるため、設定画面では土曜開始を推奨する。
- 週を跨いだ完了取り消しは、過去週不変の原則を優先してスコアへ反映しない。
- 85%基準は原著の裏取り未完のため目安表記とし、70〜100で設定可能にしている。
- cycleWeekProgressは新しい週次実行スコアと並走し、統合判断は実運用後とする。
- 任意曜日集合のrecurrenceはMVP外で、当週の増減は単発Block作成と計画追加で扱う。
- 節目の予定日変更履歴はoriginal/currentの2点だけを保持する。
- 未更新閾値は固定8日で、expectedUpdateDaysの可変化はMVP外とする。
- measurementのobservedAtは記録時刻固定で、過去日補正は再入力で扱う。
- トーストの1日1回ログは端末ローカルで、保存前の再読込やリモートstate採用による同日再提示を許容する。
- 2端末がともにmanual確定した場合は週メタLWWとitem和集合で収束し、勝者メタのselectedBlockIdsを分母にする。
- 過去日の成果状態は現在の予定日から再計算され、週末時点のstatusスナップショットは保持しない。
- autoSource(IRON LOG連動)はMVP外とする。
- 設定の12週開始日を変更しても各Projectは自動移行せず、WBSの明示的な新サイクル移行を使う。
- 日報の既存12週進捗行と新設予定の週次コミット行は対象Project集合が異なりうる。
- 同一trackで目標値・期限を変えるとペース線を全期間再計算し、過去時点の状態表示も変わる。
- 2端末同期で生じた重複active trackは操作まで残り、読取側が決定論で1本を選ぶ。
- 有効節目0件のmilestone trackは通常保存では生成されないため、空の期限・節目チェーン表示は防御していない。
- WBSエディタの未保存入力は別の保存操作によるrenderで最新保存値へ戻る。
- 成果入力の成功1操作では、データ保存・quiet日報・saveAndRenderによりsaveStateが計3回走る。
- Block完了の成功1操作では、Block保存・週次item刻印・saveAndRender等によりsaveStateが計4回走る。
- トーストの「その他…」展開中に対象trackがcloseまたは削除されると確定は無視され、自動消滅タイマー解除後のトーストが開いたまま残る。通常の増分/同値トーストは8秒で自動消滅する。
- 週次確定シート表示中のバックグラウンド同期で候補が変わると一覧が一時的に古く見えるが、送信時フィルタで不整合を防ぐ。
- autoCloseStaleRoutineRunsは完了解除フック未結線のため、開始時刻あり・終了時刻空のルーティンBlockで当週completedAtが残る場合がある。
- ライトテーマのTRACKS状態チップにはv260由来でTOWER暗背景上約2.9〜3.7:1となる既存コントラスト制約があり、別リリースで対応する。
- iOS実機PWAでのService Worker更新、オフライン起動、ネイティブ入力、390/768/1024/1280pxの最終目視確認は監督者側で必要。
