# CHANGES v101

## 概要

K報告(2026-07-15)「ビジョンタブをPCブラウザで開くと毎回固まる」の再現・原因特定・修正。

SW `CACHE_NAME` を v100 → v101 に更新。

---

## 原因(現物調査・実測)

ビジョンボード(`renderVisionBoard()`)は45歳/80歳/今(33歳)の3枚のPDFを表示する。実データの
サイズは 45_vision.pdf=3.4MB / **80_vision.pdf=18MB** / now_vision.pdf=3.6MB で、80歳分だけ
突出して大きい。

v85実装は、ビジョンボードタブを開く(またはPDFタブを切り替える)たびに `ensureVisionPdfLoaded()`
が自動的に `fetchGitHubRawBlob()` でPDFを取得し、取得完了後は無条件で
`<object data="blob:...">` にインライン埋め込んでいた(1ファイル1回のfetchキャッシュ自体は
機能しており、二重fetchや無限ループは無かった)。

Playwright + 実Chromium(headless)で18MB相当のPDF(1ページ・3200x1900の高解像度画像を
埋め込んだ有効なPDF)を用意し、デスクトップ幅(1280px)でこの経路を計測した結果:

- アプリ本体のJSメインスレッドの応答性(15ms間隔のハートビートtickの最大間隔)は **145.6ms** で、
  数秒級の長時間ブロックは確認できなかった。
- タブ切替クリックからUIのactiveクラス反映までは **239ms**。
- `fetchGitHubRawBlob`(`response.blob()`)自体は非同期でメインスレッドを塞がない。

つまり、**このアプリ自身のJS実行そのものは大容量PDFの取得・Blob化では固まっていない**。
残る説明は、`<object>` が起動するブラウザ内蔵PDFビューア(別プロセス/別レンダラで多ページ・
高解像度の画像コンテンツを描画する処理)がSPA自身のタブの描画・入力キューと競合し、体感上
「画面全体が固まる」症状を生んでいるという点にある。JS heapは正常なまま画面が無応答に見える
という症状(K報告の「毎回固まる」を含む)とも整合する。この競合の内部メカニズム(別プロセスの
GPU/コンポジタ資源競合か、メモリ圧か)までは本セッションでは特定できていない(下記「残る
懸念」参照)。

Blob URLキャッシュ(`cachedVisionPdfUrls`)自体は各セッション(=各ページロード)で空になる
ため、「開くたびに」= セッションごとに毎回この重い経路を通ることも、K報告の「毎回」という
体感と符合する。

## 修正内容

- **自動fetch・自動インライン埋め込みを廃止**。`ensureVisionPdfLoaded()` を
  `loadVisionBoardPdf()` に置き換え、タブを開いただけでは何もfetchしない
  (`data-action="vision-board-load"` の明示クリックでのみ発火)。
- **`<object>` インライン埋め込みを完全撤去**。取得完了後は実アンカー
  (`<a href="blob:..." target="_blank" rel="noopener">別タブで開く</a>`、v85から既存の
  「別タブで開く」導線と同じ仕組み)のみを表示し、重いPDF描画はブラウザ本来の独立した
  PDFビューア(別タブ・別プロセス)に完全に委ねる。SPA本体のタブは重い描画と競合しなくなる。
- 1ファイル1回のBlobキャッシュ(`cachedVisionPdfUrls`)、多重fetch防止
  (`_visionPdfLoadInFlight`)はv85の設計をそのまま維持(オンデマンド化しただけで、
  再訪時に再fetchしない挙動は変えていない)。

### UX変更点(明記)

従来は「タブを開く→自動でPDFがインライン表示される」の1ステップだったが、v101からは
「① 『このPDFを読み込む』ボタンを押す → ② 『📂 別タブで開く』を押す」の2ステップになる。
3枚とも同じ扱いに統一した(サイズで挙動を分けると一貫性を欠くため。またAPIから事前にファイル
サイズを取得する経路が無く、閾値判定を追加すると複雑化するため見送った)。iPhone/iPadでも
同じUXになる(v85時点でPCとiPhoneで実装を分けていなかったため、両対応で統一する形が
最も安全と判断)。

## テスト

`tests/v101.test.js`(新規、6ブロック・21チェック):

1. タブを開いただけでは自動fetchされない(オンデマンド化の確認)
2. 「読み込む」クリックで1回だけfetchされ、blob URLの`<a href>`に切り替わる
3. 同じファイルを再度開いても再fetchしない(1ファイル1回のキャッシュ維持)
4. 未クリックの他タブは一切fetchされない(3ファイル同時取得なし)
5. 18MB相当の大容量PDFが絡む一連の操作(タブ切替→読み込む→リンク表示)が5秒以内に応答し、
   読み込み完了後も他のUI操作(タブ切替)が即座に反映される(固まっていないことの直接確認)
6. 「別タブで開く」の実アンカークリックで実際に新しいタブ(popup)が開く(`<object>`不使用の確認)

既存スイート `tests/v85.test.js`([A1][A2])・`tests/github-vision-pdf-fallback.test.js` は
UX変更(自動表示→クリックしてから表示)に合わせて更新した(検証内容の弱体化ではなく、
新しいクリック操作を挟む形に書き換えただけ。Blob URL化されること自体の検証は維持)。

`npm run test:core`(11スイート、v101追加分含む)ALL PASS。`node --check app.js` exit 0。

## 残る懸念・未対応

- **`<object>`埋め込みによる真の内部メカニズム(GPU/コンポジタ資源競合かメモリ圧か)は
  未特定のまま**。headless Chromiumでの計測ではアプリ自身のJSメインスレッドは大きくブロック
  されなかった(最大145.6ms)ため、実機のPC実ブラウザ(GPU構成・拡張機能・メモリ状況が
  異なる)で同じ計測をしてこの修正で体感が解消したことをKに確認してもらう必要がある。
  今回の修正は「重い描画をSPA自身のタブから完全に排除する」という、原因の特定箇所によらず
  有効な方向で対策したものであり、原理的には解消するはずだが、実機での体感確認は未実施。
- 3枚とも一律クリック方式にしたため、3.4〜3.6MBの2枚(45歳/now)も従来の自動表示より
  ワンクッション増える。K体感でここが不便であれば、小さい2枚だけ自動表示に戻す差分は
  小さい(が、ファイルサイズを事前に知る手段が無いため「決め打ちの閾値」か「先に軽いHEAD
  相当の問い合わせを追加する」かの判断が要る)。
- `URL.revokeObjectURL`によるBlobの解放は引き続き未実装(v85からの既知の未対応点、CHANGES_v85.md
  参照。オンデマンド化により実質的なfetch頻度が下がったため優先度はさらに下がったと判断)。
- `../taskchute-notes/review.md` には本タスクと無関係な既存の未対応指摘が多数(severity: high
  含む)残っているが、本タスクは監督者からビジョンボードのフリーズ修正に限定して指示された
  ため、スコープ外として着手していない。
