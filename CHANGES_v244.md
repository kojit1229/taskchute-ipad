<!-- generated by scripts/release-record.js; edit releases/vNNN.json instead -->
# CHANGES v244: 12WY同期配線の防御ガード対称化

## 概要

computeSyncMergeの3コレクション配線で、ローカル側とマージ入力にも || [] ガードを追加し、normalizeState未通過のstateでも例外化しないようにした。

## 変更ファイル

- releases/v244.json
- src/sync/github.js
- sw.js

## 変更内容

- computeSyncMergeの3コレクションのマージ呼び出し(tracks/trackMeasurements/weeklyCommitments)で、state側・remoteNorm側の両引数を || [] でガードした。
- changedVsLocal相当の3項のsameArrayByReference比較で、state側配列を || [] でガードした(未定義時は空配列同士の比較となり誤検知しない)。
- Service WorkerのCACHE_NAMEをv244へ更新した。APP_SHELLは変更なし。

## 変更意図

- CI全量のv197(素のfixtureでcomputeSyncMergeを直接呼ぶ)で発覚した、v243配線のローカル側ガード欠落を塞ぐ。
- remoteNorm側のchangedVsRemoteガード(v243)と同じ防御水準へ両側を揃え、fail-closeの取りこぼしをなくす。

## 検証

- node tests/v197.test.js: ALL PASS(修正前はTypeErrorでnull化)。
- node tests/run-all.js store-core merge-core track-merge-core track-sync-characterization track-normalize: 5 suite全件PASS、exit 0。
- node scripts/release-gate.js releases/v244.json --final: 監督者の標準環境で実行しexit 0(結果は最終報告参照)。
- 実行コード差分はsrc/sync/github.js 8行・sw.js 2行で200行以下。

## 既知の制約・保留

- v197が示したとおり、normalizeStateを介さずcomputeSyncMergeを呼ぶテストは最小fixtureで新コレクションを持たない。今後コレクションを追加する際は両側ガードを標準とする。
