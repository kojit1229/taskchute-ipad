# CHANGES v90

## 概要

K指示(2026-07-14)「0秒思考タブのテーマ一覧を、WBSのプロジェクトのように大テーマ、
小テーマの階層構造にしてください」への対応。テーマ一覧(0秒思考タブ)に「大テーマ
(グループ)」を導入し、小テーマ(既存のテーマ)をグループ配下にまとめて表示できるようにした。

**注意**: taskchute-notes/ROADMAP.md の論理番号「v90: Study With Me」はv84として既に実装済み
(CHANGES_v84.md参照)。本バージョン(SWの実番号v90)は無関係の別機能であり、混同しないこと。

SW `CACHE_NAME` を v89 → v90 に更新。

---

## データ契約(最終形)

```js
state.zeroThinking.groups = [{ id: uuid, title: string, order: number, createdAt: "YYYY-MM-DDTHH:mm:ss" }]
state.zeroThinking.themes[].groupId = uuid | null   // null = 未分類
```

- `groups` はテーマとは独立した配列。既存の `themes` / `entries` とは別に持つ(WBSの
  Project/Taskのような「別エンティティを関連付ける」設計に合わせた。プロジェクト自体を
  テーマ配列内にネストする設計にはしていない)。
- `normalizeState`(app.js)に後方互換補完を追加:
  - `zeroThinking.groups` が配列でなければ `[]` を補完(旧端末データでgroups自体が
    存在しなくても消えない・落ちない)。
  - `themes[].groupId` が無ければ `null` を補完(**既存値優先** — 既に `groupId` を
    持つテーマは上書きしない。展開順は他の後方互換補完(questionId/source)と同じ
    `"groupId" in t ? t : { ...t, groupId: null }` パターンに揃えた)。
- 新規テーマ作成箇所(手動追加・AI自動取り込み)では `groupId` を明示的にセットしていない
  (既存コードが `questionId`/`source` を明示しない箇所と同じ慣習)。`undefined` は
  グループ判定側で `null` と同様に「未分類」として扱われるため実害はない。これにより
  **AI自動取り込みテーマは自動的に未分類へ入る**(要件5)。

---

## UI構成

### 1. グループ表示(テーマ一覧)

`renderZtThemeTab`(app.js)を改修し、`ztThemeListHTML()` がグループの有無で表示を分岐する:

- **グループが1件も無い場合**: 従来どおりのフラット表示(`.zt-theme-item` が並ぶだけ)。
  既存ユーザーがこの機能に触れるまで見た目は一切変わらない。
- **グループが1件以上ある場合**: グループごとの見出し(`.zt-group`)配下に所属テーマをまとめ、
  末尾に「未分類」ゾーン(`.zt-group-unclassified`)を追加する。未分類を末尾に置いたのは、
  既存のWBS(単発Taskがプロジェクト選択の末尾に来る)や月間ボードの「未定」プールの配置
  慣習に合わせたため。
- グループ見出しには件数表示(`◯件`)を必ず出す(要件2)。

この分岐は `ztTab`(それ以外/★お気に入り)の現在表示中の一覧(`items`)に対して適用される
——お気に入りタブと通常タブは従来どおり独立したフィルタのまま、グループ階層はその内側に
畳み込む形にした(新しいタブは作っていない、要件5)。

### 2. 折りたたみ(要件2)

グループの開閉は「details確立パターン」の**開閉記憶機構そのもの**(v71 `isHomeFoldOpen`/
`setHomeFoldOpen`、localStorageキー `taskchute-journal-home-fold-v1`)を再利用した
(`ztGroupIsOpen`/`ztGroupToggleOpen`)。

ただし表示要素そのものはネイティブ `<details>/<summary>` ではなく、`div` + ボタンで
自前実装した。理由: グループ見出しにはリネーム(タイトルタップ)と削除ボタンを同居させる
必要があるが、`<summary>` 内にボタン等の子要素を置くと、そのクリックが `<summary>` の
デフォルト動作(親 `<details>` のトグル)にバブリングして意図せず開閉してしまう
(この挙動を止めるにはクリックごとの `preventDefault` 制御が要る)。既存コードには
`<summary>` 内にインタラクティブな子要素を置く前例が無く、iOS Safariでの動作保証が
取れないため、今回は自前の開閉トグル(`data-action="zt-group-toggle"`)+永続化は
既存関数の再利用、という組み合わせにした。開閉状態の記憶という「確立パターン」の実体
(isHomeFoldOpen/setHomeFoldOpen)はそのまま流用しているため、初出のロジックを増やしていない。

### 3. グループの追加・リネーム・削除・テーマのグループ移動(要件3)

- **追加**: テーマ一覧見出し行に「+ 大テーマ」ボタン(`data-action="zt-group-add"`)。
  既存のカテゴリ管理(`addCategory`)と同じ軽量な `window.prompt()` 方式(新しいモーダルは
  作らない)。
- **リネーム**: グループ見出しのタイトル文字列をタップ(`data-action="zt-group-rename"`)。
  同じく `window.prompt()`(現在値を初期値として表示)。
- **削除**: グループ見出しの「削除」ボタン(`data-action="zt-group-delete"`)。
  `window.confirm()` で確認(配下件数がある場合はメッセージに明記)。**削除してもテーマは
  消えず、`groupId` を `null` に書き戻して未分類へ実際に移動する**(要件どおり。
  既存の `deleteProject` のような論理削除+子の孤児化ではなく、明示的な付け替え)。
- **テーマのグループ移動**: 各テーマ行に `<select data-action="zt-theme-set-group">`
  (グループが1件以上ある時だけ表示)を常時同居させ、選ぶだけでその場から別グループへ
  移動する。v79月間ボードの「月選択」タップ代替と同じ設計判断(ドラッグ&ドロップは
  実装コストと誤操作リスクが高いため今回は作らない、というのがK提示の「編集UIかselect」の
  うちselect側を採用した理由)。`change` イベント経由で `ztThemeSetGroup()` を呼び、
  wish-set-month と同様に選択直後に `render()` して見た目を即座に反映する。

### 4. 既存機能の完全互換(要件4)

グループ機構は既存の `themes`/`entries` の構造やロジックに一切手を入れていない
(`ztAddSubmit`/`ztToggleFav`/`deleteZtTheme`/`openZtWrite`/`saveZtEntry` は無改修)。
テーマ追加(手動・AI自動取り込み)・削除+不採用記録(v86)・0秒思考の実施フロー
(テーマ選択→書く→保存)は、グループの有無・所属に関わらず従来どおり動作する
(tests/v90.test.js [4]で回帰確認)。

---

## テスト(`tests/v90.test.js`、新設)

① グループ作成・テーマ所属表示(グループ0件時はフラット表示のまま/追加後は見出し+件数+
  テーマのgroupId更新) ② グループ折りたたみがリロードを跨いで記憶される ③ グループ削除で
  配下テーマが消えず未分類(groupId:null)へ戻り、見出し自体も消える ④ 未分類テーマの従来動作
  (お気に入り・0秒思考の実施フロー・ワンタップ削除がグループ機構と無関係に動く)
  ⑤ AI自動取り込み(source:"ai-feedback")テーマがグループ存在下でも未分類ゾーンに入る
  ⑥ normalizeStateの後方互換(groups キー自体が無い旧データへの[]補完、
  themes[].groupId 欠損への null 補完、既存groupIdを持つテーマは上書きされないこと)

全量 `npm test`(38スイート、v90含む)フォアグラウンド実行で ALL PASS(exit code 0)を確認済み。

---

## 未対応・懸念点

- ドラッグ&ドロップによるテーマの並び替え・グループ間移動は作っていない(select方式のみ。
  v79月間ボードと同じ判断)。
- グループ自体の並び替えUI(ドラッグでorder変更等)は作っていない。`order` フィールドは
  持たせたが、現状は作成順(0,1,2...)で並ぶのみ。並び替えニーズが出たら次回検討。
- グループの階層は1段のみ(大テーマ→小テーマ)。WBSのようなタスクの多段ネスト
  (親子孫)には対応していない(K指示が「大テーマ、小テーマ」の2階層だったため)。
