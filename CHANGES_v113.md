# CHANGES v113

## 概要

K依頼(2026-07-16)。自宅PCのloop側で新設した `loop/english-phrases.sh` が毎日
`personal-data/taskchute/英語表現集_YYYY-MM-DD.md` を生成・push するようになったため
(初回pushは配布版v113着手時点で既にpush済み)、v92のAIレポートビューア(その他 > AIレポート)
から本文をアプリ内で読めるようにした。英語ジャーナルのAIフィードバック「💬 使える表現」から
日次で自動統合された表現集を、Kがアプリでまとめて振り返れるようにするのが目的。
SW `CACHE_NAME` を v112 → v113 に更新。

## 変更内容

### app.js

- `AI_REPORT_TYPES`(v92導入の配列。種類タブ・一覧取得・履歴セレクタ・本文表示は既にこの配列
  駆動の完全汎用実装だったため、v110のバッチ実行サマリ追加時と同様、新規UIロジックの追加は
  不要だった)に6件目のエントリを追加:
  `{ id: "english", label: "英語表現集", prefix: "英語表現集_", guide: "..." }`。
  ファイル名の日付サフィックス方式(`英語表現集_YYYY-MM-DD.md`)は既存5種別と同じ命名規約に
  沿っており、一覧取得(`aiReportFilesForType`)・本文取得(`triggerAiReportBodyLoad`)・0件時の
  フェイルソフト(`renderAiReportBody`)はいずれも既存コードのまま新種別に対応した。
- ファイル冒頭の見出しコメント(AIレポートビューアの種別一覧)と`normalizeState`のコメント
  (v92のaiReportType説明)に「英語表現集」を追記(実装ロジック自体に変更なし、ドキュメント
  追従のみ)。

### sw.js

- `CACHE_NAME`を`taskchute-journal-pwa-v113`に更新。

## 検証

- `node --check app.js` / `node --check sw.js` / `node --check tests/v113.test.js` すべて exit 0。
- `tests/v113.test.js`(新規、10チェック)ALL PASS: (1) 種類タブに「英語表現集」が並び選択で
  一覧取得(Contents API)が1回飛ぶ・履歴が新しい順に並ぶ・既定選択(最新)の本文表示、
  (2) セレクタで別日付選択→該当ファイルGET・本文切替、(3) 他タブ往復後もセレクタ表示のまま、
  (4) taskchute/直下が0件(初回状態)の別コンテキストで例外なく「まだ生成されていません」の
  フェイルソフト案内が出る。
- `npm run test:core`(直近5本=v109〜v113 + 固定コア5本、計10本)ALL PASS。

## 未対応・懸念点

- review.mdの2026-07-14全体レビュー指摘(未対応 `- [ ]` 項目群)はいずれも本バージョンの
  スコープ外の既存設計課題であり、種別追加1件だけの本変更で新たに悪化させるものではないため
  対応を見送った(v110と同じ判断)。特に「AIレポート履歴はtaskchute/直下をContents API 1回で
  一覧取得するが公式上限1000件」の指摘は種別が増えるほど影響が広がるため、次回以降の
  対応優先度として引き続き留意する。
- 本コミットはローカルcommitのみでpushしていない(指示どおり。監督者レビュー後にpush予定)。
