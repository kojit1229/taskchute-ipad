# CHANGES v61

## 概要

ROADMAP(`../taskchute-notes/ROADMAP.md`)フェーズ1(v61: 繰り越しの無自覚化と継続の
自己否定を止める)を実装した。既存の繰越機構(`carryOverBlock`/v46)・朝プラン下書き機構
(`confirmScheduleDraft`/v59)・`journalMeta`(v42)への軽量な上乗せのみで完結しており、
アプリ内AI呼び出しは追加していない(v60方針を維持)。

- **提案1: マイグレーション儀式** — Blockの繰り越し回数(`carryCount`)を記録し、2回目以降は
  「↻N」バッジで視覚化、3回目の繰り越しでは即座に繰り越さず一呼吸置く確認モーダルを挟む。
- **提案8: 今日の理想ワンライナー + 3日リトライ** — ホームに「今日の理想を一行で」の任意入力欄
  を追加し、書いた日から3日間ホームに小さく残す。3日目には達成/未達を問わず「続けるか手放すか」
  だけを一言で問う。日報生成にも反映する。

## 変更内容(app.js)

### 1. マイグレーション儀式(提案1)

- **`carryCount`フィールド**: `makeBlock` / `saveBlockFromModal` / `makeRecurrenceInstance`
  に追加(既定0)。`normalizeState` の Block 正規化(`{ isMIT:false, source:"", estimateMin:null,
  carryCount:0, ...block }`)で既存データにも既存値優先でデフォルト補完する。
- **繰り越し経路の両方でインクリメント**:
  - `carryOverBlock(id, { forceMIT })`(繰越パネルの「→ 今日へ」、既存 v46 の拡張): 新Blockの
    `carryCount = src.carryCount + 1`。
  - `confirmScheduleDraft()`(朝プラン確定、既存 v59 の拡張): `carryFromId` を持つ下書き項目を
    確定する際、元Blockの `carryCount` を引き継いで `+1` した値を新Blockに設定。
  - 両経路とも既存の `migratedTo` セマンティクス(二重繰越防止)は変更していない。
- **視覚マーク**: `migrationBadgeHTML(count)` ヘルパーが `carryCount >= 2` のときだけ
  「↻N」バッジを返す(派手にしない、CSSは控えめな pill 表示)。タイムラインカード
  (`renderTimelineCard`)・繰越パネル(`carryOverPanel`)・朝プラン下書き
  (`renderDraftLayer`。こちらは確定時に何回目になるかの**予告**として `carryCount+1` を表示)
  の3箇所に適用。
- **3回目の儀式モーダル**: `carryOverBlock` は `requestCarryOver(id)` を経由するようにし、
  次の繰り越しで `carryCount >= 3` になる場合は `openMigrationRitual()` で確認モーダルを
  先に出す。`confirmScheduleDraft()` も一括確定の前に対象項目をスキャンし、該当があれば
  同じモーダルを出して一括確定を保留する(選択解決後に自動で再開)。
  - 選択肢と効果:
    - **今日やる(MIT候補に)**: 通常通り繰り越した上で `isMIT = true`(既存の最大3個ルールは
      尊重し、超過時はMIT化のみ見送る)。
    - **分解する(タイトル編集へ)**: 繰り越しは実行せず、既存の `openBlockEditor()` で
      元Blockのタイトル編集モーダルを開く(朝プラン下書きの場合は当該項目のみ下書きから除外)。
    - **手放す(Wishへ移動 or 削除)**: `window.confirm` で選ばせ、Wishへ移動する場合は
      Wish Project 配下に同名Taskを作成、しない場合は元Blockを論理削除(`deleted:true`)。
    - **それでも繰り越す**: 通常の繰り越しと同じ(MIT化なし)。
  - 選択結果は `state.migrationRitualLog`(`aiScheduleHistory` と同じ思想の軽量ログ。
    `{blockId, title, carryCount, choice, at}`、上限300件)に記録する(将来のバッチ分析用)。

### 2. 今日の理想ワンライナー + 3日リトライ(提案8)

- **保存先**: `state.journalMeta[date].ideal`(既存の `journalMeta` を再利用。`normalizeState`
  で既存値優先の後方互換補完を追加)。
- **入力欄**: ホームの「いま、これ」の上に `homeIdeal()` を追加。今日、直近3日以内に理想が
  未設定の場合のみ、控えめな1行テキスト入力(`font-size:16px` 明示、iOS自動ズーム対策)を表示。
  `input` イベントで `saveState()` のみ(全再描画しないため入力中のフォーカスは維持される。
  既存の `data-journal-date` と同じパターン)。
- **3日間表示**: `idealActiveEntry(today)` が今日→昨日→一昨日の順に直近で `ideal` が
  書かれた日を探し、`dayNum`(1〜3)を返す。見つかった場合はホームに小さく
  「今日の理想(N日目): テキスト」を表示。未入力(3日とも空)なら入力欄以外は何も出さない
  (「邪魔しない」)。
- **3日目のリトライ**: `dayNum === 3` のときだけ「続ける/手放す」の2択を表示。
  - **続ける**: 今日の `journalMeta[today].ideal` に同じテキストをコピーし、今日を起点とした
    新しい3日間サイクルを開始する。
  - **手放す**: 元(2日前)の `ideal` を空にして表示窓を閉じる(否定ではなく区切りとして)。
- **日報生成(`generateReport`)への反映**: 冒頭に `> 🌱 今日の理想: {text}`(未入力日は行ごと
  省略)、「## 9. 明日への接続」に「理想は明日・明後日もホームに小さく残る。達成/未達は問わず
  3日目に続ける/手放すだけを選ぶ」という一文を追加(達成/未達の自己申告を求める文言は入れて
  いない)。

### 3. その他

- `sw.js` の `CACHE_NAME` を `v60` → `v61` に更新。
- `closeModal()` に `_migrationRitualCtx = null` を追加(選択せずに×で閉じた場合も
  一時状態を残さない)。

## 実装判断(仕様から補った/外れた点)

1. **儀式モーダルの発火単位は「Block単位」**: 朝プラン確定(`confirmScheduleDraft`)は本来
   複数項目の一括確定だが、3回目に該当する項目が1件でもあれば一括確定全体を保留し、
   その項目についてだけモーダルを出す設計にした。「今日やる」「それでも繰り越す」を選ぶと
   当該項目に解決済みフラグ(`_ritualResolved`)を立てて `confirmScheduleDraft()` を
   再帰的に呼び直し、残りの項目も含めて確定を完了する。「分解する」「手放す」を選ぶと
   その項目だけ下書きから除外し、残りは下書きに残したままにする(ユーザーが改めて
   「確定して登録」を押す)。全項目を一括で儀式にかける設計は複雑になりすぎるため見送った。
2. **朝プラン下書きのバッジは「予告」表示**: タイムライン・繰越パネルのバッジは
   Blockが**既に**繰り越された回数(`carryCount`)を示すのに対し、下書きはまだ実Block化
   されていないため、確定した場合に**何回目になるか**(`carryCount + 1`)を予告として
   表示する。両者で数字の意味が異なる点は自信がない箇所として後述。
3. **「今日の理想」の入力トリガー**: 仕様は「朝プラン確定時(または当日最初の日次オープン時)」
   だったが、専用モーダルや自動起動ロジックは追加せず、ホーム(既定の着地画面)に
   「未設定なら常に表示」という形にした。日次オープン時に自動でホームへ遷移する処理は
   既存にないため、この実装で実質的に「その日最初にホームを開いたとき」に入力機会がある。
4. **「手放す」の実装**: 「Wishへ移動 or 削除」という記述を `window.confirm` の2択
   (OK=Wishへ移動 / キャンセル=削除)で実装した。既存の `deleteCategory` 等と同じ
   確認ダイアログのパターンを踏襲。

## レビュー対応(2026-07-09、push前)

コーディネーターのv61レビューで指摘された3点に対応した。

1. **全量テストの時刻依存フレーキーを根治**: 深夜23:00境界付近に `npm test` を実行すると
   `v50.test.js`(1件)・`v59.test.js`(1件)・`v60.test.js`(1件)が失敗することが判明した。
   原因は、朝プラン/下書きスケジュールが内部で使う `computeFreeGaps`(「現在時刻〜23:00」を
   空き枠として扱う)に対し、これら3スイートが実時刻(`new Date()`)ベースで期待値を計算・
   前提を組み立てていたため、実行時刻が23:00に近いと空き枠が消失し想定と食い違うこと。
   「日中を待って実行する」運用ではなく、**テストを決定論化**する方針で対応した:
   Playwright の Clock API(`page.clock.setFixedTime()`)で「ページ内から見える現在時刻」を
   実行日の10:00に固定し(`page.goto()` 前に設定、`page.reload()` を挟んでも維持されることを
   確認済み)、アプリ本体のロジック(`computeFreeGaps`/`runAiMorningPlan`等)は無改修のまま、
   実行時刻に関係なく同じ結果になるようにした。あわせてテストのNode側コードが独自に
   `new Date()` で「今」を取り直していた箇所(v50: `nowMin`/`nowFloor`/`occupiedUntil`、
   v59: シナリオ[2]の `now2`)も、固定した同じ `Date` オブジェクトを再利用する形に統一した
   (ページ側と食い違う「今」を持たないように)。同種の依存を持つ `tests/v61.test.js`
   ([9][10]で🌅朝プランを実行する)にも同じ固定を先んじて適用した。
2. **`resolveMigrationRitual` の「分解する」分岐で画面が更新されない不具合を修正**:
   朝プラン下書きから対象項目を除外したあと `saveState()` のみを呼んでいたため、
   `openBlockEditor()` が開くのはモーダル部分だけで、背後のタイムライン(下書きレイヤ)は
   古い状態のまま表示され続けていた。既存の `saveAndRender` 慣習に合わせ、モーダルを開く前に
   `render()` を挟むよう修正した(`app.js` 内 `resolveMigrationRitual` の `decompose` 分岐)。
3. **`moveBlockToWish` のトースト文言を実態に合わせた**: Wishプロジェクトが存在しない場合
   (`normalizeState` が必ず1つ存在を保証するため通常は起きないが、念のための防御分岐)、
   Blockは削除されるだけでWishへは移動できていないのに、トーストは常に「手放しました」と
   表示していた。`moveBlockToWish()` を「Wishタスクを作成できたか」を返す関数に変更し、
   `resolveMigrationRitual()` 側で結果に応じてトースト文言を
   「Wishへ移動しました」/「Blockを削除しました(Wishプロジェクトなし)」/
   「手放しました(削除)」(ユーザーが明示的にキャンセル=削除を選んだ場合)の3通りに
   出し分けるようにした。

## 変更ファイル

- `app.js`
- `styles.css`(`.migration-badge` / `.migration-ritual-choices` / `.home-ideal*`)
- `sw.js`(`CACHE_NAME`)
- `tests/v61.test.js`(新規)
- `tests/v50.test.js`(レビュー対応: `page.clock.setFixedTime()` で時刻固定)
- `tests/v59.test.js`(レビュー対応: 同上。シナリオ[2]の `now2` 再取得も `now0` 再利用に統一)
- `tests/v60.test.js`(レビュー対応: 同上)
- `CHANGES_v61.md`(本ファイル)
- `../taskchute-notes/handoff.md`(notes リポジトリ側、本エントリ追記)

## 実機テスト手順

1. `sw.js?nocache=YYYYMMDD` を開き、1行目が `taskchute-journal-pwa-v61` になっていることを確認。
2. タスクシュート画面で、昨日の未完了Blockが2回繰り越されている状態(`carryCount:2`。
   実機では2日連続で「→ 今日へ」を押すと自然に到達する)から3回目の「→ 今日へ」を押し、
   確認モーダルが出ることを確認。4つの選択肢をそれぞれ試し、「今日やる」でMIT一覧に
   追加されること・「分解する」でタイトル編集モーダルが開くこと・「手放す」でWishへ
   移動または削除されること・「それでも繰り越す」で通常通り繰り越されることを確認。
3. 繰越パネル・タイムライン・朝プラン下書き(🌅ボタン)のそれぞれで、2回以上繰り越された
   Blockに「↻N」バッジが控えめに表示されることを確認(派手な演出になっていないこと)。
4. ホーム画面で「今日の理想を一行で」欄に入力し、保存されること(リロード後も残ること)を
   確認。2〜3日おいて(または `journalMeta` を直接書き換えて)3日目に「続ける/手放す」の
   選択肢が出ることを確認し、両方の挙動を試す。
5. 日報を生成し、冒頭に「今日の理想」が出ること、「明日への接続」に3日リトライの説明文が
   入ること、達成/未達を問う文言が入っていないことを確認。
6. 既存の繰越・朝プラン・MIT・Wishの動作(v46/v59)が壊れていないことを一通り確認。

## 確認サイン

- [ ] 実機(iPad/iPhone)で上記手順を確認
- [ ] 確認者:
- [ ] 確認日:

## テスト

`node tests/v61.test.js` で新規スイート(16シナリオ・47チェック)ALL PASS を確認済み。

納品前の全量実行(`npm test` = `node tests/run-all.js`、v49・v50・v53〜v61 の全12スイート)で
当初、実行時刻が22時前後(23:00境界付近)だったため v50.test.js・v59.test.js・v60.test.js の
各1件が実時刻依存で失敗した。「日中を待って再実行する」運用ではなく、上記「レビュー対応」の
1で述べた通り Playwright Clock API によるテストの決定論化で根治し、**同じ22時台の実行時刻の
まま** `npm test` 全12スイート ALL PASS を再確認済み(以後、実行時刻に関係なく再現するはず)。
