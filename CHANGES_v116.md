# CHANGES v116

## 概要

ROADMAP「TOC由来の提案E: 1日バッファ+消化率メーター」(クリティカルチェーン法の個人適用)を実装。
各Blockの見積もりに個別の安全余裕を足すと学生症候群・パーキンソンの法則で消えるため、余裕を1日の
末尾の「バッファ」1つに集約し、個々のタスクの遅れではなく「バッファ残量」という1つの数字だけを見る
(「Block実行率で自分を裁かない」原則のUI版)。

途中でKから追加要件が入り、**計画過積載ガード**(バッファを積む余裕がない日の検出)も本バージョンに
含めた。「積む余裕なくタスクを詰め込んだらバッファの意味がない」ため、実行中の見える化(バッファ
メーター)とは別に、計画段階で1日を見積もりで埋め尽くしていないかを検出して知らせる(自動でタスクの
削除・移動・並べ替えはしない)。

SW `CACHE_NAME` を v115 → v116 に更新。

## 変更内容

### データモデル(normalizeState)

- `state.settings.dailyBufferMin`(既定60分)。バッファサイズはKが手で設定する固定値(自動計算は
  しない)。未設定・不正値(文字列混入等)のみ既定値を補う。明示的な0以下の値はそのまま尊重し、
  「バッファ未設定」のフェイルソフト表示に使う。
- `state.settings.dayCloseHours`(既定24=24:00/翌0時、K追加要件の計画過積載ガード専用)。
  0以下・非数は常に既定へ補正する(バッファ分数と異なり「未設定」概念を持たない)。

### バッファ残量の計算(`computeBufferRemaining`)

残量 = バッファサイズ − Σ(当日の完了Blockの 実績時間 − 見積時間)。集計対象は当日の通常Block
(`category !== "ルーティン"`、v114のprotection集計除外と同じ思想)かつ`completed:true`のみ。
見積(`estimateMin`。`resolveEstimateMin`のフォールバック値は使わない生の手入力値)か実績
(`actualStartAt`/`actualEndAt`)のどちらか一方でも欠けているBlockは集計から除外する。

### 色分け(`bufferMeterLevel`)

残40%以上=緑/40%未満=黄/0以下=赤の3段階。バッファ未設定は`unset`。

### ヘッダー表示(`bufferMeterHTML`、`renderHeader`配線)

`renderHeader()`(全16タブ共通の共有コンポーネント)にバッファメーターを組み込み、個別タブごとの
配線を増やさず1箇所で完結させた。表示は「今日」を表示中のときだけ(過去日・未来日を振り返る文脈には
出さない。v114の連続欠落バッジと同じく「今日固定」の情報として扱う)。既存のタイムライン表示・
個々のタスクの遅れ表示は一切変更していない(追加の1行のみで完結)。

### 計画過積載ガード(K追加要件、2026-07-16・`computeDailyOverload`)

当日の予定Block見積合計+バッファサイズが、その日最初の予定Block開始時刻〜締め時刻(既定24:00)の
枠に収まらない場合を「過積載」と判定する。見積合計は`resolveEstimateMin`(手入力優先→過去実績
中央値→30分既定)を使い、完了/未完了を問わず当日の通常Block全件が対象(計画時点の総荷重を見るため)。
過積載日はヘッダーのメーターが通常の緑/黄/赤とは別の第4状態(灰色)になり、「計画時点でバッファ未確保
(◯分不足)」+責めないトーンの案内文(「タスクを減らすか、見積もりを見直しませんか」)が出る。
**自動でタスクの削除・移動・並べ替えは一切しない**(検出して知らせるだけ)。既存の朝プラン・下書き
機構の挙動も変えていない。バッファ自体が未設定の日は判定しない。

### 設定タブUI

「1日バッファ」パネル(バッファサイズ・締め時刻の数値入力)を追加。`data-setting-dailybuffermin`/
`data-setting-dayclosehours`のchangeイベントで対応するsettingsフィールドを更新する。

### styles.css

`.buffer-meter`(green/yellow/red/unset/overloadの5状態)、`.buffer-overload-hint`(過積載時の
責めないトーンの案内文)。

## 検証

- `node --check app.js` / `node --check sw.js` / `node --check tests/v116.test.js` すべてexit 0。
- `tests/v116.test.js`(新規、21チェック)ALL PASS:
  (1) 残量計算(超過で減る/早終わりで戻る/見積・実績無し・未完了・ルーティンは除外)、
  (2) 色3段階の境界値(ちょうど40%=緑・ちょうど0%=赤、20%=黄)、
  (3) 過去日・未来日表示ではメーターが出ない(非表示。クラッシュしない)。今日に戻ると再表示、
  (4) バッファ未設定(0・負の値)のフェイルソフト表示、
  (5) 計画過積載ガード(過積載/収まる/ちょうど収まる境界/ルーティン除外)。
- `npm run test:core`(直近5件=v112〜v116+固定コア5件、計10スイート)ALL PASS
  (v112/v113/v114/v115/v116/v50/v59/v67/v70/v72、既存機能への回帰なし)。

## 未対応・懸念点

- 計画過積載ガードの「可処分枠」定義(最初の予定Block開始時刻〜締め時刻)は設計判断であり、
  Kの実運用(朝一で予定を入れない日、深夜まで予定が続く日等)でしっくりくるかは要観察。
  decisions.md参照。
- 締め時刻(dayCloseHours)は数値入力(時間・小数可)のみで、時刻ピッカー(`type="time"`)を
  使っていない(24:00がtype="time"の範囲外〈23:59まで〉のため)。UXとして分かりにくい可能性がある。
- 過積載判定は`resolveEstimateMin`のフォールバック(過去実績中央値→30分既定)を使うため、見積未入力の
  Blockが多い日は実態と乖離した見積合計になり得る(既存の見込み終了時刻機能と同じ制約を踏襲)。
- 本コミット群はローカルcommitのみでpushしていない(指示どおり。監督者レビュー後にpush予定)。
