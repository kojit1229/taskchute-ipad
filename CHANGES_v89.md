# CHANGES v89

## 概要

`taskchute-notes/ROADMAP.md` の「v93: ゼロ摩擦ルーティンチェック」を実番号v89として実装した。
狙いはルーティン記録の「アプリを開いて押す」依存をなくすこと——特に「フロー中は記録行為自体が
意識から抜ける」という K個人ログのパターンへの対処。強制はしない・自動で勝手にチェックしない、
の既存方針(v57以来の宣言→報告ループ等と同じ思想)をそのまま踏襲した。

## [A] まとめてワンタップ確定

`overdueUncheckedRoutines(blocks)`(app.js)を新設: カテゴリ「ルーティン」・未削除・未完了・
`plannedStartAt <= 現在時刻`のBlockを開始時刻順で返す決定論関数(副作用なし)。これを共通ソースに、
2箇所へ「✓ ここまで全部やった(N件を一括チェック)」ボタンを追加した:

- ルーティンタブ(`renderRoutine`): 表示モード(ルーティンのみ/ルーティン+予定)に関わらず、
  `allBlocks`から常にカテゴリ「ルーティン」だけを対象に判定する(主戦場のため)。
- ホームの「今日のルーティン」(`homeRoutine`。isToday引数を追加): zone2/zone2-degradedいずれの
  文脈でも表示される。

タップで呼ばれる`bulkCheckRoutinesUpToNow()`は、対象BlockだけをtoggleBlockと同じ副作用
(`taskId`連動のtask status更新、`actualEndAt`の補完)込みでまとめてcompleted化し、
「N件のルーティンをまとめて記録しました」の1回のトーストだけを出す(N件同時の完了演出は
視覚的にうるさく、責めない/派手すぎないトーンの方針に合わないため意図的に出さない)。

**個別解除**は既存の`toggle-block`(チェックボックス)でそのまま行える——一括ONの取り消しを
1件ずつのタップに委ねる設計は「強制しない」という既存方針の延長。

## [B] 時刻ベースの自動チェック提案バナー

`homeRoutineCheckBanner(blocks, isToday)`を新設。過ぎた未チェックのルーティンが1件以上あるときだけ
「N件のルーティン、やっていたら1タップで記録 →」を責めないトーンで表示し、タップでルーティンタブへ
遷移する(そこで一括確定できる)。v73縮退モードの日は`homeDegradedBanner`と排他表示にし
(`renderHome`側で`degraded ? homeDegradedBanner() : homeRoutineCheckBanner(...)`)、
縮退モードの日にまで「やっていないこと」を思い出させないようにした。

自動でチェックすることは一切せず、常にユーザーのワンタップ操作を経由する(tests/v89.test.jsの[④]で
「バナー表示・アプリを開いただけでは何もcompletedにならない」ことを確認済み)。

## [C] PC側からの記録合流の下準備(バッチ)

`loop/coach-daily.sh`に`get_hyperfocus_notified_unrecorded <date>`を追加。v92過集中ブレーカー
(`loop/hyperfocus-breaker.sh`)は通知するだけでアプリへの書き戻しをしないため、その通知履歴
(`memory/hyperfocus-notified.log`、`日付\tBlockID\tタイトル`)を対象日で抽出し、app-state.jsonの
blocksと突き合わせて「通知したが今も未記録(completed!=true)」なものだけを翌朝のコーチング入力に
合流させる(`get_declarations`の直後に新しい`----`セクションとして追加)。

**設計判断: アプリへの書き戻しはしない。** memory/hyperfocus-notified.logはPC側のローカルログで
あり、これをapp-state.json(taskchute-ipad/personal-data経由でK端末とも同期する共有データ)へ
書き戻す経路を作ると、①同期タイミングの競合(K端末側の変更と衝突しうる)②「PC側が知らないうちに
Blockを書き換える」という新しい種類の副作用、の2つの複雑さが増える。v60以来の設計原則
「アプリにAIの知恵を入れる唯一の経路はバッチ生成ファイル→GitHub→fetch」に倣い、今回も
「一方向の合流(バッチ→コーチング入力)」だけに留めた。将来的にアプリ側からも参照したくなれば、
別途「合流サマリーmdを生成してfetchする」経路(既存のAIフィードバックと同じ形)を検討する。

## テスト

`tests/v89.test.js`(新規):

- ① 一括チェックで現在時刻以前・未完了のルーティンのみONになる(未来・既完了・非ルーティン
  Blockは対象外。境界値=現在時刻ちょうども対象に含まれることを確認)。actualEndAtの補完も確認。
- ①b 一括チェック後は対象が0件になりボタン自体が消える。
- ② 一括ON後も個別のチェックボックスで1件だけ解除でき、他の一括ON分は道連れで消えない。
- ②b 解除後は再び「過ぎた未チェック」扱いになり、ボタンが(1件分として)復活する。
- ③ 提案バナーの表示条件: 過ぎた未チェックがあれば表示・タップでルーティンタブへ遷移/
  全部完了済みなら非表示/v73縮退モードの日は`cond-degraded-banner`のみでルーティン提案バナーは
  非表示(排他確認)。
- ④ バナー表示やアプリを開いただけでは何もcompletedにならない(自動チェックされない)。
- ⑤ normalizeStateの後方互換: 本バージョンは新規stateフィールドを追加していないため、
  `routineViewMode`欠落・`plannedStartAt`未設定のルーティンBlockを含む最小限の旧形状stateでも
  ホーム/ルーティンタブがpageerror無く描画され、提案バナー・一括確定ボタンが静かに対象外になる
  (例外を投げずフィルタで弾ける)ことを確認。

`npm test`(全量、フォアグラウンド実行)で全37スイート ALL PASS(exit code 0)を確認した。

## 自信がない箇所・懸念点

- バナーの表示先はホームのみにした(ルーティンタブ自体を開けば一括確定ボタンがすぐ見えるため、
  ルーティンタブ内には別途バナーを置いていない)。ROADMAPの「日中にアプリを開いたとき」という
  文言により忠実に沿うなら、アプリ起動直後の共通レイヤ(全タブ)に出す設計もありえたが、
  既存の`homeDegradedBanner`と同じ「ホームで気づく」パターンに揃えた——実運用でホームを
  経由しない導線(例: 通知から直接ルーティンタブへ)が主になるようなら再検討が必要。
- `bulkCheckRoutinesUpToNow`は完了演出(祝福エフェクト)を一切出さない設計にした。1件ずつの
  `toggle-block`では祝福が出る既存挙動との非対称(まとめて確定した方が地味に見える)が、
  意図どおりかは実運用で確認したい。
- 過集中ブレーカーの通知履歴とapp-state.jsonの突合は、Block IDでの一致に依存する。
  Block削除後に再作成した場合など、IDが変わるケースでは「Block自体が見当たらない」表記に
  フォールバックする(判定不可としてK自身の記憶に委ねる設計。誤って「記録された」と断定しない
  ことを優先した)。
