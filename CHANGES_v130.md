# v130 睡眠CSV取込の失敗メッセージを原因別に区別(空CSV検出メッセージ)

## 仕様解釈についての注記

依頼は「v130(空CSV検出メッセージ)」という短い括弧書きのみで、詳細spec本文は本セッションには
渡っていなかった。既存コード(`importSleepCsv`)と依頼名から次のように解釈して実装した:
**「CSVにデータ行が無い(空/ヘッダーのみ)」と「データ行はあるが起床時間が全件パース不能」を
区別せず同じ「睡眠データを読み取れませんでした」だったのを、原因ごとに別メッセージにする。**
解釈が違う場合は指摘してほしい。

## 背景

`importSleepCsv`は失敗時、原因を問わず一律「睡眠データを読み取れませんでした」(+スキップ数)
というメッセージを出していた。「CSVが空だった」のか「CSVはあるが表記が読めなかった」のかが
文言から分からず、K自身がAutoSleepの書き出し不備なのか取込ロジックの不備なのか切り分けられない
状態だった。

## 変更点(app.js)

- `importSleepCsv`冒頭、`parseSleepCsv`の戻り値`records`が`length === 0`(空ファイル or
  ヘッダー行のみ。`parseSleepCsv`は`lines.length < 2`で`[]`を返す)の場合、既存の起床時間
  パース処理に入る前に専用メッセージ「睡眠CSVにデータ行がありませんでした(空のファイル、
  またはヘッダー行のみの可能性があります)」を出して即returnする。
- 既存の「全行スキップ」分岐(`!count`、= records.length > 0だが全件パース失敗)は、この時点で
  records.length > 0が保証されるため、`skippedWakeValues.length`が常に真になる。メッセージを
  「起床時間を読み取れず全N行をスキップしました(表記形式をご確認ください)」に明確化した
  (原因が「起床時間の表記」であることを明示)。
- 部分成功(一部だけ読める)の経路は無変更(v120で検証済みの「取込日付範囲・スキップ数・
  今朝欠落警告」トースト)。

## 検証手順

1. `node --check app.js` / `node --check sw.js`
2. `node tests/run-all.js v105 v120 v130`
3. `node tests/run-core.js`(直近5件+固定横断コア5本、回帰確認)
