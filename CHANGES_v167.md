<!-- generated by scripts/release-record.js; edit releases/vNNN.json instead -->
# CHANGES v167: app.js分割 段階4-1(ダッシュボードの閲覧専用render抽出)

## 概要

app.js分割の段階4-1として、prep-stage4-dashboard.mdの推奨どおり最も自己完結度が高いダッシュボードタブ(v163新設)の閲覧専用12関数をsrc/features/dashboard.jsへ抽出した。escapeHTML/parseDate/personalDataReady等の汎用ヘルパーはまだsrc/**へ抽出されていないため、v166のstate live binding importに加え、src/sync/github.jsで確立済みのconfigureXxx(deps)依存注入パターンを踏襲した。Home「AIから」カードと共有するcachedFeedbackはsrc/state/feedback-cache.jsへ独立モジュール化した。ロジックは一切変更していない(移動+依存注入化のみ)。

## 変更ファイル

- app.js
- sw.js
- src/features/dashboard.js
- src/state/feedback-cache.js
- tests/dashboard-core.test.js
- tests/v163.test.js
- tests/code-index.test.js

## 変更内容

- src/features/dashboard.js(新規): isDashboardDate/dashboardWeekStart/computeDashboardMetrics/defaultDashboardDate/currentDashboardDate/setDashboardDate/shiftDashboardDate/dashboardRateHTML/dashboardTrendBarsHTML/renderDashboard/hydrateDashboardFeedback/requestDashboardFeedbackの12関数と、モジュール専用のdashboardSelectedDate/dashboardDateTouched/_dashboardFeedbackFetchStateを移動。stateはsrc/state/store.jsからlive binding importする(v166パターン)。renderHeader/escapeHTML/clamp/parseDate/addDays/dateToISO/localDateTimeToMs/todayISO/fmtMinShort/renderMarkdown/getCategoryColor/personalDataReady/fetchGitHubRawResult/renderDeferringForFocus/renderの15個はまだapp.js側に残る汎用ヘルパーのため、configureDashboard(deps)による依存注入で受け取る(src/sync/github.jsのconfigureGithubSync(deps)と同一パターン)。
- src/state/feedback-cache.js(新規): cachedFeedback(const object)のみをexportする独立モジュール。Home「AIから」カード(app.js残留)とsrc/features/dashboard.jsの両方がここからimportし、再代入せずプロパティ変更のみで共有する(store.jsのstateと同じ思想)。
- app.js: 冒頭にsrc/features/dashboard.js(configureDashboard/renderDashboard/setDashboardDate/shiftDashboardDate/currentDashboardDate/hydrateDashboardFeedback)とsrc/state/feedback-cache.js(cachedFeedback)のimportを追加。configureGithubSync呼び出しの直後にconfigureDashboard({...})を1回追加。旧cachedFeedback/dashboardSelectedDate/dashboardDateTouched/_dashboardFeedbackFetchStateのモジュール変数宣言と12関数本体を削除し、抽出先を示すコメントマーカーのみを残した。
- sw.js: CACHE_NAMEをv167へ更新し、APP_SHELLへ./src/features/dashboard.js・./src/state/feedback-cache.jsを追加した。
- tests/dashboard-core.test.js(新規): dynamic import方式でisDashboardDate/dashboardWeekStart/computeDashboardMetrics(state.settings.categoriesのデフォルト引数経路)/defaultDashboardDate/dashboardRateHTML/dashboardTrendBarsHTML/renderDashboard(スモーク)/hydrateDashboardFeedback(1セッション1回のfetch契約・キャッシュヒット・404/成功)の8グループ26assertionを固定した。v163.test.jsで既に固定済みの期待値とは異なる観点(isDashboardDate/dashboardWeekStartの単体挙動、state live binding経路等)に絞り、重複するassertionは作っていない。

## 変更意図

- prep-stage4-dashboard.md §9の推奨順(依存が薄く書き込みが無い機能を最初に着手)に従い、3機能(ダッシュボード/Wish/Avoid残り操作系等)のうちダッシュボードを段階4-1として抽出する。
- escapeHTML/clamp/parseDate/addDays/dateToISO/localDateTimeToMs/todayISO/fmtMinShort/renderMarkdown/getCategoryColor/personalDataReady/fetchGitHubRawResult/renderDeferringForFocus/renderがいずれもまだapp.js側の汎用ヘルパーであるため(設計書はsrc/core配下への抽出済みを前提としていたが実際は未抽出)、循環importを避けつつ挙動を変えずに抽出する。
- cachedFeedbackはHomeタブ「AIから」カードとダッシュボードの共有オブジェクトであるため、独立モジュールに切り出しどちらからも同じ参照を読み書きできるようにする(設計書§9 Must級)。

## 検証

- node --check app.js / sw.js / src/features/dashboard.js / src/state/feedback-cache.js / tests/dashboard-core.test.js → 全てexit 0
- node tests/dashboard-core.test.js → 全件成功(8グループ26assertion)
- npm run code:index:write / npm run test:manifest:write
- node scripts/release-gate.js releases/v167.json --suite=dashboard-core,sw-integration,v148 → PASS(v163はuncertainties記載のとおり別途対応保留)
- git diff --numstat で実行コード差分を実測して監督者へ報告する

## 既知の制約・保留

- src/配下にpackage.json(type:module)を置いていないため、Node実行時にMODULE_TYPELESS_PACKAGE_JSON警告が出る(v164から継続、動作には影響しない)。
- 残る2機能(renderStats=計器盤、renderWeekly/renderCycle等)の抽出は段階4-2以降で別途対応する。
