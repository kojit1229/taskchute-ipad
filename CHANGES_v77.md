# CHANGES v77

## 概要

1. 【不具合修正・原因特定】タイムライン下書きスケジュール(朝の一括プランニング/決定論配置エンジン)が、
   未来のタスクまで空き時間へ詰め込みすぎる問題を修正
2. 【不具合修正】下書きの配置ブロックの長さがタスクの見積時間(estimateMin)と一致していなかった問題を修正
3. 【機能追加】AIフィードバック(および朝プラン・0秒思考テーマ)が、開きっぱなしのPWAでも
   再起動なしに自動的に反映されるようにした(visibilitychange復帰時+30分毎の定期再fetch)
4. 【追加スコープ・機能追加】AIフィードバック_*.md本文内の「## 0秒思考テーマ」見出しから
   0秒思考テーマを抽出し、v75で実装済みのzeroSecThemes選定UI(AIプラン由来)へ統合した

SW `CACHE_NAME` を v76 → v77 に更新。

---

## 1. 【不具合修正】下書きスケジュールの詰め込み問題

### 1.1 原因調査(現物のコードで特定)

K指示のとおり、バッチ側(`AIプラン_*.json`)は既に現実的な提案を出しており(前日に修正済み)、
問題は**アプリ内の決定論配置エンジン**(`runAiMorningPlan` → `aiMorningPlanCandidates` /
`aiScheduleCandidates` → `fallbackMorningPlan`)にあった。具体的には以下2点。

**(a) 候補生成(`aiScheduleCandidates`)が dueDate(期限)を一切見ていない**

WBS未完了タスクの絞り込みは `status`(todo/doing)・Wish除外・当日Block化済み除外のみで、
期限が翌週・翌月であっても無条件で候補に入り、`wbsTaskCompare` で期限昇順ソートされて上位15件が
今日の下書きに混入していた。

**(b) `fallbackMorningPlan` が空き時間を安全余地なく全部埋めていた**

各候補の見積分数ぶん空き枠へ前詰めするだけで、「今日中に現実的に終えられる量」という上限の
概念が無かった。`items.slice(0, 15)` はあくまで表示件数の後付けトリムで、配置自体には歯止めが
無かった。

### 1.2 修正

**候補の絞り込み(`aiScheduleCandidates`)**

- `dueDate` が対象日より後(翌日以降)のタスクを候補から除外する条件
  `!t.dueDate || t.dueDate <= date` を追加。
- **期限なし(dueDate未設定)のタスクは候補に残した**。理由: `wbsTaskCompare` は
  期限なしを内部的に `"9999"` 扱いで最後尾ソートしており(既存実装のまま無改修)、
  期限付きタスクを圧迫しない filler として振る舞う。期限なしタスクを一律排除すると、
  期限管理をしていないタスク(バックログの多くがこれに該当しうる)が朝プランから一切
  出てこなくなり、既存の使い勝手を損なうと判断した。
- 期限が当日以前(期日超過・当日締切)のタスクは従来どおり候補に含める(除外対象は
  「翌日以降」のみ)。
- この関数は `runAiSchedule`(タイムラインの単発「📋 下書きスケジュール」ボタン)とも共有
  されているため、そちらの候補にも同じ絞り込みが及ぶ(未来タスクを詰め込まないという
  目的に対して一貫性があり、意図した副作用)。

**現実的な量(`fallbackMorningPlan`)— 安全枠+ブロック間バッファ**

指示にあった2案(①空き時間合計の60-70%上限、②件数上限+ブロック間バッファ)のうち、
**両方を組み合わせて採用**した(実装コストが小さく、①だけでは「小さいタスクを大量に隙間なく
詰め込む」ケースを防げないため)。

- `MORNING_PLAN_CAPACITY_RATIO = 0.65`(空き時間合計の65%を配置上限とする。60-70%目安の中央値)。
  超過する候補は配置せず `skipped`(理由: 「安全枠超過(空き時間を埋め過ぎない)」)に回す
  (切り詰めて配置はしない = 既存の「入り切らなければ配置しない」方針をそのまま踏襲)。
- `MORNING_PLAN_CAPACITY_MIN_FLOOR = 60`(分)。上記比率だけだと、1日の残り時間がもともと
  少ない日(例: 22:15まで埋まっていて残り45分)で既存の「入り切る分は素直に置く」挙動まで
  壊してしまう(既存テスト`tests/v59.test.js`[3]で実際に検出。45分の65%=29分では、
  30分の候補すら1件も置けなくなっていた)。そのため下限60分を設け、
  `capacityMin = max(60, floor(空き時間合計 * 0.65))` とした。空き時間が92分以上ある日だけ
  比率が実質的に効き、それ未満の日は従来どおり「入り切る分は置く」挙動になる。
- `MORNING_PLAN_BUFFER_MIN = 10`(分)。配置のたびに、消費した空き枠の先頭を
  「見積分数 + 10分」進める(旧実装は見積分数のみ進めていた)ことで、下書きブロックが
  隙間なく連続しないようにした。

### 1.3 ブロック長 = 見積時間(15分丸めの廃止)

旧実装は `clamp(Math.round((c.estimateMin || 30) / 15) * 15 || 30, 15, 240)` で見積分数を
15分刻みに丸めていた(例: 見積20分 → 15分、見積37分 → 30分)。これが「見込み時間と
スケジュールされた所要時間が一致していない」の原因。`resolveEstimateMin`(Block編集欄は
5分刻み入力、繰越の場合は過去実績の中央値)が返す値は15分の倍数とは限らないため、丸めると
表示(WBS/Block編集欄の見積分数)と下書きの長さが必ずズレる。

- `clamp(Math.round(c.estimateMin || 30), 15, 240)` に変更(15分丸めを廃止し、四捨五入のみ
  ―小数が来た場合の保険―で見積分数をそのまま使う)。見積なし(null)は既存どおり30分既定を維持。
  上下限(15分〜240分)は既存のまま維持(異常値のガードとして妥当なため据え置き)。
- ギャップに収まらない場合の挙動(「切り詰めて配置」ではなく「配置しない」)は元々
  `fallbackMorningPlan` がそうなっていた(`gapIdx === -1` なら `skipped` へ)ため、
  今回は変更していない(既存の望ましい挙動を維持しただけ)。

---

## 2. 【機能追加】AIフィードバック等の自動表示

### 2.1 現状把握

`hydrateStaticMarkdown()` はアプリ起動時(モジュール末尾の1回)と、手動リロードボタン
(`reloadStaticMarkdown`)からのみ呼ばれていた。PWAは開きっぱなしが常態のため、朝05:00に
`loop/coach-daily.sh` がフィードバックを生成・pushしても、ユーザーが手動更新するか
アプリを再起動するまで気づけなかった。

### 2.2 実装

- `maybeRefreshFeedback()` を新設。`personalDataReady` ゲート・多重発火防止
  (`_feedbackHydrateInFlight`)・最短間隔ガード(`FEEDBACK_REFRESH_MIN_GAP_MS` = 60秒。
  visibilitychange連打等の対策)を掛けたうえで `hydrateStaticMarkdown()` を呼ぶ。失敗
  (ネットワーク例外等)は `console.warn` するだけで握りつぶし、即時リトライはしない
  (次回のvisibilitychangeか30分後の定期実行に任せる)。
  なお `hydrateStaticMarkdown()` 自体が呼ぶ `fetchGitHubRawResult` は404/401/ネットワーク例外
  いずれも例外を投げず `{ ok:false }` を返す設計(既存実装)のため、この握りつぶしは
  二重の安全策(将来 `hydrateAiWorkResults` 等が例外を投げるようになっても壊れない)。
- `document.addEventListener("visibilitychange", ...)` の既存ハンドラ(pull同期・日次オープン・
  朝プラン自動下書き・自動アーカイブを呼んでいた)に `maybeRefreshFeedback()` の呼び出しを追加。
  フォアグラウンド復帰のたびに新着フィードバック等を再fetchする。
- `startTimerTicker()` の既存500msインターバル(壁時計モデルの再描画に使っている既存タイマー)に
  相乗りし、`Date.now() - _lastFeedbackHydrateAt >= FEEDBACK_REFRESH_INTERVAL_MS`(30分)を
  満たしたら `maybeRefreshFeedback()` を呼ぶ。新規の `setInterval` は増やしていない
  (指示の「既存のstartTimerTicker系に相乗り」に対応)。
- visibilitychange経路と定期経路は同じ `_lastFeedbackHydrateAt` を共有するため、どちらかが
  発火すれば以後30分は再度発火しない(重複fetch防止)。GitHub API rate limit(認証済み5000/h)
  には全く影響しない頻度。

---

## 3. 【追加スコープ】AIフィードバック内「## 0秒思考テーマ」見出しの抽出・統合

### 3.1 契約

バッチ側が `AIフィードバック_YYYY-MM-DD.md` に以下の新セクション(2〜3件)を出力する契約:

```
## 0秒思考テーマ
- [ ] テーマ(1行): 理由(1文)
```

### 3.2 実装方式

- `extractZeroSecThemesFromReport(reportText)` を新設。`extractMITCandidatesFromReport`
  (「## 明日への提案」抽出)と同じ頑健化パターン(見出し直後の空行スキップ、
  `- [ ] `/`- [x] ` チェックボックス除去、半角/全角コロン分割、コロン無し行は理由なし扱い)を
  踏襲した。見出し自体が無い(旧形式FB)場合は空配列を返す。
- `fetchZeroSecThemesFromFeedback(date)` を新設。`AIフィードバック_<date>.md` を軽量fetchして
  上記関数でパースする。`fetchZeroSecThemes`(v75、`AIプラン_*.json`のzeroSecThemes)と対になる
  独立経路(同じ考え方: 他の用途の検証失敗に引きずられず、この関心事だけを個別に取得する)。
- **表示先は現物確認のうえ、新規UIを作らずv75で実装済みの`_zeroSecThemeDraft`カード
  (タイムラインの下書きバー付近、`zeroSecThemeBarHTML()`)にそのまま統合**した。
  理由: テーマ提案という関心事自体がAIプラン由来かフィードバック由来かでユーザー体験を
  変える理由が無く(どちらも「0秒思考でやってみるテーマの提案」という同じ性質)、
  既存の「追加/見送り + zeroSecThemeLog採否記録」の型をそのまま流用するのがこのアプリの
  定石(SKILL.md記載の方針)に合致するため。カード見出しの「(AIプラン由来)」という
  出所表記は、統合後は不正確になるため削除した。
- `runAiMorningPlan()` 内で `fetchZeroSecThemes`(プラン由来)と
  `fetchZeroSecThemesFromFeedback`(FB由来)を`Promise.all`で並行取得し、
  **テーマ文字列(`theme`)が同じものは1件に統合**(プラン側を先に走査するため、重複時は
  プラン側の`reason`が残る。理由: プランは朝の一括生成物であり、フィードバックより先に
  「配置提案」という強い文脈を持つため優先が自然と判断)。両方とも取得失敗/該当セクション
  なし(=関数がnullを返す)の場合のみ、従来どおり `_zeroSecThemeDraft` を更新せず
  前回セッションの状態を保持する(v75の既存挙動を維持)。
- `state.zeroSecThemeLog`(採否ログ)・`decideZeroSecTheme()`(追加/見送りの処理)は無改修。
  FB由来のテーマもプラン由来と全く同じログ形式・同じUIフローで記録される。

---

## テスト

`tests/v77.test.js` を新設(24 → 25スイート)。既存スイートは `tests/v59.test.js` の
安全枠floor値の裏取りを含め、無改変でALL PASS(1.2で述べたとおり、修正の過程で
`tests/v59.test.js`[3]が一時的に落ちたため`MORNING_PLAN_CAPACITY_MIN_FLOOR`を導入して解消した。
テスト自体は書き換えていない)。

1. dueDateが翌日以降のタスクは朝プラン下書きの候補から除外される(当日期限・期限なしは
   引き続き候補に残る)
2. 空き時間を全部埋めない(安全枠超過分は「見送り」に回り、配置合計が空き時間合計の70%を
   超えない。連続する下書きブロック間に10分のバッファがあることも確認)
3. 配置ブロックの長さが見積時間(estimateMin)のまま(15分/30分に丸められない)。見積なしは
   既定30分を維持
4. visibilitychange(フォアグラウンド復帰)で前日フィードバックの新着が自動的に再fetchされ、
   アプリ再起動なしに表示される(route経由のfixture差し替え+`document.visibilityState`上書き
   +`visibilitychange`イベント発火で検証)
5. 既存draft機構(繰越→下書き→確定→migratedTo付与)の回帰
6. AIフィードバック_*.md内「## 0秒思考テーマ」見出しからの抽出・選定UIへの合流・採否記録
   (a: FB単独)・AIプラン由来との重複排除(b: 同一テーマはプラン側の理由が残る)・
   見出しが無い旧形式FBでもクラッシュしない後方互換(c)

`npm test`(全量、25スイート)でALL PASSを確認済み。
