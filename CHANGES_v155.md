# v155 ADHD支援「②今日の庭 S2」(月間ピクセル)

罰なしゲーミフィケーション機能(`workbench/out/2026-07-27-appidea-designs/11-habit-garden.md`
§①〜⑤、K承認済み)のS2。v153で蓄積を開始した`state.gardenLog`を、ルーティンタブ
(`renderRoutine()`)先頭の月間カレンダーとして可視化する。新しいstateフィールドは追加していない
(v153のgardenLog / `gardenStageRank()` / `--garden-pale/mid/deep`トークンをそのまま再利用)。

## 1. 設計書§④との差分(重要、監督者・K確認事項)

設計書§④は「達成順の累積方式」(モチーフ絵の1〜n番目のピクセルを`done>=1`だった日数ぶん
点灯させる。未達日は日付位置を持たず「穴」としてすら見せない方式)を本命とし、段階の濃淡
(lv1〜lv3)は「任意・MVP外」としていた。

一方、`taskchute-notes/decisions.md`「2026-07-27 ADHD支援2機能の事前確定事項(K回答)」で
Kが確定した点灯仕様は次の通り:

> 今日の庭のピクセル点灯: **段階表示**(完了1件=薄緑 / 50%以上=緑 / 全完了=濃緑。
> 0件の日は空白のまま=罰なし6ルール維持)

この「50%以上=緑」という段階概念は、日付位置を持たない累積方式には存在しない(点灯順は
達成順であって達成度ではないため)。**段階表示は実カレンダー(日付位置固定)を前提とする
仕様**であり、K確定事項を優先して累積方式・モチーフ絵は不採用にした。実装したのは:

- 月間カレンダー(週7列×週数行、当月の実日付に1セルずつ対応)
- 各セルはその日の`gardenLog`から`gardenStageRank()`(v153・再利用)で段階を判定し、
  1件以上かつ50%未満=lv1(薄緑) / 50%以上かつ未全完了=lv2(緑) / 全完了=lv3(濃緑)で着色
- 0件の日・`gardenLog`にエントリが無い日はどちらも**空白**(色を塗らない。バツ・警告色・
  グレーアウトによる強調もしない。当月の全在月日セルに同じ薄い罫線を引くだけで、未達日だけを
  名指しで囲うことはしない=罰なしルール②のピクセル版)

この裁定はK本人への直接確認を経ていない(監督者指示に基づく実装判断。v153の
「開花配色をS1にも適用」裁定と同じ扱い)。K確認後に累積方式(モチーフ絵)へ差し替える場合は
本ステップの`gardenPixelCalendarHTML()`をまるごと置き換える想定で、`gardenLog`側のデータ構造
(done/total)は変更不要。

design doc §④の任意項目のうち、gardenLogスナップショットが無い過去日(機能導入前)を
完了Block残存から`lv1`点灯させる「寛容フォールバック」は、design doc自身が「任意・MVP外」と
明記していたため今回は実装していない(未対応点として下記に記載)。

## 2. 実装(app.js)

- `gardenPixelRank(dateISO)`: `state.gardenLog[dateISO]`から段階ランクを返す。エントリなし /
  `total===0` / `done===0`はいずれも`-1`(空白)にまとめる。新しい完了率計算はせず、既存の
  `gardenStageRank()`(v153)を再利用
- `addMonthsKey(monthKey, delta)`: `"YYYY-MM"`同士の月加減算。`new Date(文字列)`を経由せず、
  `parseDate`/`addDays`と同じ「数値コンストラクタのみ」の流儀
- `gardenPixelCalendarHTML()`: ルーティンタブ先頭に置くカード本体。月初の曜日オフセット分だけ
  paddingセル(色なし、罫線もなし)を先頭に挟み、当月日数ぶんのセル(`data-date`属性付き、
  段階に応じて`lv1/2/3`クラス)を並べる。全日点灯時のみ「◯月の庭が咲きそろった 🌸」の
  1行を静的表示(比較・催促は出さない)
- 表示中の月(`_gardenPixelMonth`, `"YYYY-MM"`)はモジュールレベルの非永続変数
  (`_triageCurrentCardId`等と同じ扱い)。stateに乗せない=persist/sync対象を増やさない
- 月送り: `data-action="garden-pixel-month"`(`data-delta="-1"|"1"`)。クリックハンドラは
  `addMonthsKey`の結果が当月以前ならのみ反映する(未来月は常に空グリッドになるだけのため、
  UI(`▶`のdisabled)とロジックの両方でガードする二重の安全策)

## 3. スタイル(styles.css)

`.garden-pixel-*`一式を追加(既存`.stats-cal`の「CSS grid + spanセル」方式を踏襲しつつ、
週列×曜日行ではなく実カレンダー(曜日列×週行)として新規定義。混同を避けるためクラス名は
`.stats-cal`と別系統にした)。色は`--garden-pale/mid/deep`(v153・再measured)をそのまま使用。

**実機でのライト側判別性対策(依頼書の指示事項)**: v153のレビューコメントに記載の通り、
`--garden-pale`はライト側で単独コントラストが3:1未達という既知制約がある。ピクセルセルは
S1の芽アイコンよりさらに面積が小さいため、`.garden-pixel-cell.in-month`に`1px solid
var(--line)`の罫線を追加し、淡色セルでも境界が視認できるよう補助した(未達セルにも同じ罫線を
引くため、罫線の有無で達成/未達を区別することにはならない=罰なしルールとは無矛盾)。
**2系統レビュー対応(2026-07-28)追記**: 罫線だけでは薄緑↔緑の判別が不十分という実測結果
(Claude、L*差3.7)を受け、`--garden-pale`自体の値も調整した。詳細は下記7-4節参照。

## 4. sw.js

`CACHE_NAME`を`v154`→`v155`。

## 5. テスト(`tests/v155.test.js`)

- (A/B) 3段階(lv1/lv2/lv3)+空白の描画: 1件(25%)/2件(50%、境界値)/4件(全完了)/0件/
  エントリ無しの5パターンを検証
- (C) 月境界: paddingセル数=月初の曜日オフセット、in-monthセル数=当月日数、先頭/末尾セルの
  `data-date`が当月1日・当月末日と一致
- (D) `gardenLog`が空オブジェクトでもクラッシュせず、全セルが空白のまま描画される
- (E) 罰なし表現の不在: カード内テキストに「連続」「streak」「先月」「平均」「もったいない」
  「あと」「未達」「残り」のいずれも含まれない
- (F) 月送り: ◀/▶クリックで表示月label・グリッドが変わる。当月表示では▶が`disabled`。
  `disabled`属性を外して強制クリックしても、クリックハンドラ側のガードで当月より先へは
  進まないことも確認(UI属性だけに頼らない二重ガードの検証)

## 6. 既存テストへの影響確認

`renderRoutine()`先頭にカードを1枚追加したため、ルーティンタブへ遷移する既存スイート
(`tests/v114.test.js` `tests/v115.test.js` `tests/v89.test.js`。いずれも`currentView =
"routine"`を直接指定して遷移)を実走し、全PASSを確認した(新カードの挿入によるレイアウト崩れ・
要素カウントのズレは無し)。`tests/v153.test.js`(今日の庭S1、gardenLogの読み書き自体を
検証するスイート)も無改変で全PASSを確認済み(S2はgardenLogを読むだけで書き込み経路には
一切触れていないため、想定通り無影響)。

## 7. 2系統レビュー対応(2026-07-28、Claude+Codex)

### 7-1. セルサイズをキャップ(Codex指摘: iPad幅で1セル70〜105pxに巨大化)

`grid-template-columns: repeat(7, 1fr)` + `.garden-pixel-cell { aspect-ratio: 1/1 }` の組合せは、
パネル幅が広い実機(iPad縦横、760〜1020px)で1セルが70〜105pxまで膨張し、ルーティン一覧を
画面外へ押し下げていた。列を`1fr`ではなく固定px(`--garden-pixel-cell-size: 18px`、
`.garden-pixel-card`スコープで定義)にしてセルサイズの上限をキャップし、グリッド自体は
`justify-content: center`で中央寄せにした(パネル幅が余ってもセルは伸びず、グリッドの位置だけ
動く)。375px(iPhone)〜1020px(iPad横)まで4段階の実測で18×18pxを確認済み
(検証に使った一時スクリプトはコミット対象外、確認後に削除)。

### 7-2. 月跨ぎで前月表示が残るバグを修正(Codex指摘)

`_gardenPixelMonth`は初回描画時に一度だけ`todayISO().slice(0,7)`で初期化されるモジュール変数
だが、それ以降は保持され続けるため、PWAを閉じずに月をまたぐと(`renderRoutine()`は都度
呼ばれるだけで`_gardenPixelMonth`自体は書き換わらない)、月初に再訪しても前月表示が残り続けて
いた。新しいフラグ`_gardenPixelMonthNavigated`(月送り操作をしたら`true`)を追加し、
`gardenPixelCalendarHTML()`側で「月送り未操作 かつ 保持月が当月より過去」のときだけ当月へ
同期し直すようにした。月送り操作をした後は、その選択(過去月表示)を自動では戻さない
(通常のカレンダーアプリの慣習を踏襲)。app.js該当箇所のコメントも実挙動に合わせて更新した。

### 7-3. VoiceOver対応(Codex指摘)

各in-monthセルに`role="listitem"`+`aria-label`を付与し、`.garden-pixel-grid`自体に
`role="list"`を付けた(padding用セルは`aria-hidden="true"`で読み上げ対象から除外)。
`aria-label`は罰なしルール⑥(加点表現のみ)をそのまま踏襲する新関数
`gardenPixelDayAriaLabel(month, day, rank)`で生成: 全完了は「◯月◯日 全部できた」、
50%以上は「◯月◯日 半分以上できた」、1件以上は「◯月◯日 少しできた」、0件・記録なしの日は
「◯月◯日」(日付のみ、否定語なし)。曜日ヘッダー行(`.garden-pixel-weekdays`)は日別セルの
aria-labelと重複するため`aria-hidden="true"`にした。

### 7-4. ライト側--garden-paleの明度ランプ調整(Claude実測: 旧値のΔL*=3.7)

旧`--garden-pale`(`#6bbd8d`)は`--garden-mid`(`#2fb96d`)とのL*(CIE明度、node scriptで
WCAG相対輝度から算出)差がわずか3.7しかなく、18px角という小面積セルではライトテーマで薄緑/緑が
判別困難だった。単調に明度が下がるランプへ調整:

| トークン | 旧値 | 新値 | L* |
|---|---|---|---|
| `--garden-pale` | `#6bbd8d` (L*=70.5) | `#b7e4c8` | **86.9** |
| `--garden-mid` | `#2fb96d`(変更なし) | `#2fb96d` | 66.8 |
| `--garden-deep` | `#1b6b3f`(変更なし) | `#1b6b3f` | 39.8 |

- pale-mid間ΔL* = 20.0(旧3.7から拡大)、mid-deep間ΔL* = 27.1
- パネル(白)上のコントラスト: pale 1.41:1 / mid 2.54:1(変更なし) / deep 6.52:1(変更なし)。
  装飾用の塗り面(セル背景)であり本文テキストではないため、v147コメントと同じく4.5:1(AA
  テキスト基準)は要求しない。3段階同士の識別性(ΔL*)を優先した
- pale単体とmid/deep間の識別コントラスト: pale-mid 1.80:1 / mid-deep 2.57:1 / pale-deep 4.64:1
  (node script実測、算出式はWCAG相対輝度→CIE L*変換)

**S1の芽アイコン(`.home-garden-svg .g-stage1`)も同じ`--garden-pale`トークンを使うため
見え方を確認した**: アイコンは46×46pxのSVG(セルより大きい)で、`currentColor`経由の
fill/strokeとして使われる。淡い緑という印象自体は保たれており、むしろmid/deepとの区別が
はっきりする分、S1側の3段階識別性も(意図せず)改善された。ダーク側`--garden-pale`
(`#4a8f66`、v153で既に調整済み)は今回変更していない。

### 7-5. テストの拡充(page.evaluateによる内部変数注入は不採用と判明)

当初はレビュー指示どおり`page.evaluate`で`_gardenPixelMonth`を直接差し替える方式を試したが、
app.jsは非モジュールの通常scriptのため、`page.evaluate`内の関数式は別スコープで実行され、
トップレベルの`let`/`function`(`_gardenPixelMonth`・`render`等)を読み書きしようとすると
`ReferenceError`になることを実行して確認した(代入文だけは非strictモードの暗黙global生成で
エラーにならず「動いたように見えてしまう」罠があり、実際には実装本体には一切効いていなかった
——`render()`の呼び出しで初めてエラーが表面化して発覚)。かわりに`page.clock.setFixedTime()`
(`tests/v153.test.js` Part Dで確立済みの技法)でブラウザの「現在時刻」自体を固定し、実装の
内部変数に一切触れずに外部から検証する方式へ切り替えた:

- **(G) 月境界の実式検証**: 時計をうるう年2月(2028-02)/平年2月(2026-02)/30日の月(2026-04)へ
  固定して初回描画させ、in-monthセル数がそれぞれ29/28/30になることを確認(実装式の
  トートロジーではなく、うるう年・月末日数のズレを外部から検証)
- **(H) 年跨ぎ**: 時計を2027年1月10日へ固定→◀クリックで2026年12月(31日)へ遷移することを確認
- **(H2) 月跨ぎの回帰確認**: 時計を2026-07-31 23:00へ固定してルーティンタブを開く(7月表示を
  確認)→**reloadせず**時計だけ2026-08-01 00:30へ進め、タブを一旦ホームへ切り替えてから
  ルーティンタブへ戻る(=`renderRoutine()`の再実行、実際のPWA再訪と同じ経路)→8月(当月)へ
  自動同期されることを確認。reloadを使うとJS状態が作り直されてしまい「開きっぱなしのPWAが
  月をまたぐ」バグの再現にならないため、意図的にreloadを避けている
- **(I) VoiceOver**: グリッドの`role="list"`、in-monthセルの`role="listitem"`+当月日数ぶんの
  件数一致、rank別aria-labelの文言、0件セルに否定語が含まれないことを確認

### 7-6. sw.jsのv155コメントブロック追記・見出し文言の月非依存化

sw.js冒頭にv155の変更概要コメントを追記(履歴の欠落解消)。カード見出しは「今月の庭」→
「庭の記録」に変更した(過去月・未来月を表示しているときに「今月の庭」という見出しが実際の
表示内容と矛盾していたため)。

## 未対応・懸念点

- gardenLog導入前の過去日(完了Block残存からの`lv1`寛容フォールバック)は design doc自身が
  「任意・MVP外」と明記していたため未実装。将来必要になれば`gardenPixelRank()`に
  `blocksForDate(dateISO)`を使ったフォールバック分岐を足すだけで対応できる
- 設計書§④本命の「達成順の累積方式・モチーフ絵」は不採用(上記1節の裁定)。K確認前提の
  監督者判断であり、正式なK確認が必要
- 月送りは過去方向に上限を設けていない(`GARDEN_LOG_KEEP_DAYS`=400日より前へ戻っても
  空グリッドが出るだけでクラッシュはしないが、UI上の制限は無い)
