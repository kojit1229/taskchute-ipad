# CHANGES v110

## 概要

K依頼(2026-07-16)。自宅PCのloop側で新設した `loop/batch-summary.sh` が毎朝
`personal-data/taskchute/バッチ実行サマリ_YYYY-MM-DD.md` を生成・push するようになったため
(初回pushは翌朝)、v92のAIレポートビューア(その他 > AIレポート)から本文をアプリ内で読める
ようにした。「日報の依頼が実施されたか」「各バッチが成功/失敗したか」をKがアプリで確認できる
ようにするのが目的。SW `CACHE_NAME` を v109 → v110 に更新。

## 変更内容

### app.js

- `AI_REPORT_TYPES`(v92導入の配列。種類タブ・一覧取得・履歴セレクタ・本文表示は既にこの配列
  駆動の完全汎用実装だったため、新規UIロジックの追加は不要だった)に5件目のエントリを追加:
  `{ id: "batch", label: "バッチ実行サマリ", prefix: "バッチ実行サマリ_", guide: "..." }`。
  ファイル名の日付サフィックス方式(`バッチ実行サマリ_YYYY-MM-DD.md`)は既存の4種別
  (コンテンツ総括・自己分析・基盤ヘルス・週次レビュー)と同じ命名規約に沿っており、
  一覧取得(`aiReportFilesForType`)・本文取得(`triggerAiReportBodyLoad`)・0件時のフェイル
  ソフト(`renderAiReportBody`)はいずれも既存コードのまま新種別に対応した。
- `normalizeState`のコメント(v92のaiReportType説明)とファイル冒頭の見出しコメントに
  「バッチ実行サマリ」を追記(実装ロジック自体に変更なし、ドキュメント追従のみ)。

### sw.js

- `CACHE_NAME`を`taskchute-journal-pwa-v110`に更新。

## 検証

- `node --check app.js` / `node --check sw.js` / `node --check tests/v110.test.js` すべて exit 0。
- `tests/v110.test.js`(新規、11チェック)ALL PASS: (1) 種類タブに「バッチ実行サマリ」が並び
  選択で一覧取得(Contents API)が1回飛ぶ・履歴が新しい順に並ぶ・既定選択(最新)の本文表示、
  (2) セレクタで別日付選択→該当ファイルGET・本文切替、(3) 他タブ往復後もセレクタ表示のまま、
  (4) taskchute/直下が0件(バッチ未実行の初回状態)の別コンテキストで例外なく
  「まだ生成されていません」のフェイルソフト案内が出る。
- `npm run test:core`(直近5本=v106〜v110 + 固定コア5本、計10本)ALL PASS。
- スクリーンショット: AIレポート画面でバッチ実行サマリタブを選択し本文が表示された状態を
  scratchpadへ保存(v110/v111報告時にまとめて添付)。

## 未対応・懸念点

- review.mdの2026-07-14全体レビュー指摘「AIレポート履歴はtaskchute/直下をContents API
  1回で一覧取得するが公式上限1000件、蓄積で履歴欠落し得る」は本バージョンのスコープ外
  (既存の設計課題であり、バッチ実行サマリ追加によって新たに悪化させるものではないため
  対応を見送った)。
- 本コミットはローカルcommitのみでpushしていない(指示どおり。監督者レビュー後にpush予定)。
