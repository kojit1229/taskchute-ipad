<!-- generated by scripts/release-record.js; edit releases/vNNN.json instead -->
# CHANGES v253: 固定化習慣ストリークの計器盤表示

## 概要

最大3件の固定化ルーティンを静かな計器として表示し、保護ルーティンを実行率の分母から除外した。

## 変更ファイル

- app.js
- CHANGES_v253.md
- docs/code-index.generated.json
- docs/code-index.generated.md
- docs/test-impact-map.generated.md
- releases/v253.json
- src/core/habit-streak.js
- src/core/recurrence.js
- src/features/instruments.js
- styles.css
- sw.js
- tests/recurrence-core.test.js
- tests/suite-manifest.json
- tests/v253.test.js

## 変更内容

- EARLY BIRDの既存ストリークDOMを共通部品化し、固定化ルーティンごとに現在値・自己ベスト・累計・30日チャレンジ・直近28日ドットを表示した。
- weekdaysの土日とstreakSince前の日は四角いスキップ意匠にし、固定化1枠目を大きく、2・3枠目を控えめにした。解除済み・削除済みルールは表示対象から除外した。
- habitStreakStatsへ累計、streakSince起点の暦日challengeDay、適格日付きlast28を追加し、日付文字列パースなしで算出した。
- routineRateへrecurrencesを任意引数で渡し、protection=trueの有効ルールに属するBlockを分母・分子から除外した。既存の引数1個の呼び出し契約は維持した。
- Service WorkerのCACHE_NAMEをv253へ更新した(APP_SHELLの登録内容は変更なし)。

## 変更意図

- 自分で固定化したdaily/weekdaysルーティンだけを、書籍の『1つに絞れ』に沿って1枠目中心の静かな計器として確認できるようにする。
- 弾1のhabitStreaksと純関数を表示専用に利用し、新しいstate書き込み・通知・未達赤表示を増やさない。
- 保護系ルーティンは実行率で裁かないというv114の契約をroutineRateの分母にも反映する。

## 検証

- 変更前実測: protectionルールを渡してもroutineRateは1/2・50%となり、保護ルーティンを分母へ含めていた。修正後recurrence-coreでは1/1・100%をPASS。
- node --check app.js、src/core/habit-streak.js、src/core/recurrence.js、src/features/instruments.js、tests/v253.test.js: PASS。
- system Edge指定 tests/v253.test.js: daily完了2日、weekdays土日スキップ、未完了リセット+自己ベスト保持、0件/解除/削除非表示、30日超、既存計器、390pxを全件PASS。
- instruments-core、v247、instruments-e2e、v252、recurrence-core: 全件PASS。
- npm run code:index:write: PASS(564 functions)。npm run test:manifest:write: PASS(138 suites)。system Edge指定 release-gate --final: 302.7秒、exit 0。

## 既知の制約・保留

- iOS Safari実機PWAでのService Worker更新とオフライン表示確認は監督者側で必要。
- taskchute-notes/handoff.mdがワークスペース外でEPERMになる場合、notes側更新は監督者が再実行する。
