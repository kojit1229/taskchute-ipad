# CHANGES v91

## 概要

K指示(2026-07-14)「ジャーナルテンプレと日報フォーマットを、日次バッチが構造的に読める形に
改訂。例えば依頼事項は`### 依頼`の次の行に記載など」への対応。

事前調査の結論: 日報(`generateReport()`)の見出しは、コロン付きの唯一の例外
(`loop/scripts/journal-requests-extract.py` によるジャーナル欄内の依頼マーカー抽出)を除き、
バッチ側から**全文cat→LLM解釈**されるだけで、個別見出しを正規表現でパースするコードは存在
しない。よって既存の日報見出しは**全て維持**し、変更したのはジャーナルテンプレ内に
「### 依頼」節を新設した点のみ。突合表の全文は `loop/FORMAT_CONTRACT.md` を参照。

SW `CACHE_NAME` を v90 → v91 に更新。

---

## 変更した見出し / 維持した見出し

- **新設**: ジャーナル欄(`## 8. ジャーナル` の中身。`state.journals[date]`)のテンプレ末尾に
  「### 依頼」見出しを追加(`app.js` の `JOURNAL_REQUEST_SECTION` 定数)。
- **他の全ての見出しは維持**(`# 日報 ...` / `## 1. サマリ` 〜 `## 9. 明日への接続` /
  `## AIへの質問` / `## 🧠 0秒思考` / `## 8. ジャーナル` 見出し自体 / `## 📋 AI へのコピペ用
  プロンプト` など)。理由: いずれも正規表現パースの対象になっておらず、変更する必然性が
  なかったため(FORMAT_CONTRACT.md参照)。

---

## app.js の変更

1. **`JOURNAL_REQUEST_SECTION` 定数を追加**(ファイル先頭、`STORAGE_KEY` の直後)。
   ```js
   const JOURNAL_REQUEST_SECTION = [
     `### 依頼`,
     `(AIへの依頼はこの見出しの下に1行1件で書いてください。例:「相場帳のバグを直して」)`
   ].join("\n");
   ```
   **配置場所についての注意**: 監督者の実装指示では `defaultJournal()` の直前
   (11049行目付近)への配置が想定されていたが、実装前に `normalizeState(` の全呼び出し箇所を
   grepしたところ、`let state = loadState();`(ファイル冒頭57行目、モジュール読み込み時に
   即時実行)が `loadState()` → `normalizeState()` を起動直後に呼んでいることが判明した。
   `normalizeState()` 内で `JOURNAL_REQUEST_SECTION` を参照する以上、この `const` を
   `defaultJournal()` の直前(11049行目)に置くと、`state = loadState()` が実行される時点
   (57行目)ではまだ初期化されておらず、TDZ(Temporal Dead Zone)により
   `ReferenceError: Cannot access 'JOURNAL_REQUEST_SECTION' before initialization` で
   **起動不能になる**。これは `JOURNAL_PROMPTS` 未初期化事故(v38当時、143〜145行目の
   コメント参照)と同根の問題。そのため `JOURNAL_REQUEST_SECTION` の定義位置のみ
   ファイル先頭へ変更し、`defaultJournal()` 側では同じ定数を参照するだけにした
   (二重定義はしていない)。挙動・見出しテキスト自体は指示どおり。
2. **`defaultJournal(date)` の末尾に `JOURNAL_REQUEST_SECTION` を追加**(`## 📝 自由記述` の後)。
3. **`JOURNAL_PROMPTS` に「依頼」キーを追加**(ジャーナルタブの「💡 思考のヒント」アコーディオン
   表示用。Markdown出力には含まれない装飾)。
4. **`normalizeState(value)` に既存端末向けの追記型マイグレーションを追加**
   (`value.modal = null;` の直前)。`journalTemplate` を持つが「### 依頼」を含まない端末にのみ
   末尾追記する。既存カスタマイズ(自由記述欄の書き換え等)は壊さない。「### 依頼」を既に
   含む場合は追記しない(冪等)ことを `tests/v91.test.js` [4] で確認済み。

---

## loop/scripts/journal-requests-extract.py の変更

「### 依頼」等の見出し行(`#`が1〜6個+「依頼」+任意でコロンと同一行の続き)を最優先で検出する
`REQUEST_HEADING_RE` / `match_request_heading()` を追加し、メインループの最初の分岐にした。
見出し行の次の行から、次の空行/次の見出し行までを1つの依頼ブロックとして抽出する。

ガイド文(丸括弧だけの行)は `GUIDE_LINE_RE` で判定してブロック内容から除外する。監督者の
実装指示に示された正規表現 `r"^\s*[\((].*[\))]\s*$"` は文字クラス `[\((]` が半角`(`を
2回含む形になっており(全角`（）`にマッチしない)、コメントで謳われている「半角/全角どちらも
対象」という契約と食い違っていたため、`r"^\s*[\(（].*[\)）]\s*$"`(半角・全角それぞれの
開き括弧/閉じ括弧を1文字ずつ含む文字クラス)に修正して実装した。見出し形式のブロックは
見出し行からブロック終端までをまとめて読み飛ばすため、既存の「依頼」単独行/コロン形式との
二重検出は起きない。

**追加修正(監督者による独立レビューで発見、reviewer sub-agent指摘)**: 実装直後の見出しブロック
抽出ロジックは、legacy形式(「依頼:」「依頼」単独行)と同じく「次の空行で打ち切る」実装だった。
しかし「### 依頼」は日報末尾の独立節という設計のため、ガイド文の後に1行空けてから依頼を書く・
複数段落に分けて書く、といった自然な入力で依頼が無症状(エラー無し・count:0)のまま取りこぼされる
ことを手動テストで再現確認した(ケース: ガイド文の直下・空行を挟まず記入 → count:1で正常検出/
ガイド文の後に1行空けてから記入 → 修正前はcount:0で取りこぼし)。見出しブロックのみ「空行では
打ち切らず、次の見出しかファイル末尾まで読み進める」よう修正し、修正後は両ケースともcount:1で
正しく検出されることを確認した(legacy形式・ガイド文のみの日・既存の複数依頼マージ挙動には
影響なし)。

---

## テスト

### app.js: `tests/v91.test.js`(新設)
① `defaultJournal()` 経由の初期ジャーナルに「### 依頼」節・ガイド文が含まれる
② 「### 依頼」節込みのジャーナル本文が日報生成時に「## 8. ジャーナル」節へそのまま出力される
③ 既存契約「## 8. ジャーナル」見出し自体が壊れていない
④ `normalizeState` 後方互換: 既存journalTemplateへの追記(既存内容は保持・二重追記されない・
  再読込しても冪等)

`node tests/run-all.js v91` で個別実行しALL PASS確認済み。その後 `npm test` で全量実行
(結果は本ファイル末尾の検証結果参照)。

### loop/scripts/journal-requests-extract.py: 手動テスト(スクラッチパッドの合成データ)
1. 「### 依頼」見出し+ガイド文プレースホルダのみ(未編集) → `{"count": 0}` を確認
2. 「### 依頼」見出し+実際の依頼(改行区切りの継続行) → 正しく1ブロックとして抽出を確認
3. 既存の「依頼」単独行形式・「依頼:」コロン形式・非マーカー文中語(「依頼書を提出する」)の
   除外 → 従来どおり動作(後方互換の回帰確認)

---

## 検証結果

- `node --check app.js`: OK
- `python -m py_compile journal-requests-extract.py`: OK
- `bash loop/guardrails/verify.sh`(ClaudeCodeルート): 実行・結果は完了報告本文を参照
- `npm test`(全量): 実行・結果は完了報告本文を参照
- `node tests/run-all.js v91`: ALL PASS

---

## 未対応・懸念点

- `normalizeState` の journalTemplate 追記マイグレーションは、Kの実機データ(既存の
  `journalTemplate` が既にカスタマイズされている可能性)に対して**追記のみ・既存文字列の
  改変なし**という設計にしたため、理論上は安全と考えている。ただし実機での実地確認(実際に
  同期後 端末上でジャーナルタブを開いて崩れが無いか)はこのセッションでは行っていない
  (taskchute-ipad本体はcommit/push禁止のため、Kの承認後の反映時に確認を推奨)。
- 見出し名自体(「### 依頼」)は指示どおりだが、既に「依頼」という単語を含む長文の自由記述を
  書いた場合(見出しではなく地の文としての「依頼」)は今回のパーサでは検出されない
  (見出し形式でない限り対象外という設計どおりであり、意図的な制約)。
- **(監督者独立レビューで発見・修正済み)** 見出しブロック抽出は当初legacy形式と同じ
  「空行で打ち切る」実装だったため、ガイド文の後に1行空けてから依頼を書くと無症状で
  取りこぼす不具合があった。「### 依頼」節は日報末尾の独立節という前提に立ち、空行では
  打ち切らず次の見出し/EOFまで読み進めるよう修正し、再テストで解消を確認済み(上記
  journal-requests-extract.pyの節・検証結果参照)。
