<!-- generated by scripts/release-record.js; edit releases/vNNN.json instead -->
# CHANGES v185: 今日コックピット B4(P10〜P11)第3テーマ完成+全画面適用

## 概要

第3テーマcockpitの保存・起動・設定UIを6点セットで完成し、cockpit選択時だけ全18ビューの共通chromeを管制室調へ統一した。light/darkの既定値と共通スタイルは変更していない。

## 変更ファイル

- app.js
- index.html
- releases/v185.json
- src/features/timeline.js
- styles.css
- sw.js
- tests/today-core.test.js
- tests/v140.test.js
- tests/v75.test.js

## 変更内容

- v182で先行定義したcockpitトークンへpanel/grid/go/book/mono互換トークン、庭の3段階色、timeline now-line色を追加した。
- THEME_COLOR_BY_MODEへcockpitを追加し、resolveThemeとnormalizeStateを許可リスト方式へ更新した。
- 設定画面へ「コックピット」を追加し、index.htmlの同期スクリプトをapp.jsと同じ解決構造へ更新した。
- P11aとして共通パネルのコーナーブラケット、等幅数字、ボタン・badge・segmented・modal・navigationの枠線とアクセントを追加した。
- P11bとしてグリッド背景、homeのzone tint、timeline now-lineのcockpit色を追加した。
- homeの4 zone tintをcockpit配色へ写像し、timeline now-lineはlight/darkで従来色を維持するCSS変数フォールバックへ変更した。
- sw.jsのCACHE_NAMEをv185へ更新した。
- レビュー反映: cockpitの.btn.dangerに危険色(赤ソフト背景+赤文字)を追加(M1)。--monoスタック先頭にui-monospace/SFMono-Regular/Menloを追加しiOSでCourierへ落ちる問題を回避(M2)。
- 同梱修正: renderStatsの空データ案内をlegacyBody判定へ復元(v184回帰、CI v54/v143検出)。
- 同梱修正: tests/v75・v140のCIフレーク根本対応(静的ページ上でlocalStorage seed→goto方式。診断はworkbench/out/2026-07-29-today-cockpit-impl/ci-flaky-diagnosis.md)。

## 変更意図

- P10: cockpitをテーマ色、解決、state正規化、設定選択肢、起動時フラッシュ防止へ同時追加し、再読込時のdarkフォールバックを防ぐ。
- P11: 個別ビューのHTML/JSを増やさず、共通クラスへのcockpit限定CSSで全画面の意匠を統一する。
- 既定テーマとlight/darkの見た目を維持し、カテゴリchipのユーザー設定色も上書きしない。

## 検証

- node --check app.js / src/features/timeline.js / tests/today-core.test.js / tests/v75.test.js / tests/v140.test.js → 全てexit 0。
- node tests/run-all.js today → 35セクション([24]〜[29]のB4テーマ検証含む)ALL PASS。v143/v54/v75/v140 も個別再走でPASS(v140は4連続PASSで安定確認)。
- 実ブラウザ確認(監督者): cockpit適用(--bg #050a14)・today 7パネル描画・時計進行・ビュー遷移後テーマ維持・danger赤色・monoスタック先頭ui-monospaceをcomputed styleで確認。Codex実装時に19ビュー巡回済み。
- 2系統レビュー: reviewer(Claude)=条件付き合格(H1/H2/M1/M2を本版で反映。M3トークン二重定義とM5同期スクリプト突合テストは次バッチ送り)/ Codexレビュー=本記録コミット時点の結果を統合。
- push前最終ゲート: release-gate --final(test:core)PASS(本記録コミット時点)。

## 既知の制約・保留

- release-record.js --writeが生成するCHANGES_v185.mdとtaskchute-notes/handoff.mdは許可ファイル外のため監督者工程に残す。
- テストは本バッチでtoday-core([24]〜[29])とv75/v140(フレーク修正)を編集した(実装Codexはtests/不触、テストは別担当+監督者)。
- レビューM3: --cockpit-*系と非prefix系のトークン二重定義が残る(値は同一。片方だけ変えるとドリフト源)。次バッチで別名化予定。
- レビューM5: index.html同期スクリプトとapp.js側の静的突合テストは未追加(現状は本体完全一致を目視確認)。次バッチ候補。
- レビューM4: cockpit時のみ.panelがposition:relativeになる(絶対配置子孫の基準変化の可能性)。tasks/wbs/settingsの目視で異常なしを確認済み。
- 作業ツリーに同居していた別作業(scripts/impact-regression.js等のテスト影響選定基盤)はv185に含めない(別セッションの成果物。コミット対象から除外)。
