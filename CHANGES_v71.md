# CHANGES v71

## 概要

機能追加を重ねて最適化されていなかったUI(タブ順・ホームの情報密度・散らばったAI表示)を整理した。
**破壊的な作り直しはしていない**: タブの削除・機能の削除・データ構造の変更・配色/フォントの刷新は
一切行っていない。並び替えと折りたたみ(既存の`<details>`流儀を踏襲)による整理のみ。

以下、Kが違和感を感じたら個別に戻せるよう、変更点を「変更前→変更後」で列挙する。

---

## 1. タブ順の並び替え(app.js: navItems)

サイドバー(デスクトップ)と「その他」メニュー(モバイルの`renderMore()`、navItemsをそのまま
フィルタして使っている)の並びを、朝型・MIT駆動・実行重視というKの実データ裏付け型の傾向
(`workbench/out/2026-07-09-personal/effective-todo-and-thinking.md`の「現在地」節参照)に合わせて
実行系優先へ並び替えた。

**変更前(v33〜v70の順序)**:
ホーム / ジャーナル / 0秒思考 / ビジョン / タスクシュート / WBS / タイムライン /
ルーティン / ポモドーロ / やりたい / やらない / 日報 / 週次 / 計器盤 / 設定

**変更後(v71)**:
ホーム / タスクシュート / タイムライン / WBS / ルーティン / ジャーナル / 週次 / 日報 /
計器盤 / やりたい / やらない / ビジョン / 0秒思考 / ポモドーロ / 設定

**根拠**:
- 実行系(ホーム・タスクシュート・タイムライン・WBS・ルーティン)を先頭に集約。1日に何度も
  参照するタブほど上に来るようにした。
- 日次1回系(ジャーナル・週次・日報)を実行系の直後に。
- 参照系(計器盤・やりたい・やらない・ビジョン)はその後ろへ。
- **ポモドーロは最後尾の1つ手前(設定の直前)へ降格**した。v70でBlock開始時に自動起動する
  ようになったため、独立タブとして毎回タップして開く必要性が下がったことを反映した
  (指示どおり「統合はしない・並び替えとアクセス手段の整理まで」に留めている。タブ自体・
  機能自体は無変更)。
- 0秒思考も後方へ移動。`effective-todo-and-thinking.md`のデータ(0秒思考の実行率は直近8日で
  0/7)から、日次で強く前提とすべきタブではないと判断した。ただしタブの削除・頻度変更(データ)
  は行っていない(UI上の並びだけ)。

**変更していないもの**: `mobileNav`(iPhone/iPadの下部4タブ: ホーム/WBS/実行/時間/その他)は
**意図的に変更していない**。理由: この4タブはKが最も高頻度で親指操作する「筋肉記憶」の対象であり、
並び替えによる実利(タスクシュートとWBSの順序整合)よりも、慣れた位置が変わることによる操作ミス・
迷いのリスクの方が大きいと判断した(指示の「リスクを最小に」を優先)。もし変更を希望する場合は
`app.js`冒頭の`mobileNav`配列を編集すれば良い(1行、影響範囲は下部ナビのみ)。

---

## 2. ホーム(コックピット)の情報整理(app.js: renderHome ほか)

### 2-1. 最上部(常時表示・折りたたみ無し)の並び

**変更前**: ヘッダー → 日付バー → 鮮度インジケータ(単独) → 信条 → 寿命カウントダウン →
今日の理想 → AIが処理した作業(単独) → いま、これ(Now) → スコアボード →
「今日、すすめる」ゾーン(MIT+タスクシュート) → …

**変更後**: ヘッダー → 日付バー → 今日の理想 → **いま、これ(Now)** → **今日の主役(MIT)** →
**「AIから」集約カード** → スコアボード → 「今日、すすめる」ゾーン(タスクシュートのみ) → …

- 「いま、これ」(Now)と「今日の主役」(MIT)を最上部の独立要素に引き上げた(従来はMITが
  「今日、すすめる」ゾーンの中でタスクシュートと2カラム表示され、スクロールしないと全体が
  見えなかった)。
- MITを引き上げたことに伴い、ホームのスコアボード「今日の主役」セルのジャンプ先を
  `homezone-1`から新設した`#home-mit-anchor`(MITを囲うdiv)に変更した(`homeScoreboard`)。
- 「今日、すすめる」ゾーンはタスクシュートのみの単カラムになった(`home-grid single`)。
  「今日のリズム」ゾーン(ながれ/ルーティン)は変更なし(2カラムのまま、実行系ゾーンとして
  引き続き常時展開)。

### 2-2. 参照系セクションの折りたたみ化(既定closed)

以下4セクションを`<details>`(既存の`.journal-prompts`/`.lev-helper`と同じ折りたたみ流儀)で
包み、**既定を閉じた状態**にした:

| セクション | 変更前 | 変更後 |
|---|---|---|
| 三つの信条 | 常時展開 | 折りたたみ・既定closed |
| 寿命カウントダウン(今年/45歳/80歳) | 常時展開・無題(見出しテキスト自体が無かった) | 折りたたみ・既定closed。見出し「寿命カウントダウン(残り時間)」を新設 |
| 長い弧をたしかめる(12週サイクル/未完了タスク/開いている問い/週次リンク) | 常時展開 | 折りたたみ・既定closed |
| 今日の足あと(完了リング・充電収支) | 常時展開 | 折りたたみ・既定closed |

- 開閉状態は**state本体(GitHub同期対象)とは別の**専用localStorageキー
  (`taskchute-journal-home-fold-v1`)に端末ローカルで記憶する(`homeFoldSection`/
  `isHomeFoldOpen`/`setHomeFoldOpen`)。同期データを汚さない設計判断(既存の
  `AUTO_MORNING_PLAN_KEY`等と同じ「非致命・try/catch」流儀を踏襲)。
- スコアボードの「長い弧」ジャンプ(`home-jump`→`homezone-3`)は、対象が折りたたまれていると
  中身が見えないため、**閉じていれば自動的に開いてからスクロールする**よう`home-jump`ハンドラを
  拡張した(既存の動作を壊さないための追加対応)。

**変更していないもの**: 「今日、すすめる」「今日のリズム」の2ゾーン(実行系)は折りたたみ化して
いない。指示の「参照系(信条・寿命カウントダウン・足あと等)」の対象に含まれないと判断したため。

---

## 3. 散らばったAI系表示の集約(app.js: homeAiHub 新設)

**変更前**: 以下の3つがホーム上の別々の場所に分散していた。
1. 鮮度インジケータ(`aiFreshnessLine`)— ヘッダー直下に単独表示
2. 「AIが処理した作業」カード(`homeAiWork`)— 信条・寿命カウントダウンの下に単独表示
3. 「昨日のフィードバックからの候補」(MIT候補)— `homeMIT`関数の内部に埋め込み表示

**変更後**: 上記3つを**「AIから」という1つの集約カード**(`homeAiHub`、`.home-ai-hub`)にまとめ、
最上部(Now・MITの直後)に配置した。カード内の構成は上から: 鮮度インジケータ → AI作業結果
(あれば) → 前日AIフィードバックのMIT候補(あれば)。

- 中身のロジック自体(`pendingAiWorkResults`/`aiWorkResultRowHTML`/`aiFreshnessLine`/
  `extractMITCandidatesFromReport`)は無変更。置き場所だけを移動した。
- MIT候補の追加アクション(`mit-candidate-add`)も無変更のまま動作する(移動しただけ)。
- **「AIプラン」については意図的にホームへ持ち込んでいない**: `../taskchute-notes`側の設計方針
  (CLAUDE.md「新機能でAI提案系を作るときは、新しいUIを作らず既存の`_scheduleDraft`+draftレイヤに
  流し込む」)どおり、AIプランは既にタイムラインタブの下書きスケジュールバー(`draftBarHTML`、
  タイムライン表示の最上部)として実装済みであり、ホームに複製すると同じ情報が2箇所に分散する
  ことになるため据え置いた。

**DOM構造の変更に伴うテスト更新**: `tests/v67.test.js`の「AIが処理した作業」カード検出セレクタが
`.home-plabel:has-text(...)`(旧: 独立カードの見出し)から`.home-ai-sub:has-text(...)`(新: 集約
カード内のサブ見出し)に変わったため、該当テストを更新した(UI変更に伴う正当な更新。機能自体は
検証内容も含めて無変更)。

---

## 4. CSS(styles.css)

- `.home-fold` / `.home-fold-summary` / `.home-fold-chevron` / `.home-fold-body`: 折りたたみ
  カードの共通スタイル(`.journal-prompts`と同じ「▶マーカーをspanで表示・webkit既定マーカーは
  非表示」の流儀)。
- `.home-ai-hub` / `.home-ai-sub`: 「AIから」集約カードとカード内サブ見出しのスタイル。
- `.home-creed`の横並びレイアウト(画面幅760px以上)を、`<section>`直下から`<details>`の
  `.home-fold-body`直下に付け替えた(信条カードを折りたたみ化したことに伴う、デスクトップ幅での
  レイアウト崩れ防止。モバイル(iPad/iPhone、既定の使用環境)の見た目は変更していない)。

---

## 5. Service Worker

- `sw.js`: `CACHE_NAME`を`taskchute-journal-pwa-v70`→`taskchute-journal-pwa-v71`に更新。

---

## 6. テスト

- `tests/v71.test.js`(新規): タブ順(navItems)、mobileNav不変更の確認、ホーム折りたたみ4種の
  既定closed、開閉のlocalStorage記憶とリロード後の維持、「AIから」集約カード(鮮度+AI作業結果+
  AIフィードバック候補が1カードにまとまっていること・候補追加アクションが動くこと・MIT3件で
  候補セクションが出ないこと)、スコアボードジャンプの新ターゲット(`#home-mit-anchor`)と
  折りたたみ自動オープンを検証。Clock APIで時刻固定(10:00固定)+AIプラン/AIフィードバック/
  週次レビューの実ファイルfetchを`page.route`で常に404隔離(既存v67〜v70スイートと同じ方針)。
- `tests/v67.test.js`(更新): 「AIが処理した作業」カード検出セレクタを`.home-plabel`から
  `.home-ai-sub`に更新(上記3節参照。UI構造変更に伴う正当な更新、検証内容は無変更)。
- 全量`npm test`(19スイート、v71含む)で **ALL PASS** を確認済み。

---

## 実装判断(仕様から補った点)

1. **「今日の理想」(homeIdeal)は折りたたみ化しなかった**: 指示の参照系リストに明示されておらず、
   1行のみの軽量表示ですでに情報量が小さいため、畳む効果が薄いと判断し常時表示のまま残した。
2. **「今日、すすめる」「今日のリズム」ゾーンは折りたたみ化しなかった**: 実行系(その日に何度も
   参照・操作する)コンテンツのため、指示の「参照系」の対象外と判断した。
3. **AIプランはホームへ集約しなかった**(上記3節参照)。
4. **mobileNavは変更しなかった**(上記1節参照)。
5. **折りたたみの開閉状態はstate本体ではなく専用localStorageキーにした**: 指示に「折りたたみ状態
   はlocalStorage記憶」と明記されていたため、GitHub同期・エクスポート対象の`state`オブジェクトとは
   分離し、端末ローカルのUI設定として扱った。

## 変更ファイル

- `app.js`(navItems順序、renderHome構成、homeCreedBody/homeLifespanBody分離、homeAiHub新設、
  aiFeedbackCandidatesHTML分離、homeScoreboardジャンプ先、home-jump自動オープン、
  折りたたみ状態のlocalStorageヘルパ、toggleイベント委譲)
- `styles.css`(`.home-fold*` / `.home-ai-hub` / `.home-ai-sub` / `.home-creed`デスクトップ
  レイアウト付け替え)
- `sw.js`(`CACHE_NAME`を`v70`→`v71`)
- `tests/v71.test.js`(新規)
- `tests/v67.test.js`(セレクタ更新)
- `CHANGES_v71.md`(本ファイル)
- `../taskchute-notes/handoff.md`(notes リポジトリ側、本エントリ追記)

## 実機テスト手順

1. `sw.js?nocache=YYYYMMDD`を開き、1行目が`taskchute-journal-pwa-v71`になっていることを確認。
2. サイドバー(またはモバイルの「その他」)のタブ順が、ホーム/タスクシュート/タイムライン/WBS/
   ルーティン/ジャーナル/週次/日報/計器盤/やりたい/やらない/ビジョン/0秒思考/ポモドーロ/設定の
   順になっていることを確認する。下部4タブ(ホーム/WBS/実行/時間)は従来どおりであることを確認。
3. ホーム画面で、上から「いま、これ」→「今日の主役」→「AIから」→スコアボード→
   「今日、すすめる」→「今日のリズム」の順に並び、その下に「三つの信条」「寿命カウントダウン」
   「長い弧をたしかめる」「今日の足あと」が折りたたまれた状態(▶マーク)で並んでいることを確認する。
4. 折りたたみをタップして開き、リロードしても開いたままであることを確認する(端末内保持)。
5. スコアボードの「12週 今週」セルをタップし、「長い弧をたしかめる」が閉じていても自動で開いて
   スクロールすることを確認する。
6. 前日のAIフィードバックにMIT候補が含まれる状態で、「AIから」カード内に「🤖 昨日のフィードバック
   からの候補」が表示され、「＋ 主役に」で今日の主役に追加できることを確認する。
7. 既存のWBS/タスクシュート/タイムライン/朝プラン/週次レビュー/レバレッジ台帳/ポモドーロ自動起動/
   Now画面/一括承認(v39〜v70)の動作が壊れていないことを一通り確認する。

## 確認サイン

- [ ] 実機(iPad/iPhone)で上記手順を確認
- [ ] 確認者:
- [ ] 確認日:
