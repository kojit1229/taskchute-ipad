<!-- generated by scripts/release-record.js; edit releases/vNNN.json instead -->
# CHANGES v335: 実行タブ: PCサイドバー/その他メニュー統合・旧ビュー呼び出しのexec寄せ

## 概要

v333/v334で2版連続持ち越しだった§C(review-v333-claude-a.md M-4)を回収した。PCサイドバー(navItems)の「タスクシュート」「タイムライン」2項目を「実行」1項目へ統合した(「その他」メニューのmoreItemsには元々この2項目が単独では含まれていなかったため対応不要と確認済み)。app.js内に残っていた最後の2箇所のsetView("timeline")呼び出し(energy-open-category、AI下書きスケジュール確定)をsetView("exec")+_execModeへ寄せた。旧tasks/timelineビュー自体はrender()の分岐に残し、直接setViewしても壊れない(ナビの「実行」がactiveになるようrenderSidebar/renderBottomNavのフォールバックを追加)。既存E2E(today-core/tower-core/v107/v108/v144/v199/v267/v306/v307/v50/v53/v57/v60/v62/v65/v76/v85/v86/v97/v98/github-state-blob-fallback)のうちPCサイドバー経由で旧ビューへ到達していた箇所をexec+モード指定へ全件セレクタ追随した。

## 変更ファイル

- CHANGES_v335.md
- app.js
- docs/test-impact-map.generated.md
- releases/v335.json
- src/features/timeline.js
- sw.js
- tests/github-state-blob-fallback.test.js
- tests/suite-manifest.json
- tests/today-core.test.js
- tests/tower-core.test.js
- tests/v107.test.js
- tests/v108.test.js
- tests/v144.test.js
- tests/v199.test.js
- tests/v267.test.js
- tests/v306.test.js
- tests/v307.test.js
- tests/v335.test.js
- tests/v50.test.js
- tests/v53.test.js
- tests/v57.test.js
- tests/v60.test.js
- tests/v62.test.js
- tests/v65.test.js
- tests/v76.test.js
- tests/v85.test.js
- tests/v86.test.js
- tests/v97.test.js
- tests/v98.test.js

## 変更内容

- app.js: navItems配列から{id:"tasks"}{id:"timeline"}を削除し、{id:"exec", label:"実行", mark:"E"}を今日の直後へ追加した。renderSidebar()の未着手バッジ条件をitem.id==="tasks"からitem.id==="exec"へ変更した。
- app.js: 「energy-open-category」ハンドラ(app.js:1064付近)のsetView("timeline")をsetView("exec")+_execMode="actual"へ変更した(元々実績時間帯のカテゴリフィルタ用途のため実績モードに寄せた)。
- app.js: 「AI下書きスケジュール確定」(runAiSchedule内、app.js:3890付近)のsetView("timeline")をsetView("exec")+_execMode="plan"へ変更した(直前でstate.timelineMode="planned"にしており、実績モード固定だとrenderTimelineViewのmodeが強制的に"actual"へ上書きされ下書きのドラッグ調整UI(renderDraftLayerはmode==="planned"限定)が見えなくなるため、energy-open-categoryとは異なり計画モードへ寄せた。uncertainties参照)。
- app.js: renderSidebar()/renderBottomNav()のactive判定を「navItems/mobileNavに無いビューは常に『その他』」から「旧tasks/timeline滞在中は『実行』をactiveにし、それ以外の未知ビューだけ『その他』」へ変更した(review-v333-claude-a.md M-4「どのボタンもアクティブでない画面を作らない」への対応)。
- sw.jsのCACHE_NAMEをv335へ更新。
- src/features/timeline.js: renderTimelineRail()をexecの計画モードでも表示する変更を試みたが(execTargetBlocksで絞り込まれた一覧と、railの簡易タイムラインが同じdata-id・data-actionを別クラスで重複描画し、既存E2Eのdata-id一意セレクタ前提を広く壊す副作用が判明したため)撤回し、現状(state.currentView==="tasks"のときのみ表示)を維持した。コメントで検討経緯と却下理由を記録した(uncertainties参照)。

## 変更意図

- PCサイドバー・「その他」メニューのナビ語彙を実行(exec)へ完全に一本化し、旧2ビューへの直接導線を消す。
- コード内に残っていた最後のsetView("timeline")呼び出し2箇所をexecへ寄せ、v333で新設したexecラッパーを唯一の到達経路にする。
- 既存E2Eの改修を先送りせず全件追随し、v333/v334から2版連続だった持ち越しを解消する。

## 検証

- node --check app.js / src/features/timeline.js / sw.js はいずれも成功。
- 実行コード差分(git diff --numstat -- app.js sw.js src。styles.cssはuncertainties(a)の理由で本バージョンの意図した変更に含めない): app.js 27+/7- ・ src/features/timeline.js 11+/4- ・ sw.js 2+/1- の合計47行(200行以内)。
- npm.cmd run test:manifest:write / node scripts/test-manifest.js --check はPASS。
- CHROME_PATH指定でnode tests/run-all.js v335 v334 v333 v332 v331 today-core tower-core v50 v307 action-registry-core v313 timeline-render-core timeline-tick-wiring + 今回セレクタ追随した全スイート(github-state-blob-fallback/v107/v108/v144/v199/v267/v306/v53/v57/v60/v62/v65/v76/v85/v86/v97/v98)をフォアグラウンド(timeout 600000)で2回実行。詳細な結果は impl-v335-report.md参照。
- git status --shortは変更ファイル一覧(app.js/sw.js/src/features/timeline.js/上記テストファイル/生成物・release記録)と一致。styles.cssの差分は本バージョン着手前から存在していた由来不明のWIP(uncertainties(a))で、この版では手を加えていない(元の状態のまま)。

## 既知の制約・保留

- renderTimelineRail()(#timelineRailのエネルギーレール等)はexecの計画モードでは表示されない(uncertainties(b))。旧tasksビューへ直接到達する経路(テストのseed等)以外でこのUIを見る手段は現状無い。
- tests/v267.test.js・tests/v277.test.js・tests/v65.test.jsに本バージョン非起因の既存不具合が残る(uncertainties(d)(e)参照、対象外として未修正)。
- styles.cssに由来不明の既存WIP(16行)が残っている(uncertainties(a)参照、K確認推奨)。
