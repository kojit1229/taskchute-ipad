# v120 睡眠CSV取込の修正

## 根因

- `起床時間`の正規表現が`YYYY-MM-DD HH:mm`の各桁を固定しており、AutoSleepのロケール差による`/`・`.`区切り、1桁の月日/時を読めなかった。
- 読めない行は通知なしで破棄していたため、一部の日付だけ入る「成功に見える部分取込」になった。
- 読取不能時は再描画されずfile inputの値が残るため、同名ファイルの再選択で`change`が発火しない経路があった。

7/18の元CSVは残っていないため、当日行が「CSVに無かった」のか「表記差で破棄された」のかは断定できない。ただし7/18の取込が7/17までで止まった事実はどちらでも説明でき、7/19に睡眠取込の形跡がない事実は同名ファイル再選択が発火しなかった可能性と整合する。

## 変更点

- 日時をDateへ渡さず、正規表現で1桁月日/時、秒の有無、`-`・`/`・`.`区切りを正規化する。
- 起床時間を読めない行を数え、生値を`console.warn`へ出す。
- トーストに取込日付範囲とスキップ数を表示し、最新起床日が今日より古ければ今朝分の欠落を警告する。
- file inputを選択直後にリセットし、同じファイルを再選択可能にする。
- Service Workerキャッシュをv120へ更新する。

## 検証手順

1. `node --check app.js`
2. `node tests/run-all.js v105 v120`
3. ジャーナルで表記揺れを含むAutoSleep CSVを取り込み、日付範囲・スキップ数・今朝分欠落の表示を確認する。
4. 同じCSVを続けて選択し、2回とも処理されることを確認する。
